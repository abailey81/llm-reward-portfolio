# DEVIATIONS — post-freeze deviation log

This file is an **append-only** record of every departure from the frozen pre-registration
(`PREREGISTRATION.md`) made **after** the Phase-1 freeze.

`PREREGISTRATION.md` is FROZEN after Phase 1 (CLAUDE.md prime directive 3): the hypotheses,
candidate budget, seeds, fitness, the frozen tail-diagnostic set, splits, embargo, benchmark suite,
and analysis plan cannot be silently edited. Where the executed work must diverge from the frozen
plan — whether a forced change, a discovered defect, or an approved amendment — the deviation is
logged **here** rather than by rewriting the pre-registration, so the frozen document stays a true
record of what was committed to in advance and this log stays a true record of what actually
happened and why.

Rules:
- **Append only.** Never edit or delete an existing row; correct a row by appending a new one that
  supersedes it (reference the original date).
- **One row per deviation.** Record the date (UTC, `YYYY-MM-DD`), the pre-registration section
  affected, what changed, the rationale, and whether the user explicitly approved it (a change to a
  frozen decision requires sign-off — CLAUDE.md directive 3 / stop-and-ask trigger).
- An empty table below means **no post-freeze deviations have been recorded yet** (the executed work
  matches the frozen pre-registration).

| Date (UTC) | Prereg section | Deviation | Rationale | Approved by |
| ---------- | -------------- | --------- | --------- | ----------- |
| 2026-07-31 | §13-continued **R88** (`model_suite` queue semantics, ops-only) — and, on the other reading, the COMPLETION of **R101** | **The intra-user SGE `-p` ladder is retired; every job we submit now goes out at `-p 0`.** Changed in `src/cluster/campaign.py`: `PRIORITY_STAGE1 −100 → 0`, `PRIORITY_RUNG_BASE −300 → 0`, `PRIORITY_H3_SINGLESHOT −100 → 0`, the per-block descent `−10·(i−2)` removed, and `_core_priority()` now returns `PRIORITY_CORE` for **every** arm instead of only the two H2 arms and `bayes_opt`. No registered QUANTITY changes: seeds, arms, candidate budget, fitness, α, BH q, SESOI, the TOST margin, splits, embargo, the benchmark suite, the stopping rules and the single-look discipline are all untouched. | **R88 assumed `-p` is an intra-user ordering knob. It is not — it is a GLOBAL POSIX priority, and Myriad weights it at `weight_priority = 4.0`, the largest weight in `qconf -ssconf`.** R88's own text relied on the scheduler "starving back-of-queue work first", which holds only while we are effectively the pool's sole consumer. **R101 (Okhrati seed-parity, all lines in LOCKSTEP) already SUPERSEDED R88's ladder** and `mode_d_launch.ps1` was updated at the time ("No line passes `--priority` any more"); the arm-level and rung-level ladders inside `campaign.py` were never updated — a half-applied amendment. **MEASURED LIVE 2026-07-31 at 384 cores, down from 728:** our pending jobs `prior` 1.811–1.828, every one at `-p −100`; four other users sampled at `prior` 2.000–2.082, every one at `-p 0`; **2,144 of 2,646 cluster-wide pending jobs outranked us**; pool d held 2,480 free slots and 246 placeable 8-slot jobs, so the capacity existed. The predicted penalty `4.0 × [(0+1023)/2047 − (−100+1023)/2047] = 0.195` matches the observed 0.189. **120 of our 124 queued jobs were the three CONTROL arms** (`placebo` 41, `placebo_shuffled` 42, `scalar_cvar5` 37) while every RUNNING treatment-arm job sat at `-p 0` — i.e. the ladder was starving precisely the arms that isolate the effect of the feedback CONTENT, and had opened a **four-generation depth gap** (treatments g5, controls g1–g3). Because a line completes only when its SLOWEST arm completes, this also gated the entire campaign. At C4 it was worse: blocks 2+ descended to −300…−340, putting the **top seed rungs the power analysis rests on** at the lowest standing we can hold (penalty 0.66) against an exogenous 2026-08-27 stop. **The deviation therefore INCREASES fidelity to the registered design**: it removes a differential-depth threat to H2's identification, restores R101's lockstep, and honours the standing absolute rule *never lower the SGE priority of any of our jobs, ever*. Effect-blind by construction — `-p` changes WHEN a job runs, never WHAT it computes, so it sits outside the determinism envelope exactly as `pack` does. Rung ORDER survives without the ladder: blocks are submitted in rung order and `weight_waiting_time = 1.0`, so an earlier block outranks a later one on age; the cumulative-tier BANKING rule is enforced in analysis, never by the scheduler. **Why logged here and not as an amendment row:** `canonical_bytes()` hashes the WHOLE of `PREREGISTRATION.md`, and `freeze.py` forbids re-freezing — so a post-freeze amendment row is impossible without moving the frozen hash. Verified after the change: `freeze --check` RC=0 with the canonical hash **`3ca6f01a…` UNMOVED**. Full narrative, evidence and the falsification record: `docs/CAMPAIGN_EXECUTION_RECORD.md` §54. | **Tamer** — standing full delegation (2026-07-13), reaffirmed explicitly 2026-07-31 ("I give you absolutely full permissions… maximise the speed to an absolute maximum possible"), and the change enforces his own absolute standing rule. Ratified on his behalf; flagged here for his review as the first post-freeze deviation. |
