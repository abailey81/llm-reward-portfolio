# ACKNOWLEDGED SENTINEL VERDICTS — one "check:severity" per line.
#
# An entry here means: INVESTIGATED, UNDERSTOOD, and consciously accepted for now, with the reason
# recorded. `campaign_watch.py` reports these as (ack) and does not raise the alert level; anything
# NOT listed here is reported as (NEW) and DOES alert.
#
# The point is alarm hygiene. D15 survived ten hours because a CRITICAL sat in a JSONL beside six
# green guards. The countermeasure is not to lower the bar but to make the KNOWN quiet so the NEW is
# loud. Never add a line here to silence something you have not run to ground — an unjustified entry
# re-creates exactly the failure this file exists to prevent.
#
# Remove an entry the moment its underlying condition is resolved.

# D15 (record s.28). Four `baseline_volatility_scaled_return` records (seeds 14-17) trained on the
# one Xeon Gold 6140 host in the estate, node-d00b-024, while the unit's other 26 ran on 6240s.
# TRIAGED: the fence (-l h=!node-d00a-230&!node-d00b-024) is live on all 12 lines and verified in
# newly-written jobscripts, so no FUTURE record can land there. H2 is untouched (every LLM-arm record
# is homogeneous) and the affected baseline is not the binding H1 max. OPEN: the bit-comparison
# experiment that decides whether the four archived records are replaced or kept with a measured
# equivalence. Remove this line when that experiment resolves.
substrate_fields:CRITICAL

# record_sanity WARN (record s.26.4). `baseline_differential_sharpe-s1` and -s5 each show exactly ONE
# safe-default call in 400,000 (0.00025%), caused by DSR's documented zero-denominator warm-up
# (A_0 = B_0 = 0, so the first step's denominator is literally (B - A^2)^1.5 = 0). True-positive
# alarm, benign by design, 40,000x below R115's 10% floor, and irrelevant to eligibility because R115
# governs LLM-authored candidates while this is a hand-written H1 comparator. Check deliberately left
# untouched: weakening it to quiet a known-benign signature is what this project refuses to do.
record_sanity:WARN

# silent_hang UNKNOWN — emitted once at 2026-07-28 22:09:17, at launch, with detail "no progress
# artifact to age-check". A start-up state, not a stall: there was nothing to age-check yet.
silent_hang:UNKNOWN

# gate_failure_drift WARN (CUSUM 0.21 > 0.15). TRIAGED 2026-07-30. The check is a CUSUM on the
# AGGREGATE gate-failure rate against target 0, and its sibling's docstring reads "a rising rate is an
# early sign of a systemic authoring fault". It is NOT systemic here — it is a MIXTURE artefact:
#   qwen3.5-9b  41 of 54 rejects (76%), 92% rate vs its OWN registered ~83% baseline
#   gemini-2.5-flash 6% vs ~17% · qwen3.6-27b 11% vs ~17% · gpt/haiku/kimi/sonnet 0%
# i.e. every strong model is AT OR BELOW its expected rate, and the drift is the deliberate
# capability-gradient BOTTOM ANCHOR doing exactly what it was selected to do (a registered FINDING,
# not a fault). The aggregate CUSUM cannot see that decomposition; the per-model `rejects` guard —
# which scores each model against its own baseline — is the authority, and it returns ok.
# ⚠ RE-TRIAGE IMMEDIATELY IF the per-model guard ever flags a STRONG model above its own baseline:
# that would be the systemic fault this check is actually looking for.
gate_failure_drift:WARN

# reward_scale WARN — "AUTHORED-arm reward-scale ratio 154x; baselines span 437099x (registered
# ratio-form rewards - fixed by formula, reported as context)". TRIAGED 2026-07-30: benign BY DESIGN,
# and the check's own detail says it is context rather than a fault. The huge baseline span is a
# FORMULA property - the H1 canon deliberately mixes difference-form rewards (return_minus_*) with
# RATIO-form ones (differential_sharpe, volatility_scaled_return, differential_downside_ratio), whose
# magnitudes are not commensurable by construction. It cannot bias the agent because the critic is
# scale-INVARIANT: PopArt value-target normalisation is ON (`popart: true` in both config/algos.yaml
# and config/prototype.yaml) and VERIFIED live - 59 of 59 sampled records carry a non-null
# `popart_scale`. Reward magnitude is therefore absorbed, not learned against.
reward_scale:WARN

# --- REPO GUARD verdicts (campaign_guards.py), prefix "guard:" ---
#
# guard:truncation CRITICAL — ONE of 1,099 LLM calls returned stop_reason `length`, i.e. it hit our
# own 16,384-token output cap: leg7 `nvidia/nemotron-3-super-120b-a12b`, 2026-07-30T08:22:49Z,
# $0.0149. The guard is RIGHT that this contaminates the authoring-reliability finding - a candidate
# that failed because OUR cap truncated it is an instrument artefact, not evidence the model cannot
# write executable reward code.
# SCOPE, measured: 1 of 1,099 calls run-wide (0.09%); within leg7, 1 of 114 calls, so 10 of
# nemotron's 11 rejects are GENUINE and only 1 is instrument-induced. No other line has any.
# WHY ACKNOWLEDGED RATHER THAN FIXED: the cap is REGISTERED (R106 - caps matched at 16,384 across all
# eleven models, which is the property that makes the cross-model comparison fair), so it must NOT be
# raised mid-run; and the spend ledger is append-only, so this verdict can never return to green. The
# handling is an ANALYSIS-TIME obligation (record s.9): exclude `stop_reason == "length"` rows from
# every authoring-reliability denominator, or report them separately. That is detectable precisely
# because `stop_reason` is persisted on every row (the 18dead8 fix earning its keep).
# ⚠ RE-TRIAGE IF the truncated-call count rises materially or appears on a SECOND model: one verbose
# outlier is an artefact, a pattern would mean the matched cap is too low to measure some models at all.
guard:truncation

# capacity_accumulation WARN — "concurrency DECLINING to ~1016 cores; check for a kill event, a
# drained queue, or a cluster-wide load spike". TRIAGED 2026-07-30 by MEASUREMENT: none of the three
# suspected causes. It is the GENERATION DRAIN, and it is a data dependency rather than a fault.
# Measured across 56 arm-slots: pending depth 5 on 14 slots, 4 on 4, 3 on 8, 2 on 6, and **1 on 24** —
# average 2.61 in flight against a design peak of 5, i.e. ~52% utilisation. An arm submits 5 candidates
# and cannot author generation g+1 until ALL of g returns, so every generation drains 5 -> 1 before the
# next begins. Sum of pending (146) matches the scheduler's submitted total (143) almost exactly, and
# the scheduler is placing 115 of 143 (80%) — so Myriad is MEETING our demand; we are simply not asking
# for more, because there is nothing more to ask for during a serial reflection chain.
# ⚠ The search phase is LATENCY-bound, not throughput-bound: its length is 6 x (training + authoring),
# so extra cores would sit IDLE and would not shorten it. Cores become decisive at C4 (39,760 trainings
# at rung 568). RE-TRIAGE IF concurrency declines while the QUEUED backlog is deep — that would be a
# genuine placement failure rather than a drained pipeline.
capacity_accumulation:WARN
