# ACKNOWLEDGED SENTINEL VERDICTS — one "check:severity" per line.
#
# An entry here means: INVESTIGATED, UNDERSTOOD, and consciously accepted for now, with the reason
# recorded. `campaign_watch.py` reports these as (ack) and does not raise the alert level; anything
# NOT listed here is reported as (NEW) and DOES alert.
#
# The point is alarm hygiene. D15 survived ten hours because a CRITICAL sat in a JSONL beside six
# green guards. The countermeasure is not to lower the bar but to make the KNOWN quiet so the NEW is
# loud. Never add a line here to silence something you have not run to ground — an unjustified entry
# re-creates exactly the failure this file exists to prevent.
#
# Remove an entry the moment its underlying condition is resolved.

# D15 (record s.28). Four `baseline_volatility_scaled_return` records (seeds 14-17) trained on the
# one Xeon Gold 6140 host in the estate, node-d00b-024, while the unit's other 26 ran on 6240s.
# TRIAGED: the fence (-l h=!node-d00a-230&!node-d00b-024) is live on all 12 lines and verified in
# newly-written jobscripts, so no FUTURE record can land there. H2 is untouched (every LLM-arm record
# is homogeneous) and the affected baseline is not the binding H1 max. OPEN: the bit-comparison
# experiment that decides whether the four archived records are replaced or kept with a measured
# equivalence. Remove this line when that experiment resolves.
substrate_fields:CRITICAL

# record_sanity WARN (record s.26.4). `baseline_differential_sharpe-s1` and -s5 each show exactly ONE
# safe-default call in 400,000 (0.00025%), caused by DSR's documented zero-denominator warm-up
# (A_0 = B_0 = 0, so the first step's denominator is literally (B - A^2)^1.5 = 0). True-positive
# alarm, benign by design, 40,000x below R115's 10% floor, and irrelevant to eligibility because R115
# governs LLM-authored candidates while this is a hand-written H1 comparator. Check deliberately left
# untouched: weakening it to quiet a known-benign signature is what this project refuses to do.
#
# ── ★ RE-SCOPED 2026-07-31 (RUN 8, record §67.3). THE ENTRY ABOVE WAS UNDER-SCOPED THREE WAYS. ──
# Re-measured from the raw archive (1,461 records, `frozen/` and `.pull_tmp` excluded). The triage
# above is CORRECT in mechanism and verdict but names the wrong extent, and a stale acknowledgement is
# exactly the rot this file exists to prevent (the same failing already re-triaged once below, in
# record_sanity:CRITICAL):
#   1. it names TWO records; there are **NINE** hand-written-baseline records with 0 < fallback < 1e-4
#      (`baseline_differential_sharpe` at seeds 1, 5, 9, 21, 24 -- not just 1 and 5).
#   2. it names ONE baseline; there are **TWO**. `baseline_differential_downside_ratio` (seeds 7, 16,
#      24, 27) is affected and is not mentioned at all -- and seed 27 shows **2** safe-defaults, so
#      "exactly ONE" is also wrong. This CORROBORATES rather than undermines the stated cause:
#      `differential_downside_ratio` is, like `differential_sharpe`, a RATIO-form reward with the same
#      zero-denominator warm-up, so it is precisely where a second instance was predicted to appear.
#   3. it asserts the affected class is hand-written comparators only ("R115 governs LLM-authored
#      candidates while this is a hand-written H1 comparator"). **SEVEN LLM-AUTHORED records are
#      affected**, across six legs: glm_5_2/scalar-g3-c3 (15 of 400,000), gemini_2_5_flash/
#      distributional-g0-c4 (3), nemotron_3_super/placebo_shuffled-g2-c0 (2), qwen3_6_27b/
#      scalar_cvar5-g0-c3 (2), deepseek_v4_pro/scalar-g0-c2 (1), glm_5_2/scalar_cvar5-g0-c1 (1),
#      nemotron_3_super/distributional-g5-c4 (1).
# STILL BENIGN, and the verdict is UNCHANGED: the worst fraction anywhere is **0.00375 %**, i.e.
# **2,667x below R115's 10 % floor**, and there are **ZERO on the confirmatory core line**. The check
# stays untouched -- weakening it to quiet a known-benign signature is what this project refuses to do.
# ⚠ RE-TRIAGE IF any of these crosses ~1 % (three orders of magnitude of headroom would be gone), if
# one appears on the CORE line, or if a THIRD ratio-form baseline joins -- the last would mean the
# warm-up is systematic across the ratio family rather than incidental.
record_sanity:WARN

# silent_hang UNKNOWN — emitted once at 2026-07-28 22:09:17, at launch, with detail "no progress
# artifact to age-check". A start-up state, not a stall: there was nothing to age-check yet.
silent_hang:UNKNOWN

# gate_failure_drift WARN (CUSUM 0.21 > 0.15). TRIAGED 2026-07-30. The check is a CUSUM on the
# AGGREGATE gate-failure rate against target 0, and its sibling's docstring reads "a rising rate is an
# early sign of a systemic authoring fault". It is NOT systemic here — it is a MIXTURE artefact:
#   qwen3.5-9b  41 of 54 rejects (76%), 92% rate vs its OWN registered ~83% baseline
#   gemini-2.5-flash 6% vs ~17% · qwen3.6-27b 11% vs ~17% · gpt/haiku/kimi/sonnet 0%
# i.e. every strong model is AT OR BELOW its expected rate, and the drift is the deliberate
# capability-gradient BOTTOM ANCHOR doing exactly what it was selected to do (a registered FINDING,
# not a fault). The aggregate CUSUM cannot see that decomposition; the per-model `rejects` guard —
# which scores each model against its own baseline — is the authority, and it returns ok.
# ⚠ RE-TRIAGE IMMEDIATELY IF the per-model guard ever flags a STRONG model above its own baseline:
# that would be the systemic fault this check is actually looking for.
gate_failure_drift:WARN

# reward_scale WARN — "AUTHORED-arm reward-scale ratio 154x; baselines span 437099x (registered
# ratio-form rewards - fixed by formula, reported as context)". TRIAGED 2026-07-30: benign BY DESIGN,
# and the check's own detail says it is context rather than a fault. The huge baseline span is a
# FORMULA property - the H1 canon deliberately mixes difference-form rewards (return_minus_*) with
# RATIO-form ones (differential_sharpe, volatility_scaled_return, differential_downside_ratio), whose
# magnitudes are not commensurable by construction. It cannot bias the agent because the critic is
# scale-INVARIANT: PopArt value-target normalisation is ON (`popart: true` in both config/algos.yaml
# and config/prototype.yaml) and VERIFIED live - 59 of 59 sampled records carry a non-null
# `popart_scale`. Reward magnitude is therefore absorbed, not learned against.
#
# ---- CORRECTED 2026-07-30 16:10 UTC (record s.44.4). THE TRIAGE ABOVE OVERSTATES THE
# PROTECTION. Carrying a non-null `popart_scale` proves the mechanism is INSTRUMENTED, not
# that it ACTED. `popart_min_scale: 1.0` means sigma = max(1.0, raw_rms), and MEASURED over
# the 1,024 records carrying the field: **509 engaged (sigma_max > 1.0), 515 PINNED at the
# floor** - i.e. PopArt is inert on 50.3% of the archive, systematically by reward family
# (3 of 11 baselines engaged, all four DFO arms pinned, the LLM arms 62-67% engaged).
# WHAT SURVIVES, and it is the half that matters: the engaged fraction is UNIFORM across the
# five LLM arms (65.5 / 65.2 / 67.1 / 67.4 / 62.1 %), so the property is ARM-SYMMETRIC and
# cannot confound H2. Where it is asymmetric is H1, whose eleven baselines split perfectly by
# ratio-form vs difference-form. The floor is not a bug - it guards against sigma->0
# amplification - but the CLAIM was wrong, and an analysis-time obligation now stands:
# report the PopArt engagement rate beside the H1 family comparison.
reward_scale:WARN

# --- REPO GUARD verdicts (campaign_guards.py), prefix "guard:" ---
#
# guard:truncation CRITICAL — ONE of 1,099 LLM calls returned stop_reason `length`, i.e. it hit our
# own 16,384-token output cap: leg7 `nvidia/nemotron-3-super-120b-a12b`, 2026-07-30T08:22:49Z,
# $0.0149. The guard is RIGHT that this contaminates the authoring-reliability finding - a candidate
# that failed because OUR cap truncated it is an instrument artefact, not evidence the model cannot
# write executable reward code.
# SCOPE, measured: 1 of 1,099 calls run-wide (0.09%); within leg7, 1 of 114 calls, so 10 of
# nemotron's 11 rejects are GENUINE and only 1 is instrument-induced. No other line has any.
# WHY ACKNOWLEDGED RATHER THAN FIXED: the cap is REGISTERED (R106 - caps matched at 16,384 across all
# eleven models, which is the property that makes the cross-model comparison fair), so it must NOT be
# raised mid-run; and the spend ledger is append-only, so this verdict can never return to green. The
# handling is an ANALYSIS-TIME obligation (record s.9): exclude `stop_reason == "length"` rows from
# every authoring-reliability denominator, or report them separately. That is detectable precisely
# because `stop_reason` is persisted on every row (the 18dead8 fix earning its keep).
# ⚠ RE-TRIAGE IF the truncated-call count rises materially or appears on a SECOND model: one verbose
# outlier is an artefact, a pattern would mean the matched cap is too low to measure some models at all.
#
# ── RE-TRIAGED 2026-07-30 14:00 UTC: THE SECOND-MODEL TRIGGER ABOVE HAS FIRED. ──
# A second `length` row appeared: leg10 `moonshotai/kimi-k3-20260715`, 2026-07-30T11:19:39Z,
# tokens_in=581, tokens_out=16384 (exactly the cap), $0.2475. Run-wide the count is now **2 of 1,311
# calls (0.15 %)**, on two models, both OpenRouter legs, and **none on the confirmatory core line c1**.
# VERDICT: still an artefact, not a pattern, and the handling is unchanged — but the honest reading is
# now "two verbose outliers on two models", not "one". Supporting evidence that it is not a capability
# signal: kimi-k3's gate-reject rate is 0 % across 46 records, i.e. the truncated call did not become a
# rejected candidate, so the cap has cost us at most one candidate on one leg. nemotron's own rate
# (21 %) is unchanged by removing its single truncated call.
# THE CAP STAYS AT 16,384: it is registered (R106) and matching it across all eleven models is exactly
# what makes the cross-model comparison fair; raising it mid-run would break that, and the spend ledger
# is append-only so this verdict can never return green.
# ⚠ RE-TRIAGE AGAIN IF a THIRD model truncates, if any single model exceeds ~1 % of its own calls, or
# if a `length` row ever appears on `c1` — any of those would mean the matched cap is biting the
# measurement rather than clipping an outlier.
#
# ── ★ THE THIRD-MODEL TRIGGER HAS FIRED. Re-triaged 2026-07-31 17:0x UTC, RUN 8 (record §62.5). ──
# Checked because the entry above was last evaluated at 1,311 calls and volume had since grown ~80 %;
# a trigger nobody re-runs is decoration. Measured over all 2,361 ledger rows:
#   nvidia/nemotron-3-super-120b-a12b   2 of 215 own calls = 0.93 %
#   moonshotai/kimi-k3-20260715         1 of 209 own calls = 0.48 %
#   qwen/qwen3.6-27b                    1 of 205 own calls = 0.49 %   <-- THE THIRD MODEL
# Run-wide 4 of 2,361 = 0.17 %.
# VERDICT: STILL AN ARTEFACT, and the reason is the RATE, not the count. Across three evaluations the
# count has grown 1 -> 2 -> 4 while calls grew 1,099 -> 1,311 -> 2,361, i.e. 0.09 % -> 0.15 % -> 0.17 %.
# A roughly CONSTANT low rate against rising volume is a background population of verbose outliers;
# an instrument that was "too small to measure some models at all" would show a rate RISING with
# volume, or concentrate in one model. Neither holds.
# WHAT DID CHANGE, and it is the honest reading: the affected set is now THREE models rather than two,
# so the analysis-time exclusion (drop `stop_reason == "length"` from every authoring-reliability
# denominator, or report it separately) is more load-bearing than when it covered one leg. All three
# are OpenRouter REPORT-ONLY legs (R80).
# ✔ THE ONE THAT MATTERS IS STILL CLEAN: zero `length` rows on the confirmatory core line `c1`, so
# H2/H1 are untouched. Trigger 3 has NOT fired.
# ⚠ NEAREST LIVE EDGE: nemotron at 0.93 % is within a whisker of the 1 %-of-own-calls trigger. If it
# crosses, that IS the "cap is biting this model" signal and the per-model reliability figure for
# nemotron must be reported with the truncated calls excluded and the exclusion stated in the text.
# THE CAP STAYS AT 16,384 (registered R106; matching it across all eleven models is what makes the
# cross-model comparison fair, and the ledger is append-only so this verdict cannot return green).
guard:truncation

# capacity_accumulation WARN — "concurrency DECLINING to ~1016 cores; check for a kill event, a
# drained queue, or a cluster-wide load spike". TRIAGED 2026-07-30 by MEASUREMENT: none of the three
# suspected causes. It is the GENERATION DRAIN, and it is a data dependency rather than a fault.
# Measured across 56 arm-slots: pending depth 5 on 14 slots, 4 on 4, 3 on 8, 2 on 6, and **1 on 24** —
# average 2.61 in flight against a design peak of 5, i.e. ~52% utilisation. An arm submits 5 candidates
# and cannot author generation g+1 until ALL of g returns, so every generation drains 5 -> 1 before the
# next begins. Sum of pending (146) matches the scheduler's submitted total (143) almost exactly, and
# the scheduler is placing 115 of 143 (80%) — so Myriad is MEETING our demand; we are simply not asking
# for more, because there is nothing more to ask for during a serial reflection chain.
# ⚠ The search phase is LATENCY-bound, not throughput-bound: its length is 6 x (training + authoring),
# so extra cores would sit IDLE and would not shorten it. Cores become decisive at C4 (39,760 trainings
# at rung 568). RE-TRIAGE IF concurrency declines while the QUEUED backlog is deep — that would be a
# genuine placement failure rather than a drained pipeline.
#
# ── ★ THE RE-TRIAGE TRIGGER ABOVE FIRED, AND IT WAS RIGHT. 2026-07-31, record §54. ──
# Cores fell 728 -> 384 across one session while the backlog stayed DEEP (49 running / 124 queued),
# which is exactly the condition this entry said would mean a real placement failure. It did, and the
# cause was OURS: `_core_priority()` submitted the three CONTROL arms at `-p -100` while the two H2
# treatment arms rode at 0, and Myriad weights the POSIX `-p` field at **4.0**, the largest weight in
# `qconf -ssconf`. Measured: our pending jobs `prior` 1.811-1.828 against four other users at
# 2.000-2.082, all of whom submit at `-p 0`; **2,144 of 2,646 cluster-wide pending jobs outranked us**;
# 120 of our 124 queued jobs were controls. Pool d had 2,480 free slots — the capacity was there.
# FIXED: the whole §14.3 ladder is retired to 0 (R101 had already superseded it; only the launcher had
# been updated). Logged as the FIRST entry in DEVIATIONS.md. This WARN stays acknowledged because the
# generation-drain reading below is still the correct explanation of the *shallow* dips — but the
# trigger's wording is now proven, and the next time it fires the FIRST thing to check is our own `-p`.
capacity_accumulation:WARN

# record_sanity CRITICAL — "1/300 recent record(s) are GARBAGE (distributional-g2-c2): the authored
# reward failed on 399912/400000 steps (100%) and the agent trained mostly [on the default]".
# TRIAGED 2026-07-30, and it is a TRUE POSITIVE that is correctly handled twice over.
# LOCATED: the record is on the kimi-k3 LEG, not the confirmatory core (the core's own
# distributional-g2-c2 is clean at 0.0000% fallback). Its fallback is 99.978% and its val_fitness is
# 7.82e-06, i.e. essentially zero -- so it is excluded by R115 (>= 10% floor) AND would never have been
# selected on fitness anyway.
# THE SCIENTIFIC POINT, which is why this alarm is worth keeping loud: compare it with the qwen3.5-9b
# case at 49.98% fallback, whose val_fitness was +0.2336 -- the HIGHEST in its arm. A FULLY broken
# reward scores nothing and is self-limiting; a PARTIALLY broken reward can score BEST, because the
# harness default silently does half the work. That asymmetry is exactly why an execution-quality floor
# is necessary and why fitness alone cannot substitute for it.
# ⚠ RE-TRIAGE IMMEDIATELY IF such a record appears on the CORE line, or if any high-fallback record
# tops its arm on a line whose winner has not yet been frozen.
#
# ── THE SPECIFIC RECORD HAS CHANGED. Re-triaged 2026-07-31 (record §53.12). ──
# The sentinel now reports `scalar-g5-c3` at 234,771/400,000 = 58.69 %, on the glm-5.2 leg — NOT the
# kimi-k3 record described above. An acknowledgement that names the wrong instance is exactly the rot
# this file exists to prevent, so both are now covered:
#   * kimi_k3/distributional-g2-c2   99.98 %  — D17-adjacent, fully broken, self-limiting
#   * glm_5_2/scalar/scalar-g5-c3    58.69 %  — **NOT D17.** D17 produces RECIPROCALS (1/2 -> 49.983,
#     1/3 -> 33.333) because its period is the cold-start warm-up plus one; 58.693 % is not a
#     reciprocal of any integer, so this is a data-dependent failure — the ordinary genuinely-broken
#     class R115 exists to exclude. The distinction matters for the authoring-reliability finding: a
#     D17 record is biased AGAINST its own model (§37.6) and this one is not.
# BOTH are correctly handled: R115-excluded, and NEITHER tops its arm — glm's scalar arm is topped by
# `scalar-g0-c2` at val_fitness 0.2704 with 0.00000 % fallback, so the floor is not even load-bearing
# there. Zero breaches on the confirmatory core line; the trigger above has NOT fired.
record_sanity:CRITICAL
