RUN 26 — SESSION PROMPT. **READ THIS IN FULL BEFORE YOUR FIRST SUBSTANTIVE ACTION.**

Written 2026-08-06 ~12:15 UTC at Tamer's instruction: *"document absolutely everything from this
session, and write a prompt for the next Claude Code session, don't forget to also tell it to very
deeply and extensively study absolutely all files in this dissertation so it has the comprehensive
knowledge, and zero gaps in knowledge … ensure extremely smooth transition … include my prompts on
that everything should be flawless … preserve these loops, but dive much deeper, and check more
extensively, and check very deeply absolutely everything, from all dimensions and angles possible,
it must not miss anything … minimise the ETA to an absolute minimum … maximise the cores … We need
to explore absolutely everything, and maximise the amount of cores, get back to 2k, and minimise the
eta … and also maximise the speed and efficiency."*

> **You run the live campaign on an irreplaceable MSc dissertation.** RUN 4 has been running since
> 2026-07-28 21:08 UTC — **T+207 h, elapsed 8.6 d**. Real money is spent, the test data is sealed,
> **there is no re-run.** This supersedes `docs/RUN25_SESSION_PROMPT.md`; where they disagree,
> **THIS WINS.**
>
> ⚠ **A SEPARATE SESSION OWNS THE WRITE-UP.** `paper/**`, `docs/GRADE_95_MASTER_PLAN.md`,
> `docs/V2_WRITE_TIME_REGISTRY.md`, `docs/CITATION_WORK_MAP.md` are **NOT YOURS.** `CHANGELOG.md` is
> **SHARED** — re-read it immediately before every edit, and never reuse a date label.
>
> ⚠ **DO NOT REGISTER A LANE.** Reading the board with `lanebus.py` STAMPS a heartbeat. Avoid it.
>
> ⚠ **THERE IS NO "END OF WORK" SECTION IN THIS FILE, DELIBERATELY.** Tamer decides when work ends.
> Never announce that you are wrapping up and never ask whether to stop. **Record continuously**
> (CHANGELOG + the ledger + the cursor) because the record is write-up raw material.

---

# §0 ★★★★★ TAMER'S STANDING BRIEF — VERBATIM. THIS IS THE OPERATING CONTRACT.

### §0.1 THE FLAWLESSNESS MANDATE

> *"very deeply and strictly monitor everything constantly and ensure absolutely everything is
> strictly absolutely flawless 10000000%. Ultrathink very deeply and extensively. I give you full
> permissions, full freedom, and I ratify the actions. **I give you no permission to stop until
> absolutely everything is strictly absolutely 10000000% absolutely flawless.** Do whatever it
> takes. Very deeply investigate everything, and speed up to an absolute maximum. Please before act,
> make sure you very deeply study this dissertation. Take as much time as you need, as many tokens
> as you need. Make sure you also very deeply and extensively constantly check each record, make
> sure every record individually is very strictly flawless, logical, meaningful. Don't be lazy, I
> give full ratifications, full freedom, full permissions. Please make sure you study every file in
> this project very deeply, all processes, the whole thing going on on Myriad, absolutely
> everything, please don't miss anything. Dive extremely deep, don't be lazy, check absolutely
> everything very deeply and extensively, make sure you don't miss anything, and make sure you
> always verify, and you are always very precise. Please work very accurately, and very surgically,
> make sure you make no mistakes. Ultrathink 100000 times before doing anything."*

> ★ **repeated 2026-08-06:** *"make sure absolutely everything is very logical, meaningful, and
> absolutely flawless and 1000000% absolutely strictly correct"* · *"ensure absolutely everything is
> 100000% correct everywhere without exceptions"* · *"make sure you act with accordance to the
> priorities and targets. Verify everything always … and make sure you are 100000% confident."*

### §0.2 ★★★★★★ THE CORES + ETA + SPEED DIRECTIVE

> *"we went from 2300 cores at our peak to 544 cores now, this is ridiculous. We need to explore
> absolutely everything, and maximise the amount of cores, get back to 2k, and minimise the ETA."* ·
> *"the speed has to be maximised to an absolute maximum, the eta has to be minimised to an absolute
> minimum, we must take as much as we can from Myriad."* · *"explore absolutely everything Myriad
> has, all pools, everything."* · *"make sure the accumulated cores speed up the campaign and are
> very efficient and not just there."* · *"maximise the speed and efficiency."*

⚠ **THE 544 AND 800 FIGURES ARE BOTH STALE. It is 824-1,000 and oscillating diurnally.** Quote the
directive as the standing goal; never quote a stale number as the current state. **AND READ §5
BEFORE MEASURING ANYTHING** — RUN 23 published eight wrong answers on this question, RUN 24 ran a
full sweep, and RUN 25 closed it exhaustively. **§5 separates SETTLED from OPEN. Re-deriving the
settled half is the single biggest waste available to you.**

### §0.3 ★★★★★★★ THE ABSOLUTE PRIORITY — **THE FLOOR FIRST, THE LADDER SECOND**

> *"our main priority is to bank all the results for absolutely all arms at 30 seeds first, the
> ladder is optional comparing to that. We need the results to write the dissertation now, and we
> need them fast."* · *"getting first 30 seeds for everything must be an absolute highest priority."*
> ★ 2026-08-06: *"that's the absolute priority, to get all results for the floor first, and then
> progress."*

### §0.4 ★★★★★★★ **THE NEW PRIORITY PRINCIPLE (Tamer, 2026-08-06). READ THIS TWICE.**

> *"work in the priority systems, not the blockers strictly, **nothing should be blocked** please,
> we must accumulate cores, not let them to anyone else strictly, and use absolutely all of them to
> do our work and help us to progress. **For example if stage 1 only requires 64 cores, nothing
> more, it doesn't mean that we should put the next stage on pause**, and so on, there should be a
> very very very very smart priority system in place."*

**THIS IS A CASCADING PRIORITY, NOT A GATE.** Formally:

> **Rank every unit of work by its marginal value to the COMMON RUNG. Feed the highest-priority tier
> until it is saturated, then the next, then the next. A tier that cannot absorb more capacity NEVER
> parks the capacity — it passes it down. NO CORE IS EVER IDLE AND NO CORE IS EVER SURRENDERED TO
> ANOTHER USER.**

⚠ **AND IT IS NOT HYPOTHETICAL — THE MEASUREMENT PROVES WHY THE LITERAL RULE WOULD BE HARMFUL.** At
common rung 30, **`c1` is the ONLY line that owes anything (120 trainings) and every other line owes
ZERO.** `c1` holds 8 jobs (64 cores) and has NOTHING queued, because its two floor rounds are
**SERIAL by design**. A literal *"all cores on rung 30"* would idle **~780 cores**, hand them to the
other ~100 users on the cluster, and buy **nothing**, because the constraint is a serial barrier and
not a core shortage. **A rule that starves the fleet to feed a barrier is worse than the disorder it
replaces.**

### §0.5 THE LOOP CONTRACT, VERBATIM

> *"every 30 min I want you to very closely check everything very deeply, check everything, all
> lines, all records, all outputs, all processes, absolutely everything, it all must be
> 1000000000% strictly absolutely flawless."* · *"make sure if something is found, its always
> fixed"* · *"make sure the checks that are every 30 minutes do not stop until they ensure
> absolutely everything is 10000% absolutely strictly flawless. I give them full permissions, do
> whatever it takes."* · *"also dont forget to add the speed check component all the time, and its
> maximisation"*
> ⭐ *"make sure you very deeply and extensively study this whole project, have an extremely
> extensive knowledge and absolutely 0 gaps in knowledge."*
> ⭐ *"preserve these loops, but dive much deeper, and check more extensively, and check very deeply
> absolutely everything, from all dimensions and angles possible, it must not miss anything."*

**RE-ARM THE 30-MIN CRON** at `7,37 * * * *` with the STEP contract in §9. ⚠ **Tamer CANCELLED it
mid-pass on 2026-08-06 at 11:5xZ** (*"stop these checks, cancel them"*) — so **ASK HIM before
re-arming**, then run it. The campaign's own `cycle.py` (~1 min) never stops and is green.

### §0.6 HOW TO READ THAT MANDATE — the most important paragraph in this file

**Full permission raises the bar on the THINKING; it does not lower the bar on VERIFICATION.**

RUN 23 published EIGHT wrong conclusions. RUN 24 made TEN of its own and caught every one. **RUN 25
made SEVEN — including three inside a single instrument it had just built — and every one was caught
by a SECOND, INDEPENDENT ROUTE.** §11 lists them all, because the failure shapes repeat.
**Two derivations, or it is not a finding.**

⚠ **AND TAMER'S RATIFICATION DOES NOT OVERRIDE THE HARNESS PERMISSION CLASSIFIER.** `qhold`, `qrls`,
`qdel`, and `Stop-Process` on live campaign processes are **CLASSIFIER-BLOCKED for the agent** —
refused three times under explicit ratification. When it happens: **stop, say so plainly, hand Tamer
the exact command.** Do not route around it.

---

# §1 YOUR FIRST COMMANDS

```bash
cd /c/Users/User/Desktop/dissertation_papers/llm-reward-portfolio
date -u +%Y-%m-%dT%H:%M:%SZ                      # ★ FIRST. Never assume the clock.
python docs/ops/loginnode_guard.py --once
tail -5 docs/ops/watch/CYCLE_LOG.md
.venv/Scripts/python.exe docs/ops/remote_inbox.py --status      # is Tamer waiting on you?
.venv/Scripts/python.exe docs/ops/core_accumulator.py           # ★ cores, 8 invariants, ATTRIBUTION
.venv/Scripts/python.exe docs/ops/job_rank_governor.py          # ★ THE FLOOR + LADDER LOCK + ALLOCATIVE
.venv/Scripts/python.exe docs/ops/line_balance.py --once
.venv/Scripts/python.exe docs/ops/arm_jobs.py
.venv/Scripts/python.exe docs/analysis/record_seed_completeness.py
.venv/Scripts/python.exe docs/analysis/loader_collision_watch.py
ls outputs/campaign_cluster_run4/batches/*.permanent.jsonl | grep -icE "sweep|_test"   # MUST be 0
ssh -o BatchMode=yes myriad "hostname"
```
Then say **"Resuming from: … — next: …"** and CONTINUE. **Never ask "what now".**

### ⚠⚠ THREE EXECUTION RULES RUN 25 PAID FOR — ADOPT THEM ON DAY ONE

1. **CAPTURE TO A FILE AND PRINT THE REAL RC. NEVER PIPE A TOOL INTO `sed`/`grep`/`tail`.**
   RUN 25 ran `core_accumulator.py 2>&1 | sed -n '...'`, got **empty output**, and was one step from
   scoring a whole board instrument as green. Unfiltered it was **RC=127 — the tool had never run**
   (the shell's cwd had reset). Do `tool > /tmp/x.txt 2>&1; echo "RC=$?"; cat /tmp/x.txt`.
2. **USE ABSOLUTE PATHS.** `cd` does not reliably persist between Bash calls here.
3. **NEVER TRUST A FORMATTED TERMINAL VIEW FOR ANYTHING LOAD-BEARING.** Three false signals in one
   session came from renderings, not the system: a single-space `grep 'Full jobname: c1_'` matched
   **zero** of 8 live jobs (the field has MULTIPLE spaces); `ps -ef` rendered a cycle command line as
   `--quiet mpaigna`, a fragment that does not exist; and `ps -ef` showed "two cycle.py processes"
   that `Win32_Process` proved were **one process tree** (the venv `python.exe` is a launcher stub
   that re-execs the base interpreter — its child's PPID is the parent). Read the structured source.

⚠ **`bash docs/ops/run_record_layers.sh` NOW COSTS 1,753 s (29.2 min), NOT the 1,342 s the RUN 25
brief quoted.** Measured 2026-08-06 over 19,166 records = **91.5 ms/record**, projecting to
**~3,853 s (64 min) at the 42,128-record end state.** ⭐ **BUT THE RISK WARNING IS OVERSTATED AND
THAT IS MEASURED TOO:** it runs its seven layers **SEQUENTIALLY**, so the instantaneous load is ONE
archive scanner — the same profile as a single governor run. **Across its whole 29-minute run the
cycle sweep peaked at 286.8 s against the 900 s false-DEAD cap, with most cycles at 28-31 s.**
**Run it hourly. Do not keep deferring it.** ALL 7 LAYERS RAN, ALL RC=0 at 2026-08-06 11:19Z.

⚠ **YOUR OWN SSH/SCAN LOAD MOVES THE SWEEP, and RUN 25 proved it with a two-point control at
IDENTICAL archive size (18,906 records both times): 29.6 s while idle, 638.3 s during instrument
runs.** Before blaming archive growth for an elevated sweep, check whether YOU caused it.

---

# §2 ⛔ MANDATORY READING — ZERO GAPS IN KNOWLEDGE IS A REQUIREMENT

**Tamer's instruction is explicit: study EVERY file, all processes, everything on Myriad, and have
ZERO gaps. Read these END TO END before acting, not in excerpt.**

| file | why |
|---|---|
| **this file** | the brief |
| **`CLAUDE.md`** (2,298 lines) | LAW. the ★ PRIORITIES, the four authorities, Okhrati's D1–D6, Stefan's S1–S11, the 95+ doctrine, the human register, ACCURATE·SURGICAL·ULTRATHINK·SELF-VERIFY |
| **`PREREGISTRATION.md`** (1,061 lines) | THE FROZEN CONTRACT. **R101** (lockstep — read it in full, it is the basis of the LADDER LOCK), **R106**, **R107**, **R108**, **R111**, **R115**, Amendment **E1** |
| **`docs/ops/watch/FLAWLESS_LEDGER.md`** | ⭐ **THE CONTRACT FOR THE LOOP.** The CONTRACT · THE PLACEMENT POLICY · the JSV-throttle rule · the SPEED component · **the pack-width resolution (RUN 25)** · every OPEN row incl. **R25-1 … R25-4** |
| **`CHANGELOG.md` `[2026-08-06c]`** | ⭐⭐ **RUN 25 IN FULL — fourteen numbered sections.** The single densest record of what is now known. |
| **`docs/DEFERRED_FIXES_RUN4.md`** | every known-open defect, **D1–D73**. Read **D25** (the job cap) and **D73** (the ladder has no ordering mechanism — NEW, RUN 25). |
| **`docs/MYRIAD_EXPERT_DOSSIER_2026-07-24.md`** | ⭐ **§0-PRE M5 REFUTES TICKET CONCENTRATION.** M4b the flow equilibrium. M3/M3c the throughput curves. M7 the work model. The diurnal profile. |
| **`docs/CAMPAIGN_DAY_RUNBOOK_2026-07-13.md`** §10 | the lever list and what was already taken |
| **`docs/HANDOFF.md`** §1–§5 | current state + standing orders + the authority map |
| **`docs/ops/MAINTENANCE_2026-08-12.md`** | ⚠ **UCL OFFICIAL — WED 12 AUG, MAY RUN INTO THU 13.** Six days out. |
| **`docs/REMOTE_CONTROL.md`** | ★ Tamer's inbound channel. `remote_inbox.py --status` is a STEP-1 board item. |
| **`memory/session-current-focus.md`** ▶ NOW | the live cursor |
| **the instruments** | read each docstring IN FULL: `job_rank_governor.py` (now ~1,800 lines — the LADDER LOCK, the rung-distance term, the trend, the verdict) · `core_accumulator.py` · `placeable_capacity.py` · `stage_eta.py` · `occupancy_watch.py` · `loader_collision_watch.py` · `record_seed_completeness.py` · `run_record_layers.sh` |

### ⛔ THE READING GATE — answer these FROM THE SOURCES before acting
1. What are the **four authorities**, and what happens when they conflict?
2. What is **H2**, and why is the fed tail **ENDOGENOUS**?
3. **Why is ~18 h of the rung-30 floor irreducible, and what makes the two rounds serial?** (§4)
4. **Which pools accept our jobs, which do not, and how was that established?** (§5)
5. **Why is concentrating cores on one line the WORST allocation for this objective?** (§6)
6. **What is the signature of a SUCCESSFUL bulk `qrls`, and why does it look like a failure?** (§7)
7. **Which paths are drift-fenced, and what is the unfenced-copy pattern?** (§5.4)
8. **Which way does the driver log's clock run?** (host-local **+0100**, NOT UTC.)
9. **What does `analyze_campaign.py` do today, and why must you NEVER "deduplicate the archive"?**
10. **Why does the LADDER LOCK deliberately NOT put all cores on the current rung?** (§0.4, §6)

---

# §3 STATE AT HANDOVER (2026-08-06 12:13 UTC, T+207 h)

```
records 19,152 · spend $45.5019 · drift 0 · freeze MATCHES · sci OK · Eqw 0 · permanent-leak 0
CORES 824 (103 jobs) · eligible 794 · held 0 · jobs 897/1000 · PEAK EVER 2,328
cores 1h 844 · 12h 711 · 24h 933      accumulator: ACCUMULATING (8/8 invariants OK)
records/h 1h=20 · 12h=56 · 24h ~109   ALLOCATIVE EFFICIENCY ~21-22%
pytest 3,044 passed / 3 skipped / 0 failed   ·   seven layers ALL RC=0 (11:19Z)
preflight 15 OK / 2 ATTENTION   ·   line_balance CLEAN   ·   C4 NONE · C5 NONE
exogenous stop 2026-08-27 (20.5 d) · dissertation due 2026-09-01 · UCL maintenance 08-12
HEAD 96d1e7e9 · working tree ~105 modified (watch logs churn; NEVER `git add -A`)
backup branch: origin/backup-2026-08-06 (pushed)
```

### THE LADDER — banked rungs (S15), and WHAT CAPS EACH LINE
| line | rung | what caps it |
|---|---|---|
| gemini-2.5-flash · gpt-5.6-luna · h3 · qwen3.5-9b · sonnet-5 | **568** | COMPLETE |
| haiku-4.5 | **189** | `placebo` has **2 holes** below frontier 567 — **8 trainings bank 568** |
| qwen3.6-27b | **100** | `placebo_shuffled` 222 holes below frontier 408 |
| deepseek · glm-5.2 · nemotron-3-super | **30** | `distributional` holds only 30 contiguous seeds |
| kimi-k3 | **30** | `placebo_shuffled` 234 holes below frontier 427 |
| **core (`c1`, the CONFIRMATORY Opus 5 line)** | **0** | **4 arms hold NO record: bayes_opt, distributional, scalar, tpe** |

⚠⚠ **NAMING.** *"core"* = the **CONFIRMATORY LINE**, batch tag **`c1`**, archive dirs `search/` and
`test/`. **"CPU cores"** = physical cores. **NEVER WRITE "core" UNQUALIFIED.**

---

# §4 ★★★★★★★ THE FLOOR — ~~RUNG 30 ≈ 00:01Z ON 7 AUGUST~~ ⛔ **CORRECTED 2026-08-06: THIS MODEL OMITS A ~32 h QUEUE-WAIT TERM**

> ## ⛔⛔⛔ READ THIS BEFORE THE BLOCK BELOW. THE ARITHMETIC IN §4 IS MISSING A TERM WORTH ~40 HOURS.
>
> `drain + wall` is **not** when a rung lands. It is `drain + `**`QUEUE WAIT`**` + wall`, and the
> queue wait was never measured until RUN 26. Measured 2026-08-06 across **all 99 running jobs**:
>
> ```
> min 28.0h   p25 32.2h   MEDIAN 32.6h   p75 33.3h   max 37.3h
> under 1h:  0 (0%)   ·   over 24h: 99 (100%)   ·   over 30h: 97 (98%)
> ```
>
> **Not one job started in under 28 hours.** `c1`'s own round-1 jobs were submitted **Aug 4 22:19Z**
> and started **Aug 6 04:58Z** — **30.6 h**. So the block below is right about the DRAIN and the
> WALL and silent about the term that dominates both.
>
> ⚠ **AND THE WAY IT SURVIVED IS THE LESSON.** RUN 25 re-derived this date every pass, got 00:01Z
> every time, and read the stability as evidence of correctness — §4's own instruction is to check
> *"whether the INPUTS move"*. They DID move, and the answer held, **because the missing term was
> not an input at all.** ⇒ **RE-DERIVING A NUMBER EVERY PASS DOES NOT VALIDATE THE MODEL IT COMES
> FROM.** Only a term the model does not have can hide there, and stability is exactly what it looks
> like from the outside.
>
> ⇒ **USE `docs/ops/queue_wait.py`** (RUN 26). It prints the live distribution, the per-line
> breakdown (a line's wait IS its ticket rank made visible) and rung 30 under BOTH models — with the
> queue term at zero it reproduces the number below exactly, which is how the omission is
> demonstrated rather than asserted. `docs/ops/stage_eta.py` was separately telling Tamer rung 30
> landed in ~90 minutes; it now GATES any rung whose owing units have not started.
>
> ⇒ **THE ONE INTERVENTION THAT MOVES IT:** concentrating our functional-ticket allocation onto the
> floor (`docs/ops/floor_hold.sh`) is priced at **~31 hours** on this rung, because
> `share_functional_shares=TRUE` splits our pool across contending jobs and all our jobs carry
> near-identical `ntckts` — so the floor's 8 jobs otherwise compete with ~412 of our OWN sweep jobs.

```
c1 round 1 (bayes_opt 30 + tpe 30 = 60 trainings, 8 jobs) ALL RUNNING since 04:58-05:46Z
   -> round 1 completes ~14:54Z   (median SUCCESSFUL task wall 9.12 h, n=340-430 epilogues)
c1 round 2 (h2_pair: distributional + scalar = 60 trainings) NOT YET SUBMITTED
   submits when round 1 drains; another ~9.1 h
=> RUNG 30 COMPLETE ~2026-08-07 00:01Z
```

**RE-DERIVE THIS EVERY PASS FROM LIVE DATA, NEVER CARRY IT FORWARD.** RUN 25 did, and the series is
the proof the instrument is reading rather than stuck: dates `01:00 -> 00:07 -> 00:06 -> 00:04 ->
00:02 -> 00:02 -> 00:01` against median walls `9.17 -> 9.16 -> 9.14 -> 9.13 -> 9.13 -> 9.121`.
⚠ §9's rule *"no movement in three passes is an OPEN FINDING"* exists to catch a FROZEN instrument.
**A stable forecast on stable inputs is correct behaviour. The discriminator is whether the INPUTS
move — establish that BEFORE the rule can fire, as RUN 25 did.**

### ⛔ ~18 h OF THIS IS IRREDUCIBLE SERIAL PHYSICS — DO NOT TRADE RISK FOR IT

`campaign.py:1905-1910` runs `run_test_leg(..., name="h2_pair_test", interleave=True)` **AFTER** the
per-arm loop, and the pair must be **ONE interleaved CRN-paired array** (a crashed arm would be
silently absent and every seed paired against an unregistered comparator set). **The rounds cannot
overlap.** RUN 24 DECLINED narrowing `c1` on exactly this arithmetic (ledger row R24-8).
**Do not re-litigate it.**

### ⛔⛔ THE ONE THING THAT COULD STILL GO WRONG, AND IT LANDS ~00:01Z

**When round 2 drains, `c1` enters C4 and tries to submit 1,347 jobs into ~100 slots of job-cap
headroom.** `c1` has **20 sweep units** (9 arms + the 11-name H1 canon):

| block | seeds | c1 jobs |
|---|---|---:|
| t1 | 30–99 | 175 |
| t2 | 100–188 | 223 |
| t3 | 189–278 | 225 |
| t4 | 279–339 | 153 |
| t5 | 340–402 | 158 |
| t6 | 403–567 | 413 |
| | | **1,347** |

`max_u_jobs = 1000`, live count ~897. All six blocks go in **concurrently** through a
`ThreadPoolExecutor`, so `c1` places ~100 arbitrary-block jobs and then **crash-loops** (D25's
documented, survivable behaviour), and every job it does place carries **ZERO accrued waiting
time** — it queues behind kimi's 08/04 jobs. ⚠ **HOLDING BUYS NO CAP HEADROOM: an `hqw` job is
still in the system.**

⭐ **THE ONE PIECE OF GOOD NEWS, MEASURED:** C4 opens at ~00:01Z, **immediately before the
03:00–08:00Z high window** (the dossier's measured best window, mean 1,524 cores). Its jobs enter
exactly as the cluster empties — the best available timing for the worst-positioned submission.

**THE FIX, COSTED AND NOT ACTIONED (R25-2):** `--pipeline-rungs` **OFF** for `c1`. The sequential
path (`campaign.py:2024-2042`) submits one block, drains it, then the next — rung-ordered by
construction, and it removes the cap breach. It is a supervisor **FLAG**
(`scripts/mode_d_supervisor.ps1:211`), not a code edit — but that file is drift-fenced, so it needs
the §5.4 unfenced-copy pattern AND a live restart of the line carrying the entire reported result.
**TAMER'S DECISION.**

---

# §5 ★★★★★★★ THE CORES MAP — WHAT IS **SETTLED BY MEASUREMENT** AND WHAT IS **OPEN**

> ## ⛔⛔⛔ CORRECTED 2026-08-06 (RUN 26): §5's DIAGNOSIS IS WRONG. IT IS NOT CAPACITY, NOT
> ## FRAGMENTATION AND NOT A PENALTY — **WE LOSE THE PRIORITY RACE, AND WE DILUTE OUR OWN TICKETS.**
>
> Measured in one window: **78 D-pool hosts held ≥8 free slots (2,801 free slots) while we won ZERO
> dispatches in two hours**, and in the same window `ucaqcsu` won **19 jobs × 8 slots**, `zccambr`
> and `uctpec1` six each. **Pack-8 jobs were being placed all around us**, which refutes
> fragmentation, the memory consumable and pack width in a single measurement — §5.2's mechanical
> conclusions are correct but they are answers to a question that is not the binding one.
>
> **THE BINDING CONSTRAINT IS `ntckts`.** Dispatch order is decided ENTIRELY by it: `weight_urgency`
> is **0**, so waiting time earns nothing, and `prior = 4.0×npprior + 1.5×ntckts` (exact to 5 dp on
> a live job). Per-job tickets: `ucjvddm` 442,628 · `ucaphge` 400,379 · `ucaqcsu` 59,547 · **us
> 14,757.**
>
> ⚠ **AND THE FIRST EXPLANATION FOR THAT WAS ALSO WRONG, SO DO NOT REPEAT IT.** RUN 26 initially
> reported a *fair-share ceiling* — that we are throttled for being a heavy user. **Our TOTAL
> allocation is 13.2 M, which is the cluster MEDIAN.** `ucaqcsu` holds LESS total than we do and
> still ranks 4× higher per job, purely because they spread it over 174 jobs and we spread ours over
> 897. `ucbtjji` holds **five times less** and still ranks higher. **We are not penalised; we
> dilute.** `share_functional_shares` is **TRUE**.
>
> ⇒ **THE LEVER IS THEREFORE WORTH MORE THAN §5.3 SAYS, BECAUSE IT PULLS TWICE.** 16 specs per task
> doubles job DURATION *and* halves our job COUNT, so both terms of
> `cores = dispatch_rate × duration × 8` improve together. `ucbtjji` is the existence proof:
> `h_rt=172800` (48 h) against our `54000` (15 h), holding **768 cores from 98 jobs**. The patch is
> staged at `apply_duration_patch.py` — 11 edits each matching exactly once, byte-identical with the
> flag unset. ⛔ NEVER on `c1`.

## 5.1 ⛔ SETTLED. DO NOT RE-DERIVE ANY OF THIS.

**POOLS — probed with a REAL `qsub` into every one of the nine:** `d00a`/`d00b` ours (205 non-PAID
hosts) · **`b00a` SAFE, identical silicon** (Xeon Gold 6240, ucode `0x5003901`, flags sha
`9ede37ab7eb264ea`) · **`t00a` REFUSED** (AMD EPYC 9554P — a different microarchitecture would park
every line at the C3 gate) · **`e f l s u v` unreachable** (`Rejected by policyjsv` on real
submission). **`node-b00a-008` is FENCED** (same model, different flags sha).

**PARALLEL ENVIRONMENTS:** all nine on the d pool are `allocation_rule $pe_slots`. **NO
host-spanning PE exists.**

**OTHER LEVERS, each measured and CLOSED:** memory trim (+8 cores, REJECTED — would OOM 15 h jobs) ·
project/override tickets (both `oticket 0, fshare 0`) · array ticket-concentration (`--chunk-tasks 1`
is correct) · `h_rt` reduction (no slack: p999 and max both 15.01 h) · ticket concentration by
holding (**dossier M5, REFUTED: 44→9 running**) · raising our own priority (impossible) · `qdel` /
`qalter -p` (prohibited and/or classifier-blocked).

## 5.2 ⭐⭐⭐ **THE EFFICIENCY QUESTION IS CLOSED EXHAUSTIVELY (RUN 25). EVERY MECHANICAL LEVER IS MEASURED.**

| lever | verdict, all measured 2026-08-06 |
|---|---|
| **pack width** | **8 is CORRECT.** Live: pack 8 → **1,544–1,600 placeable cores against ~850 held**, so **we are ~650 cores BELOW its ceiling** — pack width is NOT binding. Narrowing raises a ceiling we are not touching while HALVING the structural core cap (`max_u_jobs × 8`) and doubling job count. ⚠ **This also EXPLAINS the RUN 22 ↔ RUN 23 contradiction without arbitrating it:** RUN 23's *"pack 8 places exactly 1 more job"* was a **01:30Z snapshot of a temporarily full cluster**; six hours later the same instrument read 1,544. **Fragmentation is strongly TIME-VARYING — never tune pack width on a single reading.** |
| **threads per training** | **The split is correct and one half is FORBIDDEN to change.** Production data, 740 timed trainings on the SAME 400,000-step training: **1 thread = 8.33 core-h/training; 8 threads = 34.72 core-h/training.** Giving a training 8 cores makes *it* 1.92× faster and the CAMPAIGN **4.17× slower**. 8 threads IS already used where latency binds (the serial GP-EI chain, R107). ⛔ **Threading the TEST leg is FORBIDDEN** — multi-threaded BLAS changes float reduction order, and every scored comparison lives on the uniformly 1-thread test leg. That is ★ PRIORITY 5 and it is not tradeable. |
| **node co-tenancy** | **+1.4 / +3.7 / +7.7 / +9.0 / +11.7 %** at 1–5 of our own tasks sharing a host (n=1,873 sweep tasks, homogeneous work). Real, SMALL, and **no `exclusive` complex exists** (`qconf -sc`), so there is no lever. ⚠ An earlier reading of +24/+51% was **CONFOUNDED** — the `floor` bucket mixed 8-thread search tasks with 1-thread baselines. |
| **failed work** | 4,031 epilogues: `rc=1` **0.002%** of task-hours (194 tasks, all <60 s, all search-stage, **ENDED 2026-08-03**) · `rc=126` 22 tasks (missing `apptainer`, M6) · **wall-kills 1.16%** — the only material waste class. |
| **raw throughput** | **AT THE CEILING.** 12 h reading **97.8 rec/h** against a 95 rec/h ceiling (`cores ÷ 9.34 core-h`). |
| **ALLOCATION** | **~21%. THE ENTIRE REMAINING GAIN IS HERE.** |

> ## ⇒ THE ONE SENTENCE TO CARRY: THE FLEET IS AT 100% MECHANICAL EFFICIENCY AND ~21% ALLOCATIVE EFFICIENCY. THERE IS NO THROUGHPUT LEFT TO RECOVER FROM PACKING, THREADS, WALL-TIME OR NODE CHOICE. **EVERY REMAINING GAIN IS IN WHICH WORK THE CORES DO.**

## 5.3 ⭐ THE ONLY STRUCTURAL PATH BACK TO 2k, AND ITS PRECONDITION IS NOW VERIFIED

**The steady-state identity is `cores = dispatch_rate × job_duration × 8`.** Measured: ~11-13 jobs/h
× 9.12 h × 8 ≈ 800-950 against 824-1,000 held — consistent. **Dispatch rate is fair-share and NOT
ours to move.** ⇒ **JOB DURATION is the only structural lever**, and it is exactly how `ucbtjji`
holds 976 cores at OUR IDENTICAL WIDTH with a 48 h `h_rt` against our 15 h.

**THE SHAPE:** have each task carry **16 specs at pack 8** → `run_one` runs **2 WAVES** (it already
supports this and warns; `run_one.py:307-315`), roughly doubling held cores at the same dispatch
rate, and **halving the job count — which directly relieves the `max_u_jobs` pressure that is about
to break `c1`** (its C4 would be 674 jobs instead of 1,347).

⭐⭐ **THE SAFETY PRECONDITION IS VERIFIED, AND IT IS SAFER THAN THE RUN 25 BRIEF ASSUMED.**
`run_one.py:379` calls `_archive_result` **INSIDE the `as_completed` loop**, so **every training
archives the moment it finishes, independently of its pack-mates.** Combined with `-r y` idempotency
and `_already_archived`, a wall-killed 16-spec task loses at most the trainings still in flight, and
a requeue skips what already landed.

⚠ **STILL A DRIFT-FENCED CHANGE** (`_chunk_packs` in `src/cluster/driver.py:107-111` couples specs
per task to slots; `h_rt` would need ~15 h → ~24 h, and 24 h is UNTESTED for placement). **Cost it,
canary it on a NON-confirmatory leg, get Tamer's GO, and never touch `c1` with it.**

## 5.4 ⭐ THE UNFENCED-COPY PATTERN (how to change a fenced file's behaviour without breaking `drift`)

`docs/ops/watchdog_fenced.ps1` and `docs/ops/mode_d_supervisor_db.ps1` are faithful copies of fenced
scripts with ONE functional difference each, verified by diff. **No fenced edit, no re-base, `drift`
stays 0.** ⚠ **THE FILENAME IS LOAD-BEARING:** the watchdog matches `CommandLine -match
'mode_d_supervisor'`, so a copy must keep that substring or one batch tag gets two supervisors (the
**P12** violation). ⚠ `Stop-Process` on a live campaign process is **CLASSIFIER-BLOCKED**.

---

# §6 ★★★★★★★ THE BIGGEST OPEN ITEM — **THE LADDER HAS NO ORDERING MECHANISM (D73)**

## 6.1 THE DEFECT, PROVEN FROM THE SOURCE AND THREE LIVE MEASUREMENTS

Two mechanisms were meant to make the C4 ladder climb in rung order. **NEITHER OPERATES.**

1. **`campaign.PRIORITY_RUNG_BASE = 0`** — the `-p` ladder was retired 2026-07-31, **CORRECTLY**:
   `-p` is a GLOBAL POSIX priority weighted `4.0` (the largest term in `qconf -ssconf`), so it sank
   us beneath every other user rather than ordering our own work. **NEVER REINSTATE IT.**
2. **Its stated replacement is structurally false.** `campaign.py:2006-2007` says *"blocks are
   submitted in rung order and `weight_waiting_time = 1.0`, so the earlier block outranks the later
   one on age alone."* **Twelve lines below, `campaign.py:2016-2017` submits all six blocks
   CONCURRENTLY through a `ThreadPoolExecutor`.** There is no age difference to order by.

**THE EVIDENCE:** every line's six blocks were submitted inside a **3–5 MINUTE window**, and on
`glm`, `kimi` and `deepseek` a HIGH block carries a **LOWER job id** than t1 (glm t5 = 91245 against
t1 = 91250). `ppri` is **0 on all 931 jobs**. Archive-side, `kimi` holds **six DISCONNECTED seed
blocks** (`0-48, 100-120, 189-212, 279-301, 340-354, 403-417`) and banks rung **30**.

**This is a HALF-APPLIED AMENDMENT — the exact failure mode the same comment block names two
paragraphs earlier for R106 and for the arm-level ladder.**

## 6.2 THE CONSEQUENCE, MEASURED

**Of 848 running cores: 192 (22.6%) were doing work that can raise a rung. 656 were not.** And the
distribution is stark — measured 2026-08-06 11:4xZ:

```
kimi     t3 160 · t1 128 · t2 128 · t6 112 · t5 96 · t4 80     = 704 cores  (owes the LEAST: ~220)
c1       floor 64                                              = 64 cores   (owes 1,520 to rung 100)
qwen3.6  t6 56 · t3 16                                         = 72 cores   (ZERO on t2, the block it needs)
deepseek · glm · nemotron · haiku                              = 0 cores    (owe 350 EACH)
```

⚠ **`kimi` alone produced 95% of all records in a 12-hour window; only 3 of 12 lines contributed at
all.** Because the reported result is a **MINIMUM over lines**, concentrating capacity on one line
is **the WORST available allocation** — it finishes a line that was never the constraint while the
actual constraint sits idle.

## 6.3 ⭐⭐⭐ **THE LADDER LOCK — BUILT, MUTATION-PROVEN, AND WAITING FOR TAMER'S HAND**

`docs/ops/job_rank_governor.py::ladder_lock_plan` implements §0.4 exactly:

* **Every line works its LOWEST incomplete block, and nothing above it.** Never block k+1 while
  block k is incomplete on that line. That is R101's registered lockstep.
* **PRIORITY ORDERING, NEVER BLOCKING.** A line that already holds the common rung is NOT idled — it
  advances its OWN lowest gap, which is precisely what the NEXT common rung needs. **Nothing idles,
  nothing overshoots, no core is surrendered.**
* **THE RELEASE RULE is what makes it a system rather than a one-off hold.** Every pass recomputes
  each line's needed block and RELEASES any held job that has become needed. **Mutation-proven:
  strip it and the selftest goes red, because without it a hold is a starvation device.**
* **THE LIVE QUEUE IS THE AUTHORITY.** A job counts as held ONLY if `qstat` says `hqw`. ⚠ RUN 25
  shipped the opposite and caught it within the hour: the journal recorded INTENT, so after a pass
  whose commands nobody ran it believed 378 jobs were held, reported `TO HOLD: 0`, and **silently
  stopped proposing the plan**.
* **THE DEPTH GUARD BINDS LAST**, at `max(4 × running, 200)`, because **M5 measured a fleet decaying
  44 → 9 running** when the eligible queue was thinned to 80.
* It **NEVER** touches `-p`, **NEVER** touches a running job, **NEVER** holds a distance-0 job, and
  **NEVER** holds a `c1` floor job. All four are asserted and mutation-controlled.

**LIVE PLAN AT HANDOVER:** `jobs above their line's needed block: 669 · TO HOLD 378 · TO RELEASE 0 ·
eligible after 416 (guard 416)`. Composition verified against a fresh snapshot: **t4/t5/t6 on
kimi/deepseek/glm/nemotron only — no rung-lifting job, no `c1` job, no haiku repair.**

⇒ **`qhold`/`qrls` are CLASSIFIER-BLOCKED for the agent. `job_rank_governor.py` prints the exact
commands and journals the ids to `docs/ops/watch/LADDER_LOCK.json`. THE PLAN GOES TO TAMER.**

## 6.4 ⚠ THE POLICY QUESTION, NOW ANSWERED — DO NOT RE-OPEN IT

RUN 24 left `PROMOTE_MAX_TIER` open. **Neither setting is right.** `0` starves haiku's repair; `1`
produces a plan that holds **all 157 of `glm`'s pending jobs — and `glm` is a STARVED BINDING line
owing 350**. ⇒ **The LADDER LOCK is the answer**: it leaves haiku's repair fully eligible at distance
0 while never holding a binding line's rung-lifting work.

⚠ **AND A VALUE CORRECTION WORTH CARRYING:** haiku's 8-training repair is the best *value per
training* in the campaign (+379 rungs on 5 arms) but it is **NOT on the critical path** — the
reported result is a MINIMUM, haiku already banks 189, and the minimum is pinned at 0 by `c1`. Its
value is per-line depth for the **R86 pooled bound** and the **capability gradient**, which is
genuine but secondary. Measured queue position at 11:0xZ: **rank 560 of 812, ~44 h out**; after the
hold, **rank 285, ~22 h**.

---

# §6.5 ⛔⛔⛔ **THE CORE COLLAPSE 1,642 → 824, AND THE PENALTY QUESTION. TAMER RAISED THIS AND HE IS RIGHT. THIS IS YOUR #1 INVESTIGATION.**

**TAMER, 2026-08-06:** *"I have received an email from RC support in response to my email to raise
my priority. They said they have received a lot of MSc students asking to raise their priorities,
and all of them were denied to ensure everything is fair. I need you to deeply check if our priority
was not downgraded, or any other slowing actions were not applied to us. Today's results are
ridiculous — at that time yesterday we were at like 1,700 cores, and now we are at 800. Ultrathink
about that, and raise the cores back."*

## ⓵ THE PENALTY QUESTION IS **ANSWERED: NO PENALTY.** Four independent checks, all clean.

| check | command | result |
|---|---|---|
| POSIX priority | `qstat -u ucestes -pri` (`ppri` column) | **0** on every job — **NOT downgraded** (a penalty would be negative) |
| resource quota | `qconf -srqsl` then `qconf -srqs slowemdown` | the cluster's ONLY RQS is **`slowemdown`**, and it reads **`enabled FALSE`** with **`limit users ucapsy0 to slots=50`** — a DIFFERENT user, and switched OFF |
| our user object | `qconf -suser ucestes` | **`oticket 0, fshare 1`** |
| the same for every other heavy user | `qconf -suser <u>` for the 8 largest | `ucecgwh · ucecwly · ucaphge · uccaewo · ucechh1 · uceccho · ucbtjji` — **ALL `oticket 0, fshare 1`. IDENTICAL to ours.** |

⭐ **AND THE STRONGEST EVIDENCE OF ALL: WE ARE THE #1 USER ON THE CLUSTER.** Live running slots:
**`ucestes 824` · ucbtjji 808 · uccaewo 632 · zccambr 516 · ucaqcsu 288.** A throttled account does
not lead the cluster. **Report this to Tamer plainly — RC did not penalise us.**

⚠ **RE-RUN ALL FOUR CHECKS ON YOUR FIRST PASS ANYWAY.** They are cheap, they are the exact thing
Tamer asked to be watched, and a state that is clean today can change. **`ppri` must stay 0 and no
enabled RQS may ever name `ucestes`.**

## ⓶ THE COLLAPSE IS REAL, IT IS **NOT** THE DIURNAL PATTERN, AND ITS MECHANISM IS **UNRESOLVED**

Reconstructed from the recorded `CYCLE_LOG` (hour, mean cores):

```
08-05 06Z 1,642 | 08-05 12Z 1,384 | 08-05 16Z 1,381 | 08-05 18Z 1,075 | 08-05 23Z   699
08-06 00Z   533 | 08-06 03Z   546 | 08-06 06Z   773 | 08-06 08Z   976 | 08-06 12Z   824
```

**Yesterday at 12Z: 1,384. Today at 12Z: 824. A 40% fall at the same hour.**

⚠⚠ **AND IT INVERTS THE EXPECTED SHAPE, WHICH IS WHY "it is just diurnal" DOES NOT CLOSE IT.** The
dossier's measured best window is **03:00–08:00Z** (mean 1,524 cores). On 08-05 that window
delivered **1,496–1,642**. On 08-06 the SAME window delivered **546–976 — our LOWEST readings of the
whole period.** The pattern did not merely shift; it reversed.

**WHAT IS ESTABLISHED:**
* `cores = dispatch_rate × job_duration × 8`. Job duration is stable (median SUCCESSFUL task wall
  9.12–9.17 h across the whole session). **So the fall is a DISPATCH-RATE fall**, measured across
  passes: **12.6 → 11.2 → 10.9 → 10.3 → 9.1 → 7.9 jobs/h.** At 9.12 h that predicts
  `7.9 × 9.12 × 8 = 576` cores against 824 held — the right order of magnitude and the right
  direction.
* **It is NOT our queue running dry:** eligible depth is **794 jobs = 6,352 slots of demand**, `Eqw`
  0, unschedulable 0, `jobs 897/1000`.
* **It is NOT the placement ceiling:** `placeable_capacity` reads **1,544 cores at pack 8 against
  824 held — ~720 placeable cores we are not taking.**
* Cluster-wide at the same moment: **4,109 running slots in 2,556 jobs, 3,808 jobs pending.**

**WHAT IS NOT ESTABLISHED, AND IS YOUR JOB:** *why the dispatch rate halved.* The honest position is
that a penalty is RULED OUT and the mechanism is OPEN. Candidate hypotheses, each with the
measurement that would settle it — **and take two independent routes before banking any of them:**

1. **FAIR-SHARE USAGE FEEDBACK.** RUN 24 recorded `weight_tickets_share 1e4` against
   `weight_tickets_functional 5e8`, i.e. functional (usage-history-FREE) tickets dominate by ~5e4×,
   which is why *"holding more does NOT shrink our future share"* was banked. ⚠ **VERIFY THAT
   FIRST-HAND rather than inheriting it** — read `qconf -ssconf` yourself, and read our actual
   ticket counts with `qstat -u ucestes -ext` (⚠ RUN 25's attempt used the wrong column index and
   returned zeros for everyone — **find the right column and prove it with a known-good row**).
2. **A COMPETITOR AROSE.** `uctpec1` alone had **769 pending jobs** at 12Z. Compare the top users'
   RUNNING slots against yesterday. If someone large arrived, our share fell for a legitimate reason
   and the answer is capacity, not fairness.
3. **THE POOL EMPTIED OF *PLACEABLE* SHAPE.** Fragmentation is strongly time-varying (§5.2). Track
   `placeable_capacity --pack 8` on a **diurnal series**, not a single reading.
4. **OUR OWN SUBMISSION SHAPE CHANGED.** Which lines were submitting on 08-05 versus now? Today
   `deepseek`, `glm`, `nemotron` and `haiku` hold **ZERO running jobs** while holding queued work.
   Reconstruct from `CYCLE_LOG` and the driver logs.

⭐ **AND NOTE THE ONE STRUCTURAL LEVER THAT DIRECTLY ATTACKS A DISPATCH-RATE FALL:** since
`cores = rate × duration × 8` and **the rate is not ours to move**, **DURATION is** — §5.3, whose
safety precondition RUN 25 verified. **If the rate has genuinely halved, the duration lever is worth
twice what it was.**

---

# §7 ⚠⚠ A BULK `qrls` IS THROTTLED BY THE SITE JSV — AND SUCCESS LOOKS EXACTLY LIKE FAILURE

Releasing 395 jobs at once does **not** return them to `qw` directly. The site's `policyjsv`
(`jsv_allowed_mod` includes **`h`**) applies a **SYSTEM hold** and drains it at **~400 jobs/h**.
Measured: `hs` 395 → 368 → 348 → 293 → 230 → 168 → 105 → 41 → **0**, while `qw` rose 476 → 857.

> ### THE SIGNATURE OF A **SUCCESSFUL** RELEASE MID-THROTTLE:
> `qrls` prints **NOTHING** · `qstat -s hu` = **0** · `qstat -s hs` **> 0** · state still reads `hqw`
> · and `qrls -h s` answers **`denied: "ucestes" must be manager to remove manager hold`**.
> **ALL OF THAT IS NORMAL. WAIT AND RE-MEASURE. DO NOT ESCALATE AND DO NOT RE-ISSUE.**

**THROTTLE vs SANCTION — one command each:** targeted at us? `qstat -u '*' -s hs | awk 'NR>2{print
$4}' | sort -u` → **40 users** · throttled? `qstat -u ucestes -s r | wc -l` → still dispatching ·
draining? the same `-s hs` count over ~7 min → **monotone FALLING**, `qw` rising.

⇒ **ONE HOLD PER ROUND.** Check `qstat -u ucestes -s hs` FIRST; if non-zero, WAIT.

⚠ **THE 90-MINUTE HOLD RULE IS UNDER REVIEW AND IT IS TAMER'S CALL (R25-3).** It was written for
TACTICAL promotion holds. **A LADDER LOCK hold is STRATEGIC — it must persist until the blocks below
drain, which is days.** The proposed substitute, which is strictly stronger than a timer and follows
the ledger's own *"the release predicate IS the lever"* lesson: **release from the journal if cores
fall, if eligible depth drops below the measured 291-job burst floor, or if the 12 h record rate
falls.** **Do not silently break the 90-minute rule; get it amended.**

---

# §8 EVERYTHING RUN 25 DID

| # | what |
|---|---|
| **1** | ⭐⭐⭐ **FOUND AND REGISTERED D73** — the C4 ladder has NO ordering mechanism. Proven from the source (`PRIORITY_RUNG_BASE = 0` + a `ThreadPoolExecutor` defeating the age-ordering its own comment relies on) and three independent live measurements. |
| **2** | ⭐⭐⭐ **BUILT THE RUNG-DISTANCE TERM** in `job_rank_governor.py` — the dimension the V0–V3 value model was structurally blind to (`job_tier` gives a `_sweep_t1` and a `_sweep_t6` job on one line the IDENTICAL score). `job_sweep_tier` · `line_needed_block` · `rung_distance` · `allocative_efficiency` · `tier_value_hold_plan`. Mutation-proven three ways. |
| **3** | ⭐⭐⭐ **BUILT THE LADDER LOCK** (§6.3) — Tamer's rung-by-rung policy as a standing, self-releasing system, with the release rule, the live-queue authority check, and the depth guard all individually mutation-proven. |
| **4** | ⭐⭐ **CLOSED THE EFFICIENCY QUESTION EXHAUSTIVELY** (§5.2). Pack width, threads, co-tenancy, failure classes and raw throughput all measured and closed. **The fleet is at 100% mechanical and ~21% allocative efficiency.** |
| **5** | ⭐⭐ **PUBLISHED `cores doing RUNG-RAISING work` ON TAMER'S STATUS PAGE**, with its AGE and a `STALE` marker past 90 min (read from state, never recomputed on the page — `line_needed_block` walks the whole archive). |
| **6** | ⭐⭐ **FIXED `stage_eta.py`** — it led with a 1 h throughput window on a 9 h arrival quantum and **misled Tamer for the SECOND time**, having already recorded the identical 2026-08-03 incident in its own comment. It now leads with `=> OPERATIVE RATE` and labels every sub-quantum row `NOISE`. ASCII-verified by AST walk over every appended page string. |
| **7** | ⭐ **MEASURED THE TRUE SEVEN-LAYER COST: 1,753 s, not the 1,342 s the brief quoted** (91.5 ms/record; ~3,853 s at the end state) — **and proved the risk warning is overstated**: the layers run SEQUENTIALLY, and across the whole 29-minute run the cycle sweep peaked at 286.8 s against a 900 s cap. |
| **8** | ⭐ **BUILT THE LANDING FORECAST** (job start times + measured wall → when records arrive) and **validated it twice**: it called the 09:00Z burst and the quiet 10:00Z hour (predicted 32/h, measured 28/h). |
| **9** | **RESOLVED THE RUN 22 ↔ RUN 23 PACK-WIDTH CONTRADICTION** without arbitrating it — they sampled different hours of the same day, and fragmentation is strongly time-varying. |
| **10** | **REGISTERED R25-4** — `core_accumulator` A3 is a coin-flip alarm: it samples ONE job and **25 of 60 (42%) fail its test on a busy cluster**, driving the headline verdict to `AVOIDABLE LOSS` on pure capacity transients. **Diagnosed, NOT fixed.** |
| **11** | **VERIFIED THE WHOLE REPO GREEN**: pytest **3,044 passed / 3 skipped / 0 failed** (RC read from the log plus an independent character census), preflight 15 OK / 2 ATTENTION, freeze MATCHES, drift 0, seven layers ALL RC=0. |
| **12** | **RECORDED EVERYTHING**: `CHANGELOG [2026-08-06c]` (fourteen sections), ledger rows R25-1…R25-4 + the pack-width resolution, **D73**, the cursor, and 8 commits pushed to `origin/backup-2026-08-06`. |

---

# §9 ★★★★★ THE 30-MINUTE LOOP — RE-ARM IT (ASK FIRST), PRESERVE IT, AND GO **MUCH** DEEPER

⚠ **Tamer cancelled the cron mid-pass on 2026-08-06. ASK HIM before re-arming at `7,37 * * * *`.**

**STEP 0 CLOCK** (`date -u`; qstat and driver-log stamps are host-local **+0100**) ·
**1 BOARD** (§1 — capture to a file, print the REAL RC, read UNFILTERED) ·
**2 THE FLOOR** (§4 — anything on `c1`'s four arms? round 1 still 8/8? is `h2_pair` out? **re-derive
the rung-30 date from live starts + a freshly measured wall**) ·
**3 THE FLEET BALANCE + THE LADDER LOCK** (§6 — is any BINDING line starved? what does the lock
propose? what must be RELEASED?) ·
**4 EVERY RECORD** (`record_seed_completeness` + `loader_collision_watch` every pass; the seven
layers **HOURLY** — §1 shows the cost is safe) ·
**5 SPEED** (the five questions below) ·
**6 FIX** (write the falsifying test FIRST, prove it fails against the pre-fix behaviour, then fix) ·
**7 RECORD** (CHANGELOG + ledger + cursor — continuously).

**Every finding is FIXED, PROVEN-BENIGN (with the measurement), or ESCALATED (with everything around
it fixed).** No row may age three passes. **NEVER weaken a check to make it pass.**

### THE SPEED QUESTIONS, EVERY PASS
1. **Are we holding every core we could?** Measure with `placeable_capacity.py` ONLY — never an
   ad-hoc `qhost` sum. Compare placeable against held and NAME THE GAP.
2. **Is any BINDING line starved?** The makespan is set by the LARGEST deficit, so cores must be
   deficit-proportional.
3. **Are the cores doing USEFUL work?** Mechanical AND allocative. `job_rank_governor` reports both,
   plus the trend and its direction-aware verdict.
4. **Did we GROW when there was room?** The capture verdict. ⚠ **A falling core count is NOT
   automatically a loss** — check the JOINT SIGNAL first (`a core fall only counts if the RECORD RATE
   fell too`). Cores 968 → 904 while `records/h` rose 75 → 145 is **throughput ARRIVING**.
5. **Has the rung-30 date moved, and are its INPUTS moving?** (§4)

---

# §10 WHERE RUN 26 MUST GO DEEPER — RANKED BY VALUE

0. ⛔⛔⛔ **THE CORE COLLAPSE 1,642 -> 824 (§6.5). TAMER RAISED IT AND IT IS YOUR #1 ITEM.** The
   PENALTY question is ANSWERED (no downgrade, no quota, `fshare` identical to every heavy user, and
   we are the #1 user on the cluster) -- **re-verify all four checks on pass 1 anyway**. The
   MECHANISM of the dispatch-rate halving (12.6 -> 7.9 jobs/h) is **OPEN**, and the collapse INVERTS
   the measured diurnal shape, so it is not explained away. Four hypotheses and their tests are in
   §6.5.
1. ⭐⭐⭐ **GET THE LADDER LOCK APPLIED (§6.3).** It is built, tested and generated. It needs Tamer's
   `qhold`, and then **RE-RUN THE GOVERNOR EVERY FEW HOURS** so the release rule fires as blocks
   complete. **That cadence IS the system.**
2. ⭐⭐⭐ **THE `c1` C4 MOMENT AT ~00:01Z (§4).** Be awake for it. Decide `--pipeline-rungs` with
   Tamer BEFORE it happens.
3. ⭐⭐ **FIX R25-4** (`core_accumulator` A3). Discriminate CAPACITY from CONFIGURATION and stop
   sampling one job. ⛔ **Do NOT widen the phrase match (launders the alarm) and do NOT delete the
   check (loses the RUN 17 case).** Falsify against the live shape: 25 of 60 must read TRANSIENT and
   a synthetic unknown-PE job must still FAIL.
4. ⭐⭐ **THE JOB-DURATION LEVER (§5.3).** The ONLY structural path to 2k, and its safety
   precondition is now VERIFIED. Cost it, canary it on a non-confirmatory leg, get Tamer's GO.
5. ⭐⭐ **`scripts/analyze_campaign.py` D49–D51 — THE BIGGEST STANDING ANALYSIS ITEM.** The loader
   admits every `test_leg_*` line under the SAME arm labels; `_seed_scores` groups on `(arm, seed)`
   with no line term; **2,840 (arm, seed) cells are held by more than one line.** It FAILS LOUD
   (`ValueError`; `analyze()` guards only `AssertionError`). ⚠⚠ **THE TRAP: the guard's own message
   says "Deduplicate the run archive" — DOING THAT CONVERTS A LOUD FAILURE INTO A SILENT ONE. NEVER
   DEDUPLICATE THE ARCHIVE.** Repair prototyped at `docs/analysis/a79_fix_proof.py:60-84`;
   `scripts/**` is fenced, so it lands at teardown BEFORE `bank_gate`. **CRN-1 goes in the same edit**
   (`_paired:1553-1557` pairs on the seed number ALONE).
6. ⭐ **`node-d00b-020`** — 7 `rc=126` failures, latest 2026-08-05 12:29Z, and it is **NOT** in
   `--exclude-hosts` (we exclude `node-d00a-230` and `node-d00b-024`). Fold it in at the next
   restart; not worth a restart on its own.
7. **The 39 registered keys of `analyze_campaign.py`** — read `REGISTERED_OUTPUT_KEYS` from the
   artefact, NEVER a remembered count. (It is 39; CLAUDE.md said 35 until 2026-08-04 and 35 was never
   right.) D49–D72 cover part of the register; the rest have never been read.
8. **Unexhausted monitors** — `transport_health.py`, `retriage_alarms.py`, `reject_taxonomy.py`,
   `publish_status.sh` (⚠ a RUNNING bash script), `compute_ledger.py`.
9. **The SWEEP-1 cost trajectory.** The preflight already flags that the three full-archive layers
   scale with the archive; the seven-layer cost measured this session (91.5 ms/record) is the same
   trajectory. Extend `record_shrink_cache` — the blocker is the WALK (nine archive walkers, at least
   five different exclusion rules).
10. **The remaining deep study**: `src/inference/**`, `src/llm/**`, `src/env`, `src/agents`,
    `scripts/**`, `tests/**` (564 files), `docs/**` (633 files), and `paper/**` READ-ONLY.

---

# §11 ★★★ THE LESSONS RUN 25 PAID FOR — SEVEN OF ITS OWN MISTAKES

**Every one was caught by a SECOND, INDEPENDENT route. That is the only reliable mechanism. Three
were inside an instrument RUN 25 had just built, which is the shape to watch for.**

1. **A FILTERED EMPTY OUTPUT IS INDISTINGUISHABLE FROM A CLEAN BOARD.** `core_accumulator.py | sed`
   printed nothing; unfiltered it was **RC=127 — the tool had never run.** A whole board instrument
   was one step from being scored green.
2. **I PUBLISHED "THE ALLOCATION IS DETERIORATING" FROM AN ELEVEN-MINUTE WINDOW.** Marginal 15.4%
   against a 20.2% average. Two further readings took it to **20.0% against 20.7% — stable.** The
   claim did not survive its own next measurement. **The level was right; the DERIVATIVE was wrong.**
3. **MY TREND INSTRUMENT THEN READ ONE JOB AS A 100% MARGINAL** — two readings, a +8-core delta.
   Fixed with a minimum core delta AND a minimum time span, because the fleet re-shapes over one job
   duration (9.12 h), so a trend read over minutes samples arrival jitter.
4. **THE SAME INSTRUMENT KNEW ONLY ONE DIRECTION.** On a SHRINKING fleet a marginal ABOVE the average
   is the bad case, and it printed no warning at all. Mutation proved it was wrong in BOTH directions.
5. **THEN THE FIXED VERSION CONTRADICTED `core_accumulator`** — it cried SHEDDING-USEFUL while the
   accumulator read HEALTHY on the same fleet at the same moment. **The accumulator was right**: it
   carries the doctrine the trend lacked (*a core fall only counts if the record rate fell too*).
   **Two instruments in one repository disagreeing is the P307 class.**
6. **MY LADDER-LOCK JOURNAL RECORDED INTENT, NOT REALITY.** After a pass whose commands nobody ran it
   believed 378 jobs were held, reported `TO HOLD: 0`, and silently stopped proposing the plan.
   **The live queue is the authority; a journal is a recovery aid.** This repo had already paid for
   that lesson once.
7. **AND MY FIRST TEST FOR THAT FIX WAS TOO WEAK TO KILL THE MUTANT** — it asserted `hold > 0`, which
   other fixture jobs satisfied regardless. Narrowed to name the specific jobs. **A test that cannot
   fail verifies nothing.**

**AND THE INHERITED LESSONS THAT KEEP PAYING:**
* **SEARCH THE RECORD BEFORE BELIEVING YOUR OWN SCRIPT.** A real `qsub` is the oracle; `qhost` is
  not, and `qalter -w p` disagrees with reality in BOTH directions.
* **A CONTROLLED COMPARISON BEATS YOUR OWN ARITHMETIC.** Look for the user who breaks your model.
* **OVERSTATING A RISK IS AS INACCURATE AS UNDERSTATING ONE.**
* **CHECK THE FENCE OF THE FILE YOU ARE EDITING, not the file you were reading.** `.ps1` is
  ASCII-only; the status page has an ASCII gate broken five times.
* **AN AST PARSE / LINTER IS A SELF-CORRECTION SIGNAL.** It caught prose placed outside a docstring.

---

# §12 HARNESS LIMITS — MEASURED, NOT ASSUMED

```
qstat / qconf / qacct / qhost (all read-only)   WORK -- the whole scheduler surface
qsub  (small probes)                           WORKS  <- 1 core, 2 min, write only under ~/probe
qhold / qrls                                   ⛔ CLASSIFIER-BLOCKED for the agent (3x, ratified)
Stop-Process on a live campaign process        ⛔ CLASSIFIER-BLOCKED (3x, ratified)
Stop-Process on a THROWAWAY process            WORKS  <- capability proven on a dummy
qdel                                           PROHIBITED on campaign jobs + classifier-blocked
qalter -p <negative>                           PROHIBITED (one-way for a non-operator)
git commit --only <path>                       WORKS, and is REQUIRED (the tree is always dirty)
git pull --rebase                              FAILS ALWAYS on this tree -- use git fetch + git show
heredocs carrying backslashes/braces           BREAK -- use the Write/Edit tools instead
PowerShell 5.1: single object .Count           is $null, NOT 1 -- always @(...).Count
Windows python cannot read git-bash /tmp       use the scratchpad dir for cross-tool files
```
**⇒ TEST THE SPECIFIC COMMAND.** RUN 24 disproved *"`Stop-Process` is blocked"* on a throwaway — and
then found the LIVE case genuinely is blocked.

---

# §13 STANDING RULES THAT MUST SURVIVE THIS HANDOVER

- **NEVER** read a treatment arm's SEALED-TEST outcome for INFERENCE. Know what R101 (4) permits.
- **NEVER** change a frozen threshold. **NEVER** make a check pass by weakening it.
- **NEVER** raise the 900 s sweep cap or the 180 s probe timeouts.
- **NEVER** add Claude/Anthropic attribution anywhere. **Tamer is sole author.**
- **NEVER** `git clean -x`, `git add -A`/`-u`, or `git stash`. **Stage BY NAME**, leave nothing staged.
- **NEVER** `qdel` a campaign job. **NEVER** `qalter -p`. **NEVER** deduplicate or junction the archive.
- **NEVER** reinstate the `-p` rung ladder (§6.1).
- **NEVER** edit `src|scripts|config|prompts` while live (drift-fenced). `docs/**` and `tests/**` are
  safe — and §5.4 shows the unfenced-copy pattern.
- **⚠ `.ps1` FILES ARE ASCII-ONLY** and must pass `Parser::ParseFile`. **The status page is ASCII-only
  too** — verify by AST walk over every appended string, not by eye.
- **⚠ Editing a running loop is INERT** — `cycle_loop.sh`/`publish_loop.sh` need a RESTART;
  `cycle.py`/`publish_status.sh` are re-invoked each iteration and do not.
- **PowerShell console is cp1251.** `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`, and
  `subprocess.run(..., encoding="utf-8", errors="replace")` — `text=True` uses the system codepage.
- **A hold must not outlive its purpose.** Two independent release mechanisms, ids journalled, and
  see the §7 amendment question.

---

# §14 THE ONE PARAGRAPH TO CARRY

**The efficiency question is closed: the fleet runs at 100% mechanical efficiency and ~21%
allocative efficiency, so there is nothing left in packing, threads, wall-time or node choice, and
EVERY remaining gain is in WHICH work the cores do. The cause is D73 — the C4 ladder lost its
ordering when the `-p` ladder was correctly retired and the replacement it named (submission age) is
structurally defeated by a ThreadPoolExecutor that submits all six blocks at once. The consequence
is that kimi holds ~85% of the fleet while owing the least, produced 95% of one 12-hour window's
records, and four lines that actually gate the common rung hold ZERO cores. The LADDER LOCK is built
and mutation-proven and implements Tamer's rule exactly — every line works its lowest incomplete
block, a line that already holds the rung advances its own lowest gap rather than idling, held work
is released the moment it becomes needed, and the depth guard means no core is ever surrendered. It
needs one `qhold`, which is classifier-blocked, so it needs Tamer's hand. Meanwhile rung 30 lands
~00:01Z on 7 August, and roughly an hour later `c1` will try to push 1,347 jobs into ~100 slots of
cap headroom in random block order — be there for it. Measure twice, use the audited instrument,
capture to a file and read the real return code, and search the record before believing your own
script: RUN 23 published eight wrong answers, RUN 24 made ten, and RUN 25 made seven of which three
were inside an instrument it had just written, every one caught only because it always found a
second route.**
