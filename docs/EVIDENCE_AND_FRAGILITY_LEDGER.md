# Evidence-and-Fragility Ledger — the non-fragile backbone (2026-07-25)

**Standard (Tamer, 2026-07-25; `CLAUDE.md` → EVIDENCE-BACKED, NON-FRAGILE BACKBONE):** every load-bearing
decision carries a stated RATIONALE + GRADE-A evidence; the backbone must have no weak link a skeptical
expert can snap; **the grade is capped by the weakest link.** This ledger maps every load-bearing decision
→ rationale · evidence+grade · fragility · status/fix, and is maintained continuously. A decision is not
"done" until its row is **grade-A + low-fragility**, OR its residual fragility is a disclosed Limitation.

- **Evidence grade:** **A** = first-hand-verified data/measurement in-repo · **B** = first-hand-read
  literature · **C** = asserted / no derivation (a fragility to close).
- **Fragility:** low · med · **HIGH** (can a skeptical Okhrati/Stefan read snap this link?).
- **Status:** ✅ strong · 🔧 fix-in-flight (with the amendment) · ⏳ pending the 2026-07-25 backbone audit.

> This is the SEED (spine decisions I can grade first-hand from the 5-criterion audit + the design
> record + the live verifications). The two-auditor exhaustive backbone map (launched 2026-07-25) will
> COMPLETE it (every remaining decision) and CALIBRATE these grades. Rows here are conservative/honest.

## Research frame
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| The novelty cell (LLM authors reward CODE + multi-level tail feedback as the manipulated variable + pre-registered controlled comparison) | a conjunctive gap unoccupied by any neighbour | 196-paper first-hand sweep; nearest-neighbours distinguished (CH2) — **A** | MED (gallego2026 coined "feedback engineering" concurrently) | 🔧 lead with the deltas; cite gallego as concurrent axis-namer |
| Significance (why the empty cell MATTERS, not just that it's empty) | mechanism generalizes beyond finance | mechanism reframe + EDA — **B** | MED (softer than the novelty case) | 🔧 one-clause "why beyond finance" in CH1 |
| Mechanism-as-headline reframe | Okhrati rewards depth/mechanism; the null is a boundary condition | design choice, pre-freeze | LOW | ✅ |

## Model suite / capability gradient
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| **The capability gradient is REAL, not an artifact of a hostile reward contract** | The weak leg's low yield could be attacked as "your contract traps small models, so your gradient measures a Python idiom, not reward-design ability" — a construct-validity objection that would snap a headline contribution | **ADVERSARIAL A/B RUN 2026-07-26 (live, n=20 per condition).** qwen3.5-9b EXECUTABLE yield (archived responses re-run through the real AST gate + 12 contract steps, faithful to `PortfolioEnv.step`): **original contract 5/20 (25%)** vs **clarified contract 6/20 (30%)** — no material change, CIs overlap heavily. Decisively, the FAILURE MODES CHANGED: the `reward_state is None` idiom trap disappeared and was replaced by INDEPENDENT competence failures (`UnboundLocalError` on a never-assigned variable, wrong return shape, an AST-gate rejection). Clarifying the contract fixes the one trap and the model simply fails elsewhere — **A** | LOW (the objection was tested and FAILED empirically) | ✅ **The original contract STANDS.** The clarification was reverted: it bought no measured benefit on the leg it targeted and the strong leg it was checked against did not improve either (haiku 20/20 -> 9/10, itself noise). Report the A/B as a robustness check in CH6/CH7 — it converts a reviewer attack into a measured, reported quantity |
| Authoring reliability must be reported as EXECUTABLE yield, not format compliance | the registered gate scores "`extract_reward_source` succeeded and the source contains `def reward(`" — which a model can pass while 75% of its code raises on step 1 | MEASURED 2026-07-26: qwen3.5-9b scores **1.00** on the registered compliance gate while **only 5/20** of its rewards execute; `validate_once` independently rejects **15/20** pre-training (two methods agree exactly) — **A** | MED if the write-up reports the gate number as "reliability" | 🔧 the reliability table must carry the executable column, else it overstates the weak leg ~4x |

## Identification
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| Only the reward varies across arms | isolates the feedback-content effect | `src/arms/factory.py` (feedback_kind is the only per-arm variation); test-guarded — **A** | LOW | ✅ (runtime guard is test-only — a future refactor risk, noted) |
| Fixed SB3 SAC agent (TQC secondary) | static-CVaR is time-inconsistent/non-Markovian → the reward channel is the forced injection point | structural argument (`02_CHAPTER_theory.md`) — **A/B** | LOW | ✅ |
| Fed tail is ENDOGENOUS (disclosed, not "agent-independent") | the estimator fits the trained policy's own returns | `src/feedback/measurement.py` docstring — **A** | LOW | ✅ (foregrounded, not hidden) |
| Thinking common-mode within a model → headline thinking-invariant | same model+config authors both arms | empirical per-model verification 2026-07-25 — **A** | LOW | ✅ |

## Arms & baselines
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| 7 arms, each control bound to a named threat | one-to-one threat→control mapping (Table 4.1) | `config/arms.yaml` + CH4 — **A** | LOW | ✅ |
| H1 (LLM vs hand-designed) currently report-only / snooped-descriptive | selection on the sealed leg without archived val fitness | `PREREGISTRATION.md` — **A** (that it's weak) | HIGH→resolving | ✅ **R105 (2026-07-26)**: the graphical validity tier is registered; H1's DE-SNOOP (val-select best-of-4 + score on the sealed test) is candidate node N6 (ratification-pending) |
| H4 (LLM vs random/bayes search) secondary | multiplicity/power at design time | `config/preregistration.yaml` — **A** | HIGH→resolved | ✅ **R105 (2026-07-26)**: promoted to node N4 in the graphical validity tier (`bretz2009graphical`), ratification-pending |

## Feedback vector
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| CVaR sub-vector (cvar_01/05/10/25) | Kusuoka/Acerbi spanning of coherent risk | theory + literature — **B** | LOW | ✅ |
| left_tail_mass (k=2.0σ) | tail-mass summary | **C** (k=2.0 asserted, "frozen", no derivation) | MED | 🔧 justify k or reclassify as engineering stat |
| robust_skew / Bowley | tail-asymmetry summary | **C** (anchors + "not covered by the elicitability theorems") | MED | 🔧 justify anchors or reclassify; fix the mislabel ("left-tail skew" → Bowley) + sign convention |
| α-grid {1,5,10,25}% | a discretisation of the CVaR-level axis | **C** (acknowledged-arbitrary; no sensitivity run) | MED | 🔧 cheap grid-robustness exhibit |

## Parameters
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| B* = 400k steps | measured convergence knee | pre-committed extended-curve rule; paired ascents — **A** | LOW | ✅ |
| Seed ladder (30→…→568) | σ_D pilot fired the >0.10 trigger | σ_D=0.369 measured — **A** | LOW | ✅ |
| K=5 search width | 30 candidates / 6 arms | **C** (budget arithmetic; below Eureka's K=16 executability floor) | MED/HIGH | 🔧 report the first-gen executable rate as a pre-registered adequacy check |
| Fitness = DSR | reward-independent, skew/kurtosis-corrected, un-hackable selection | Bailey-LdP — **B**; BUT selection uses a within-series variance PROXY, not canonical DSR | MED | 🔧 disclose the exact selection estimator |
| **SESOI = 0.05 DSR** | "smallest practically-relevant edge" | **C (fiat — no derivation; the equivalence LINCHPIN)** | **HIGH** | 🔧 **A4**: derive from txn-cost breakeven + portfolio-choice literature + DSR-native |

## Data
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| univ5 panel (Refinitiv, survivorship-free, PIT) | licensed, real, point-in-time | provenance-captured — **A** | LOW | ✅ |
| Split C (2005-16 / 17-19 / 20-25→26) | out-of-sample tail regime in test | design choice — **A/B** | LOW–MED (single macro-regime in test — see F3) | 🔧 disclose; consider a path-aware corroborator |
| Delisting policy (liquidate_to_cash) | conservative, ratified | R44/R73 — **A** | LOW | ✅ |

## Models
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| Confirmatory = Opus 5 | GA, one-frontier, attribution-verified, price-identical | R102 live verification — **A** | LOW | ✅ (thinking now verified off) |
| 10-leg open/closed roster + family pairs | ecosystem diversity + confound-free capability pairs | model sweep — **A/B** | MED (thinking uniformity) | 🔧 **R104** |
| Per-model thinking config | reproducibility + the masking confound + task-fit | empirical per-model verification (only gemini can't disable) + literature — **A/B** | MED→LOW | 🔧 **R104**: uniform reasoning-off + off-vs-high ablation + handle gemini |

## Analysis
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| H2 co-primary IUTs (Sharpe-RA + CVaR-Tail) | two pre-registered performance channels | design — **A** | HIGH (Sharpe leg equivalence power-starved; predicted-tie under λ=0) | 🔧 reframe onto tail+mechanism; A2 drive seeds→~400; A3 add well-powered backbone |
| Multiplicity (IUT + BH-FDR) | error-rate control | design — **A** | LOW→ (strengthens under R105 graphical hierarchy) | 🔧 R105 |
| Mechanism instruments (SQ1-3, report-only) | the actual originality | SQ1 Spearman n=30 — **B**, underpowered | HIGH | 🔧 A3 promote the positive-control into core; pre-commit decision rules |
| Pooled cross-model bounded-effect | where the Sharpe precision now lives (R101) | `src/inference/cross_model.py` — **A** | MED (dependence-honest; seed-limited) | ✅/🔧 |
| Capability gradient g(capability) | the numeracy-bottleneck prediction | 2/10 SWE-bench anchors — **B/C** | MED | 🔧 foreground the pair DiDs; label cross-family descriptive |

## Theory
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| Blackwell garbling → envelope spine | scalar is a garbling of the vector; the envelope gap is the empirical object | proofs (post the M1-M13 fixes) — **A** | LOW | ✅ |
| Risk-measure chain (Artzner / VaR-subadditivity / Gneiting / Fissler-Ziegel) | exactness Okhrati checks | literature, first-hand — **A/B** | LOW | ✅ (verify sign conventions in the Conventions box) |

## Reproducibility
| decision | rationale | evidence + grade | fragility | status / fix |
|---|---|---|---|---|
| Analysis = bit-exact replay | seeded, pinned, parallel==serial byte-identical | `requirements.lock` + seeding + proof — **A** | LOW (device-conditional, disclosed) | ✅ |
| Reasoning-pin round-trip evidence | R85 claim | now IMPLEMENTED + verified (deepseek 145, qwen 650 reasoning tokens captured) — **A** | LOW | ✅ (R103 + audit-fix) |
| "Self-hosted leg" (permanence anchor) | closes the gap closed models can't | **the claim is currently FALSE — no self-hosted leg exists** | MED | 🔧 **A5**: actually self-host Qwen-9B-bf16 on Myriad |

---

## The weakest links (the fragility-ordered close list)
1. **SESOI = 0.05 (C, HIGH)** — the equivalence linchpin, asserted → **A4 derivation.**
2. **H2 equivalence power (HIGH)** + **mechanism instruments underpowered (HIGH)** → **A3 promote H1/H4 to a well-powered backbone** + reframe + drive seeds→~400.
3. **H1/H4 demoted (HIGH)** → ✅ **R105 (2026-07-26): graphical validity tier registered** (H3/H4/N5 confirmatory; H1=N6 de-snoop candidate; ratification-pending).
4. **K=5 (C, MED/HIGH)** → executable-rate adequacy check.
5. **2/6 tail components + α-grid (C, MED)** → justify/reclassify + grid-robustness exhibit.
6. **Self-hosted-leg claim (false, MED)** → **A5.**
7. **Thinking uniformity (MED→LOW)** → **R104.**

Every HIGH-fragility / C-grade linchpin has a concrete fix already scoped. Closing them, in fragility order, IS the path to the non-fragile backbone.

## Exhaustive backbone audit — additions + calibration (2026-07-25, TWO independent auditors)
Both auditors independently confirmed the seed grades AND converged on the SAME grade-capping link (SESOI→equivalence-power). New / refined load-bearing findings (all cite file:line, verified first-hand):

| finding | grade | fragility | fix |
|---|---|---|---|
| **SESOI derivation** — RESOLVED (R104, 2026-07-25). The 0.05-DSR SESOI is now DERIVED from an economic band (cost-breakeven 0.0055 < 0.0756 ann-Sharpe < practitioner 0.10) + registered as FROZEN DATA (`inference.sesoi_derivation`; `tests/test_sesoi_derivation.py`). The Sharpe↔DSR gap is NOT a bug: the TOST is already DSR-native and the T2.5 reconciliation + the INCONCLUSIVE branch already handle it (`CAMPAIGN_power.md`: MDE@80%≈0.120 DSR > 0.05 → a Sharpe-only non-rejection is reported INCONCLUSIVE, never "equivalence"). | A | **LOW** | ✅ done R104 |
| **H2-Tail bounded-effect** — RESOLVED / false-concern (R104 verify, 2026-07-25). The tail bounded-effect IS registered: the R86 pooled 90% seed-block-bootstrap CI on the (dist−scalar) CVaR-5% difference (`preregistration.yaml` synthesis_exactness.pooled_bound), reported in return units AND as a % of scalar CVaR. The "0.25·\|scalar CVaR\| margin" was a first-draft MISREAD (no such number exists in the design) — retracted; no separate tail margin is needed or registered. | A | **LOW** | ✅ verified (no fix) |
| **candidates=30 MISLABELED** — RESOLVED (2026-07-26). Relabelled MEASURE-saturation → **FIX (ratified 2026-07-01 cap; multiplicity control)** (`DESIGN_DETERMINATION.md:9`); the saturation diagnostic is an ADEQUACY check that FLAGS + DISCLOSES budget-limited arms via the `search_adequacy` exhibit, it never re-determined the cap. See the Search-adequacy row. | C | ~~HIGH~~→**LOW** | ✅ relabelled + instrumented |
| **Capability gradient PRIMARY ANCHOR DEAD** — SWE-bench-Verified published for only 2/10 legs (`preregistration.yaml:308`) → the regression cannot run (`cross_model.py:275`) → "gradient" collapses to 2 pair-DiDs + circular M2 | A (that it's dead) | **HIGH** | 🔧 down-rank "gradient"→"pair contrasts"; add ≥1 card-published-SWE-V leg |
| **Per-model authoring reliability MEASURED (2026-07-25, grade-A)** — sandbox gate-pass over 6 diversity-varied authorings: qwen3.5-9b **1/6 (17%)**, qwen3.6-27b 5/6, gemini-2.5-flash (R105 substitute) 5/6, deepseek-v4-pro 6/6. The 9B floor authors the least-robust reward code (shape 31≠30, None-state, TypeError, ast-gate) = the numeracy-bottleneck thesis + the gradient's BOTTOM ANCHOR. Campaign handles it: a sandbox-reject consumes one author draw + is logged (`loop.py:395,470`), the search continues, a leg failing the T0 floor reports as a finding (never a vote) — NO stall. | A | **LOW** | ✅ measured; feeds the reliability table + capability gradient |
| **Closed capability pair NOT confound-matched** — Haiku (4096, no reasoning) vs Opus-5 (8192) → the DiD conflates capability with token-budget/thinking | A | HIGH→**RESOLVED** | ✅ **R106 IMPLEMENTED 2026-07-27** (`f3321b5`): caps matched at **8192 for every leg AND the Opus author** — RAISED never lowered, so no leg lost headroom — and reasoning pinned OFF on all 10. The DiD is now a capability contrast rather than a capability-plus-budget-plus-thinking contrast. ⚠ The decision had been **ratified 2026-07-26 and never implemented**: it was pack item 8, signed off with the rest, but omitted from `ratification_completed` while `ratification_pending` was empty — recorded as neither done nor outstanding, and invisible to all 23 gate checks. The record is repaired in the same commit |
| **Opus-5 thinking record CONTRADICTION** — prereg/llm.yaml assert "adaptive thinking BY DEFAULT" (drives 8192) but the LIVE test shows Opus OFF by default; Anthropic legs never round-trip-gated | C→**resolved** | MED→**LOW** | ✅ **R106 2026-07-27**: the false claim is CORRECTED in `config/llm.yaml` (the 8192 cap is retained on its own merits — headroom + matched caps — not on the disproved premise), and the deeper half is closed too: Anthropic legs had **no reasoning-pin channel at all** (`make_anthropic_transport` took no such parameter and the transport rejects `extra_body`), so they were off by VENDOR DEFAULT rather than pinned. `thinking:{type:disabled}` is now wired end-to-end and **VERIFIED ACCEPTED live on opus-5/haiku-4.5/sonnet-5** before the parameter was added |
| **Self-hosted leg ABSENT** — the "self-hosted on Myriad" permanence pitch is unrealized (all hosted API) | A | MED | 🔧 A5: self-host one open leg OR strike "self-hosted" |
| **Stale MODEL_CARD + REPRODUCIBILITY_CHECKLIST** — 30 seeds / 50k steps / laptop-only / old panel, all superseded | A | MED→resolving | 🔧 CHECKLIST regenerated 2026-07-25 (seeds→ladder / steps→400k / buffer-decoupled / compute→Myriad / test-count→2,057); MODEL_CARD + DESIGN_DETERMINATION regenerating |
| **DSR selection = within-series variance PROXY** — not the canonical cross-trial DSR the paper cites (`deflated_sharpe.py:180`; common-mode → not an ID threat) | A | MED | 🔧 one methods sentence disclosing the proxy |
| **`kvasiuk2026madevolve`** — RESOLVED / KEEP (2026-07-25 audit). The cite is VERIFIED REAL: `refs.bib:1857-1861` (arXiv 2605.23007, q-fin.TR, 4 named authors incl. Münchmeyer), first-hand-verified 2026-07-02 (the refs.bib comment clears the old DO-NOT-CITE flag; corroborated by NOVELTY_FENCE_SWEEP + RELATED_WORK_WATCH + CHANGELOG). The DO-NOT-CITE label was STALE, not the citation. | A | **LOW** | ✅ keep; mandatory pre-submission abs-page re-verify |
| **K=16 stale** in `DESIGN_DETERMINATION.md:18` — contradicts the design K=5 | A | MINOR | 🔧 fixing 2026-07-25 (stale-doc sweep) |
| **N6 val-select was DEAD CODE** — RESOLVED (2026-07-26, self-caught in a non-fragility hunt on my own N6 promotion). H1's confirmatory promotion registered a val-select de-snoop, but `run_campaign._baseline_winner_record` archives `val_fitness=NaN` → `beat_human_baseline` ALWAYS falls back to the White-2000 test-snoop → N6 would have been an UNBACKED confirmatory claim. FIX (Tamer "make it smart + sound"): reframe as a snoop-free **IUT** — beat EVERY canon member one-sided at α == beat the best (`berger1982iut`), selecting no comparator (nothing to snoop, no fragile baseline val-roll). Implemented + tested (`beat_human_baseline.iut`; `test_iut_*`; freeze RC=0; 119 tests). | A | ~~HIGH~~→**RESOLVED** | ✅ done c808117 |
| **Regime config contradiction** (audit #10) — RESOLVED (2026-07-26). `power_analysis.py:308` claimed the gold vix is a decimal so regimes thresholds "never trigger → count collapses to 1 (live bug)", but `regimes.yaml:5` said fixed. VERIFIED: `loaders.load_gold_panel` head-detects + rescales vix×100→points (`loaders.py:455-458`) → regimes.yaml correct, the comment STALE. Comment fixed + `max_plausible_independent_regimes:12` registered (was a silent fallback). | A | ~~MINOR~~→**RESOLVED** | ✅ done c4154ef |
| **H4 N4 comparator strawman-risk** (audit #3 + comparator-research A) — the 2 H4 optimizers (random+GP-EI) search ONE fixed 6-primitive family (`cvar_alpha`/`window` FIXED) → "you beat a crippled search; that's search-space richness, not designer quality". FIX registered: expand to the DFO toolkit {random, GP-EI, TPE, CMA-ES} (cites ADDED c808117; multi-fidelity Hyperband/BOHB pruned as inapplicable under matched-candidate-count) + report LLM-vs-max-over-toolkit (parallels N6). | B | MED | 🔧 registered; BUILD-pending (deps `cma`/`optuna` vet+pin; Okhrati on confirmatory-vs-toolkit) |
| **Allocator suite lacks the tail-optimal benchmark** (comparator-research B) — for a TAIL study the min-CVaR (Rockafellar-Uryasev) allocator is the decisive missing benchmark ("beats even the classical tail-optimal allocator on its own turf"). The 8 are otherwise comprehensive (audit LEAVE-ALONE confirms). FIX registered: add `min_cvar` (near-free deterministic backtest, mirror `min_variance`). | B | LOW | 🔧 registered; BUILD-pending |
| **Search-adequacy severity** (audit #1, TOP grade-mover) — RESOLVED-by-INSTRUMENT (2026-07-26; deep research + ultrathink). K=5/30 is ~1/13 Eureka's 400/task (honestly below the field norm — stated plainly). DEFENCE built + PRE-REGISTERED (`mechanism.search_adequacy`): (1) the tri-partite NULL-SEVERITY decomposition — responsiveness-power (met: the 750-authoring sub-experiment + positive control, UPSTREAM of the 30-search) · search-adequacy (instrumented) · equivalence-seed-power (the E1 ladder) — localises the reviewer attack to ONE leg; (2) the pre-committed `search_adequacy` EXHIBIT (best-so-far saturation + executable-rate + oracle-headroom-vs-N6 + within-gen diversity + coverage-vs-k; `mayo2018severetesting`/`altman1995absence`/`brown2024monkeys`/`liu2024eoh`); (3) the SQ1-upstream-of-K logic (HOLDS for the MECHANISM headline, NOT H2 equivalence — disclosed); (4) DECISION (ultrathink, Ramin-ratifiable): **KEEP K=5/30** — increasing width trades the already-weakest leg (seeds: reframed INCONCLUSIVE, rungs>189 aspirational) for a marginal gain that is conservative for H1/H4 + does not bind the headline (upstream of K); the coverage-vs-k instrument is a stronger defence than a bigger raw K at zero seed cost. EoH/GEPA precedent: small budgets ARE adequate on quality, not width. | C→**A** (defensible + instrumented) | ~~HIGH~~→**LOW** | ✅ instrument PRE-REGISTERED + relabel done + 3 cites; residual = disclosed bounded-limitation for non-saturating arms (e.g. the prototype's scalar flag → re-run + disclose); K-budget Ramin-ratifiable |
| **CH4 prose ↔ config contradiction** (audit #2, communication gate) — CH4:220-222 still "max over FOUR, DESCRIPTIVE-ONLY" + CH4:274/301 "Bonferroni-across-four" while config = 11-canon/N6-confirmatory/graphical-tier. Understates the strongest new result + is an internal contradiction. FIX: rewrite CH4 §4.7+Table 4.1+theory §3.7 to the 11/N6/tier design. | A | MED | 🔧 registered; GATED on R105 ratification (V2 registry) |
| **Fed vector 2/6 components asserted** (audit #5) — `left_tail_mass` (k=2.0σ) + `robust_skew` (Bowley) sit OUTSIDE the §3.5 elicitability theory (which covers only the 4 CVaR levels; theory §3.7 concedes it). FIX: derive-or-reclassify (justify k=2.0/Bowley with a cite OR reclassify as engineering summary stats) + a cheap fed-vector ABLATION (H2-Tail survives dropping the 2) + fix the `robust_skew` "left-tail skew" label/sign. | C | MED | 🔧 registered |
| **α-grid {0.01,0.05,0.10,0.25} arbitrary** (audit #8) — acknowledged-arbitrary, no sensitivity run (`cvar_01` also flagged high-variance). FIX: a report-only grid-robustness exhibit (authored-code responsiveness / H2 ordering invariant under a denser/sparser grid). | C | LOW | 🔧 registered |
| **Single sealed test PATH** (audit #11) — one 2020-2026 realization (walk-forward DEFERRED R43, disclosed CH4:39-51). FIX (optional severity-add): elevate the theory's regime-CONCENTRATION corollary (§3.6) to a report-only DIRECTIONAL exhibit (predicted sign) — free, uses a corollary already derived. | A | MED (disclosed) | 🔧 registered |
| **B\* and σ_D were CALIBRATED on GPU/TF32; the campaign EXECUTES on CPU/fp32** (deep review, 2026-07-27) | the CPU lane (R107) changes the substrate AFTER both calibrations were measured, and `agent_numerics.tf32: True` is a **CUDA-only** switch — so the pilots' matmuls ran at ~10-bit mantissa while the campaign's run at full 23-bit | **Grade B (inferred, NOT archived).** R77 states the B\* curve was measured on **"cluster val-DSR"** (2026-07-18), and the cluster path defaulted to `device: cuda` until the CPU lane landed 2026-07-26; `run_sigma_pilot_train.py` defaults `--device auto`, which resolves to cuda on the laptop, and the σ_D pilot (2026-07-03) predates the cluster integration (2026-07-08). ⚠ The pilot records carry **NO device provenance at all** — `env_fingerprint` is a bare label string (`sigma_pilot:<reward>:test[3835,5406)`), with no `env.json`, no `cuda_available`, no device — so this cannot be VERIFIED from the archive, only inferred | **LOW for the decisions, MEDIUM for reproducibility.** B\* was chosen at a pronounced KNEE (+0.145/+0.161/+0.162 at 400k/800k/1.6M, increments beyond collapsing to +0.016-0.017) — a large-scale feature of the learning curve, not a ULP-level one, so TF32-vs-fp32 cannot move it. The σ_D headline (σ_seed 0.244 DOMINATES σ_D 0.369, ρ negative) is a qualitative ordering, equally robust. What is genuinely affected is the CLAIM of an unbroken determinism envelope from calibration to execution | **DISCLOSE, do not re-run** (the pilots are weeks of compute and the decisions do not move). State in CH4/Limitations that B\* and the seed count were calibrated under GPU/TF32 arithmetic and executed under CPU/fp32, and that the pilots predate device provenance. FORWARD FIX ALREADY LANDED: `run_one._resolved_device` now stamps the device that ACTUALLY ran into the campaign's env fingerprint, so the executed campaign records what the pilots could not |
| **★ PACKED JOBS ARCHIVE THE PARENT'S THREAD ENVIRONMENT, NOT THE TRAINER'S** (live pack-4 rehearsal, 2026-07-27) | The S6 homogeneity audit reads the archived thread/device fields to detect a heterogeneous determinism envelope. That only works if the record describes what ACTUALLY trained — the same 'record what ran, not what was requested' principle behind the device stamp fixed earlier today | **Grade A (measured, with a controlled contrast).** The pack-4 rehearsal archived `OMP_NUM_THREADS: 4, torch num_threads: 4` for a run launched with `--search-threads 1`. The trainings themselves were CORRECT — the heartbeat shows ~12.2 steps/s per training, consistent with 1 thread — so this is a PROVENANCE defect, not a correctness one. CAUSE: at `pack >= 2` the workers return records and the PARENT archives them, so `_archive` -> `_run_env_fp` -> `capture_env` runs in the parent, which never called `_worker_init` and still carries the `OMP_NUM_THREADS=4` that SGE sets automatically from `-pe smp 4`. CONTRAST that proves it: the pack-1 rehearsal recorded `OMP=1` correctly, because inline archiving happens in the SAME process that was initialised | **MEDIUM-HIGH for reproducibility, NIL for correctness.** The campaign's science is unaffected — trainings run at the intended thread count. But every packed record would misreport the determinism envelope, and that field is exactly what an audit would consult to rule out a heterogeneous run. Reproducibility is the project's stated #1 criterion, so a knowingly-wrong provenance field is not acceptable to ship silently | **DECIDES `--pack` FOR THE LAUNCH.** pack 1 keeps provenance correct (inline path) and still completes the full 568 ladder in 13.4 d with the headline at 1.9 d; pack 2-4 reaches the 7.93 d chain floor but mis-records threads. RECOMMENDATION: launch **pack 1** and treat the capture fix (capture env INSIDE the worker rather than the parent) as a scoped follow-up that can be applied before a later rung. Do NOT rush it at launch time — it touches the archive path |
| **★ SUSTAINED CAPACITY IS THE ONLY THING THAT CAN STILL CHANGE THE RESULT — measured live, and it is BELOW plan** (2026-07-27 evening) | The rung reached by the Aug-27 stop is set by sustained cores, and the ladder tops out at 568. At 1,685 cores the campaign finishes in 8.9 d (the serial-chain floor) and completes the FULL ladder; at 500 it truncates to rung 403. Every timeline in the project assumes a sustained figure that has never been measured with the real job shape | **Grade A (live, first-hand, 2026-07-27 19:00–19:30).** Submitted 121 jobs (100-job p6 ladder + control + rehearsal). Observed: 9 running at 19:00 -> 43 at 19:15 -> 70 at 19:25 -> **80 running / 140 cores at 19:29, with 41 still QUEUED** against ~5,800 cores free cluster-wide. Structural caps read from the scheduler: `max_u_jobs = 1000`, `max_aj_instances = 1000` -> **cores = 1,000 x cores-per-job**, so pack-1 is hard-capped at 1,000 cores and anything above REQUIRES packing. Placement latency measured by shape earlier: 1-core ~4–6 min, 2-core ~8–11 min, and the 1-core 1.6M rung placed 20/20 while 2-core jobs sat 32-deep — i.e. placement difficulty rises with cores-per-job | **HIGH — this is the live risk.** 140 cores is an order of magnitude below the 1,685 the plan assumes, with jobs queued and capacity free, so we are throttled by fair-share/node-fit rather than by the job cap or the cluster. The two routes to more cores CONFLICT: exceeding 1,000 needs packing, but packing appears to place worse. ★ SHAPE TEST RESOLVED (19:30:48, 5m10s after a simultaneous submission of 10x1-core and 10x8-core at identical priority, pool and budget): **1-core 10/10 PLACED, 8-core 0/10**. Corroborated by three independent observations the same evening — the 1-core 1.6M rung placed 20/20 while 2-core jobs sat 32-deep, and the rehearsal's 8-core jobs did not place in 20 min. MECHANISM: our jobs sit at the cluster priority FLOOR, so we live on BACKFILL, and backfill slots small jobs into gaps while larger jobs need a reservation the scheduler will not grant low-priority work. So packing — the only route above 1,000 cores — is UNAVAILABLE to us | **MEASURE, THEN SET `--pack` FROM THE MEASUREMENT.** Do NOT freeze a pack value on inference. Deliverables from the run in flight: (i) the shape verdict once our queue drains, (ii) sustained cores over a full cycle, (iii) the per-node throughput DISTRIBUTION (p10/median/p90) which is what the makespan actually depends on. Fallback that is already safe: pack-1 at ~1,000 cores gives a 15-day campaign inside a 31-day window, so the run is NOT at risk — only how early it ends. The 636-core figure must not be re-quoted as sustained (see its own row) |
| **★ PROPOSAL (Tamer's ratification required): PUT THE 3 DFO COMPARATOR CHAINS ON GPU — it breaks the 8.9-day serial cap** (2026-07-27) | The campaign's makespan is `max(work/cores, longest serial chain)`. The chain term is a HARD FLOOR no number of cores can touch, and at 1 CPU thread it is **8.90 d**, which caps useful capacity at **1,685 cores**. Tamer's standing directive is to take everything Myriad offers; past 1,685 cores every additional core currently buys NOTHING. This is the only lever that moves that floor | **Grade A (computed from measured constants).** Chain depths x per-training cost: `bayes_opt` 25 serial, `tpe` 20, `cma_es` 20, LLM reflection 6. At 13.0 steps/s/core CPU vs the 102 steps/s G1 GPU anchor: bayes 8.90 d -> 1.13 d, tpe/cma_es 7.12 d -> 0.91 d. **bayes ALONE is not enough — TPE then becomes the pole at 7.12 d** (a trap `lanes.py` already flags). All three on GPU moves the pole to the LLM reflection chain at **2.14 d**, raising the ceiling **1,685 -> 7,021 cores**. Cost: **71 GPU-hours total, ~27 h wall on 3 GPUs**; 23 GPUs were free at the 2026-07-27 probe. Makespan at 5,000 cores: 8.90 d -> **3.00 d** | **LOW scientifically, MEDIUM operationally.** It is a determinism-envelope change on the SEARCH leg only — the sealed TEST leg, where every scored contrast and all CRN pairing live, stays uniformly 1-thread CPU and is untouched. Covered by ratified principle: R108 admits the device as a **nuisance BLOCK** with comparison units kept device-homogeneous, and R107's own logic is that a search-leg change alters only WHICH candidate is selected, never a measured quantity. **THE INTEGRITY ARGUMENT IS INVERTED IN OUR DISFAVOUR, which is why it passes:** `bayes_chain.py` refuses q-EI/n_init speed-ups because they "would make our own hypothesis look better" — q-EI is LESS sample-efficient, so it WEAKENS the control. GPU changes float reduction order only, and applies to the three CLASSICAL optimisers the LLM must BEAT, so it can only make the controls HARDER to beat. Conservative for us, not favourable. Operational risk: GPU queue latency has historically been hours-to-days | **TAMER'S CALL — a search-leg envelope change, same class as R107, never a lane's unilateral action.** If ratified it also RESOLVES R107 cleanly: rather than amend a number that does not reproduce (2.72x measured at ~1.18x), REPLACE the lever — GPU buys 8.90 -> 2.14 d where threads buy 8.90 -> 7.52 d, and the 8-thread config is ~6.7x less core-efficient anyway. **Deliberately EXCLUDED: the LLM reflection chains stay on CPU** — those are OUR arms, and accelerating our own hypothesis is exactly the asymmetry the integrity rule guards against. Verify GPU queue latency before committing; must be DISCLOSED in CH4 as an executed-config choice |
| **⚠ RETRACTED 2026-07-27 21:0x — R107 WAS NEVER TESTED. The row below claimed a refutation it could not support; the corrected finding is in the cell after it** (live CPU gate, 2026-07-27) | **THE INSTRUMENT WAS INVALID.** The '8-thread' arm was submitted at 16:58 against cluster code `a4f903c`, whose `_worker_init()` takes NO thread argument and hardcodes 1 — verified by `git show a4f903c:src/orchestration/parallel.py`. The R107 wiring was committed at 16:43 but not DEPLOYED until ~18:55. **So that arm ran at 1 thread.** My corroborating claim — 'qstat shows 8.06x CPU so the threads were genuinely engaged' — was reading SGE's SLOT-seconds as CPU utilisation: `-pe smp 8` reports 8 x wall regardless of threads used, and 8 slots x 46 min = 6:08 against the observed `cpu=06:10:56`, a near-exact match. **Every derived figure (1.26x, 1.18x, 'the chain is 7.5 d not 3.3 d') is WITHDRAWN.** R107's 2.72x is UNTESTED, neither confirmed nor refuted | **The error was mine and it is the exact failure this project guards against**: a conclusion built on an instrument I had not validated, then reported with confidence. Recorded rather than quietly deleted | **WHAT SURVIVES, and it is useful.** Both arms ran at 1 thread, so this is n=9 of the 1-THREAD rate on the real workload: 13.0/13.1/13.2/14.0/14.6/14.9/16.1/16.1/16.6 -> **mean ~14.6 steps/s, ~12% ABOVE the registered 13.0**, i.e. the campaign's planning constant is CONSERVATIVE. **DO NOT amend R107** — 'untested' is not grounds for changing a registered value. `--search-threads 1` remains correct on two INDEPENDENT grounds: (i) the throughput arithmetic holds at ANY speed-up including 2.72x (8 threads consume 8 cores for at most a modest latency gain), and (ii) 8-core jobs were measured NOT TO PLACE (10/10 vs 0/10), which is about the core REQUEST and wholly independent of threads |
| **[SUPERSEDED — see the retraction above] R107's 2.72x thread speed-up** (live CPU gate, 2026-07-27) | R107 was ratified on a measured 2.72x at 8 threads and is the basis for (a) the `bayes_opt` chain falling 8.9 d -> 3.3 d, (b) the core crossover moving 1,685 -> 4,584, and (c) the claim that "the GPU dependency disappears entirely". All three are downstream of that one number. It was wired into execution earlier the same day | **Grade A, first-hand, n=2 and tight.** First full trainings ever completed on the Myriad CPU lane with the REAL `univ5` gold panel: `p6dist-b60000-s1` 3,729 s (**16.1 steps/s**) and `p6scal-b60000-s1` 3,624 s (**16.6 steps/s**) — 60,000 steps, 8 threads, pool `d`, `qstat` confirming a sustained 8.0x CPU parallelism throughout (the threads WERE engaged). So **~16.4 steps/s at 8 threads = 2.05 steps/s PER CORE**, against the registered 13.0 steps/s/core at 1 thread. Speed-up ~**1.26x**, not 2.72x. R107's bench (jobs 17784/17836) ran on the `b` pool with a synthetic profile at 02:30–04:30 UTC; this is the `d` pool, the real panel, shared nodes at 13–27/36 load, Monday evening | **HIGH for R107, LOW for the campaign plan.** The campaign's own constant (13.0 steps/s/core at 1 thread -> 8.55 h per 400k training -> 23.6 d) is NOT contradicted by anything measured here. What is contradicted is the 8-thread configuration, which R107 confines to the SEARCH/chain leg. Consequences: the chain is ~7.0 d not 3.3 d; the crossover is far below 4,584; and "the GPU dependency disappears" no longer follows from the measurement it rests on. Worse, threads are revealed as ~**6.3x less core-efficient than packing** (8 cores as 8 one-thread trainings ≈ 104 steps/s aggregate vs 16.4 for one 8-thread training) — so on the THROUGHPUT lane threads are strictly wrong, which is what `lanes.py` LANE 2 already said, now with harder evidence | **DECISION FOR TAMER, BEFORE THE FREEZE.** Do NOT silently keep an amendment whose headline number does not reproduce. Options: (a) amend R107 to record the measured 1.26x and re-derive the crossover/chain figures from it — the honest default, and it does NOT change the campaign's throughput plan; (b) keep 8 threads on the chain anyway (1.26x is still a real latency gain on a genuinely serial path, and the chain is not core-efficiency-bound); (c) re-bench on `d` at n>2 before deciding. The 1-thread control arm (6 jobs, submitted 18:00) pins the per-core denominator and lands ~19:20. **The campaign's max-throughput shape is now measured, not assumed: 1 thread per training, packed 8 per job.** |
| **THE 636-CORE CAPACITY FIGURE IS A BEST-CASE CEILING BEING USED AS A SUSTAINED RATE** (deep review, 2026-07-27; Tamer's question) | Every campaign timeline — 23.6 d for the full rung-568 ladder, the `lanes.plan_lanes` makespan, the "fits the Aug-27 window" conclusion — is computed from a SUSTAINED core count, and 636 is the number being used. It decides which rung the exogenous stop reaches, i.e. the depth of the headline result | **Grade A on the discrepancy, measured both sides.** The 636 peak was measured **02:30–04:30 UTC** (the cluster's quietest window) using `bench_compute.py` at its **default 2,500 steps ≈ 3 min/job** (`bench_compute.py:130`). The campaign runs **8.5 h jobs** (400k steps at 13.0 steps/s) at ALL hours. Measured live at 16:09 UTC Monday 2026-07-27 while submitting the CPU gate: **2,269 pending / 2,312 running cluster-wide**; our 8-core jobs went 0.00000 -> 2.19–3.39 priority and the first placed in ~5 min, all 6 running within ~12 min. A 3-minute job backfills into almost any gap; an 8.5-hour job needs the scheduler to commit an 8.5-hour window — a structurally harder placement | **HIGH, and it sits on a cliff.** From `lanes.total_trainings` + `training_core_hours`: at 636 sustained cores the full 568 ladder completes in 23.6 d and fits the window; at **500 sustained it truncates to rung 403**. So a ~20 % capacity shortfall costs a whole assurance rung — and the shortfall is plausible precisely because the measurement conditions (night, 3-min jobs) are the friendliest possible and the campaign's are not. I had been quoting 636 as a sustained expectation, including in a planning table shown to Tamer; that was too optimistic | **MEASURE IT WITH THE REAL SHAPE — already running.** The CPU gate (jobs 27012, 27016–27020) is the first data point at the real shape: real gold panel, daytime, normal contention, long jobs. The wide run that follows gives the per-node throughput DISTRIBUTION (p10/median/p90) across many nodes and hours, which is what the makespan actually depends on — a point estimate cannot express placement risk. Until then, treat 636 as a CEILING, plan against the measured sustained figure, and state the rung expectation in the write-up from the measurement rather than the ceiling. Do NOT re-quote 636 as sustained |
| **THE 1000-CORE COURTESY RESERVE IS STATED POLICY WITH NO MECHANISM** (deep review, 2026-07-27) | `killswitch.plan_footprint` is the module that makes "go as fast as Myriad allows" and "never jeopardise the account" the same policy: a pressure-scaled share (90/70/50 %) clamped by `FREE_CORE_RESERVE = 1000` so "our campaign can never be the reason someone else waits for a plain CPU slot". That guarantee is what makes the 8192-core ceiling defensible | **Grade A (measured by call-graph, verified by grep).** `plan_footprint`'s ONLY caller is `allocation.advise_cpu_lane` (`allocation.py:149`) — the GO-day advisor, whose output a HUMAN reads. No submission path consults it: not `campaign.run_batch`, not `driver.submit_batch`, not `run_campaign_cluster`. The reserve is therefore printed, never applied. Found while auditing whether removing the `-tc 38` artifact would leave the CPU lane without a brake | **LOW for the cluster, MEDIUM for us, HIGH for the claim.** SGE fair-share arbitrates our entitlement before dispatching anything, so an unthrottled submission cannot actually starve other users — the honest statement is that removing the throttle stops us starving OURSELVES. But a stated courtesy guarantee with no mechanism is the R85 pattern, and it is exactly the sort of claim that must not reach the write-up or a UCL RC conversation unbacked. It is also the reason `-tc 38` felt load-bearing: it was the only mechanical limit on the legacy path, doing the right job for the wrong reason at 0.7 % of the machine | **ENFORCE AT THE SUBMIT LAYER, never in the renderer.** `render_jobscript` stays a pure deterministic function (no live queries) and now defaults `tc` lane-aware: cuda keeps the real 38-GPU pool width, cpu imposes nothing. The enforcement point is the submit path, which CAN query live capacity: derive `tc` (or the round size) from `plan_footprint(free_cores, pending_jobs)` and pass it explicitly. Until that lands, an operator enforces a stand-down with an explicit `tc`, and the advisor's printout remains the human-in-the-loop control. NOT a launch blocker — the documented launch config (`--chunk-tasks 1`) renders one-task arrays where `tc` is inert either way |
| **R107 — WIRED AND EXECUTING (2026-07-27); the row below is now CLOSED** | The registered value must be the executed one. R107 was ratified on a measured 2.72x bench, fed `lanes.CPU_CHAIN_THREADS = 8` to the makespan planner, and was never applied — `_worker_init` pinned 1 unconditionally. Wiring it is what converts capacity into speed: it moves the core crossover from 1,685 to 4,584, which matters because the 2026-07-27 probe found ~5,802 cores free | **Grade A (measured, falsifiable).** `_worker_init(threads=1)` now applies the count it is given (BLAS env + `torch.set_num_threads`); `run_one._task_threads` reads it from the spec and FAILS LOUD on a mixed-thread task; `DevicePool(init_threads=)` carries it to the packed path via initargs; `bayes_chain` passes `CPU_CHAIN_THREADS` (R107's primary target, the 25-step serial GP-EI chain); `campaign.run_batch(threads=)` injects it onto specs AND raises the job's core request in the same expression, since threads without cores is oversubscription and SLOWER than 1 thread. `ClusterRun.search_threads` resolves from the DEVICE at build time, so the registered value cannot drift from the executed one by someone forgetting a flag. **PROVEN FALSIFIABLE:** reverting `_worker_init` to its hardcoded form makes the new test fail with `torch.get_num_threads()=1, asked for 8` — precisely the defect that had been live | **LOW.** Scope is R107's own: SEARCH/chain leg only. Every SCORED comparison (H1/H2/H3/H4, every paired contrast, all CRN pairing) is on the TEST leg, which never passes `threads` and stays uniformly 1 — so threads change only WHICH candidate is selected, never a measured quantity. CPU-only by construction (on the GPU lane the card does the arithmetic and extra CPU threads would only inflate the core request where cores are the binding constraint). `threads` is passed ONLY when > 1, so the entire 1-thread path is byte-identical to the certified one | **DONE.** The missing test class is now closed: `test_worker_init_ACTUALLY_APPLIES_the_thread_count_it_is_given` runs the executor in a SUBPROCESS and asserts `torch.get_num_threads()`. The pre-existing test compared the REGISTRY to a CONSTANT — two declarations — which is exactly why it could not see a year-long gap between declaration and execution. Any future envelope knob gets the same treatment: assert the EXECUTED value, never the declared one |
| **R107 (chain threads 1 -> 8) WAS RATIFIED AND MODELLED BUT NOT EXECUTED — CLOSED 2026-07-27, see the row above** (deep review, 2026-07-27) | R107 was ratified by Tamer on 2026-07-26 on a two-job bench (2.72x at 8 threads) and its stated payoff is that the `bayes_opt` GP-EI chain falls 8.9 d -> ~3.3 d, "which drops it below the throughput term ... and the GPU dependency disappears entirely". The planner believes it. The executor does not | **Grade A (measured, read from the execution path).** `parallel._worker_init` UNCONDITIONALLY sets `OMP/MKL/OPENBLAS/NUMEXPR_NUM_THREADS = "1"` and `torch.set_num_threads(1)` (`parallel.py:229-242`) with no config gate; `run_one.py:222` calls it, and `bayes_chain.py:125` calls it too -- so the chain leg R107 targets runs at 1 thread. `jobscript.py:186-187` asserts the OPPOSITE of R107 in a comment ("CPU trainings are single-threaded ... multi-thread BLAS would change float reduction order"). Meanwhile `lanes.CPU_CHAIN_THREADS = 8` feeds `allocation.advise_cpu_lane` (`allocation.py:151,161`), and `tests/test_cluster_lanes.py:119` tests the CONSTANT against the REGISTRY but nothing tests either against EXECUTION | **NIL TODAY, BINDING AT THE CAPACITY WE ARE CHASING.** Measured via `lanes.plan_lanes(rung=568)`: at 636 cores the campaign is throughput-bound at **23.59 d** with the chain at 8.90 d (1 thread) or 3.27 d (8) -- identical makespan, so the gap costs ZERO right now. The crossover is **1,685 cores at 1 thread vs 4,584 at 8**: at 2,000 cores the unwired version becomes CHAIN-bound at **8.90 d** while the wired one stays throughput-bound at **7.50 d**, and past that point extra cores buy NOTHING until it is wired. Tamer's standing campaign-speed directive is to take as much as Myriad offers, and 636 -> 1,685 is the ceiling this silently imposes. Independently of speed it is an R85 integrity defect: a ratified amendment the code ignores | **TAMER'S CALL — a determinism-envelope change, never a lane's unilateral action.** Two honest routes: **(a) WIRE IT** — gate `_worker_init` on a thread count, pass `CPU_CHAIN_THREADS` on the search/chain leg only, keep the TEST leg at 1 (R107's own scope: every scored contrast lives on the 1-thread TEST leg, so no comparison unit changes), and add the missing constant-vs-execution test; or **(b) AMEND R107 to 1 thread** and re-plan, accepting the 1,685-core ceiling and the 8.9 d chain, and correct the "GPU dependency disappears" claim which rests on the 8-thread figure. What is NOT acceptable is launching with the planner at 8 and the executor at 1. Note this also DECIDES the thread regime for any CPU re-calibration: the ladder must run at whatever the campaign will actually execute |
| **R77's REPRODUCTION PATH IS BROKEN and the MANDATORY F11 exhibit is UNBUILDABLE** (deep review, 2026-07-27) | the R77 amendment row states the B\* verdict is "reproducible via `scripts/apply_bstar_rule.py`" and registers the 16x curve as a MANDATORY figure. Both read the per-seed archive `outputs/p6ladder/search`, which was destroyed in the 2026-07-27 01:56 deletion, is absent from git (never committed), and predates nothing in the stale 2026-07-01 backup. The incident write-up recorded p6ladder as ABSENT but assessed consequences only for `leg_gates`/`spend_ledger` | **Grade A (measured, not inferred).** Ran it: `python scripts/apply_bstar_rule.py` exits `INCOMPLETE cell p6dist b=100000: seeds []`. `ARCHIVE = outputs/p6ladder/search` (`apply_bstar_rule.py:20`); `budget_curve_exhibit` (`src/viz/figures.py:512`) needs `{winner: {budget: {seed: val_dsr}}}` ABSOLUTE per-seed levels, which lived only there. The git-tracked `outputs/tables/bstar_rule_verdict.json` SURVIVES and holds the load-bearing numbers (paired diffs, means, SEs, ratios 2.9-5.4x) | **LOW for the DECISION, HIGH for reproducibility + a registered deliverable.** B\* = 400,000 is registered and the verdict numbers are preserved and auditable, so nothing about the campaign changes and this is NOT a launch blocker. But a stated reproduction path that errors out is the R85 failure mode ("a pin nobody can verify is FICTIONAL"), and the verdict JSON holds only paired DIFFERENCES vs 200k at 400k/800k/1.6M -- enough to plot relative ascent with SEs, NOT the absolute 5-point 16x curve F11 specifies (the 100k and 200k levels are gone) | **REGENERATE ON THE CPU LANE — the single best use of pre-launch compute.** Costed from the measured constants (`lanes.CPU_STEPS_PER_S_PER_CORE = 13.0`): 5 budgets x 3 CRN seeds x 2 winners = 18.6M steps = **397.4 core-hours = 0.110 % of the 360,068 core-hour campaign**, wall-clock 34.2 h on the 1.6M rung. It buys THREE things at once: (i) restores F11 + the reproduction path, (ii) re-derives B\* on the EXACT execution substrate, converting the row above from Grade B inferred to grade-A measured, (iii) validates the CPU path end-to-end at up to 1.6M steps. The regenerated numbers will NOT match the archived GPU verdict (different substrate, non-identical arithmetic) -- report BOTH, which is itself the cross-substrate parity exhibit. Interim fallback if not run: rebuild F11 from `bstar_rule_verdict.json` as a relative-ascent plot and state plainly that the absolute curve's raw archive was lost. NEEDS TAMER'S GO (cluster submission) |

**Grade-capping link (both auditors) — NOW STRENGTHENED (2026-07-25):** the SESOI is DERIVED + registered as frozen data (R104), no longer asserted, and the Sharpe↔DSR unit handling was already rigorous (T2.5 reconciliation + INCONCLUSIVE branch). The bounded-equivalence backdrop remains the most CONDITIONAL promise in the PDF (power-limited below n\*≈173 seeds): keep it explicitly conditional/inconclusive; never let a chapter assert "equivalence". The mechanism headline is insulated (it pivots to mechanism + the pooled bounded-effect).
**Strengths confirmed grade-A:** identification (code-enforced `assert_fixed_agent_across_arms`), the fixed-SAC structural argument (Bäuerle-Ott/Lim-Malik), the theory spine (post M-fixes), the endogeneity honesty (exemplary), B*/seeds/delisting/panel (measured in-repo), the multiplicity (IUT ≤ α + BH-FDR).
