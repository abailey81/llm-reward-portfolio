# RUN 25 — SESSION PROMPT. **READ THIS IN FULL BEFORE YOUR FIRST SUBSTANTIVE ACTION.**

Written 2026-08-06 ~07:00 UTC at Tamer's instruction: *"document absolutely everything from this
session, and write a prompt for the next Claude Code session, don't forget to also tell it to very
deeply and extensively study absolutely all files in this dissertation so it has the comprehensive
knowledge, and zero gaps in knowledge … ensure extremely smooth transition … include my prompts on
that everything should be flawless … preserve these loops, but dive much deeper, and check more
extensively, and check very deeply absolutely everything, from all dimensions and angles possible,
it must not miss anything … minimise the ETA to an absolute minimum … maximise the cores … we went
from 2300 cores at our peak to 544 cores now, this is ridiculous. We need to explore absolutely
everything, and maximise the amount of cores, get back to 2k, and minimise the ETA."*

> **You run the live campaign on an irreplaceable MSc dissertation.** RUN 4 has been running since
> 2026-07-28 21:08 UTC — **T+201 h, elapsed 8.4 d**. Real money is spent, the test data is sealed,
> **there is no re-run.** This supersedes `docs/RUN24_SESSION_PROMPT.md`; where they disagree,
> **THIS WINS.**
>
> ⚠ **A SEPARATE SESSION OWNS THE WRITE-UP.** `paper/**`, `docs/GRADE_95_MASTER_PLAN.md`,
> `docs/V2_WRITE_TIME_REGISTRY.md`, `docs/CITATION_WORK_MAP.md` are **NOT YOURS.** `CHANGELOG.md` is
> **SHARED** — re-read it immediately before every edit, and never reuse a date label.
>
> ⚠ **DO NOT REGISTER A LANE.** Reading the board with `lanebus.py` STAMPS a heartbeat. Avoid it.
>
> ⚠ **THERE IS NO "END OF WORK" SECTION IN THIS FILE, DELIBERATELY.** Tamer decides when work ends,
> not you. Never announce that you are wrapping up, and never ask whether to stop. **Record
> continuously** (CHANGELOG + the ledger + the cursor) because the record is write-up raw material,
> not because a session is closing.

---

# §0 ★★★★★ TAMER'S STANDING BRIEF — VERBATIM. THIS IS THE OPERATING CONTRACT.

### §0.1 THE FLAWLESSNESS MANDATE

> *"very deeply and strictly monitor everything constantly and ensure absolutely everything is
> strictly absolutely flawless 10000000%. Ultrathink very deeply and extensively. I give you full
> permissions, full freedom, and I ratify the actions. **I give you no permission to stop until
> absolutely everything is strictly absolutely 10000000% absolutely flawless.** Do whatever it
> takes. Very deeply investigate everything, and speed up to an absolute maximum. Please before act,
> make sure you very deeply study this dissertation. Take as much time as you need, as many tokens
> as you need. Make sure you also very deeply and extensively constantly check each record, make
> sure every record individually is very strictly flawless, logical, meaningful. Don't be lazy, I
> give full ratifications, full freedom, full permissions. Please make sure you study every file in
> this project very deeply, all processes, the whole thing going on on Myriad, absolutely
> everything, please don't miss anything. This campaign run is extremely important, and it must be
> absolutely flawless across absolutely all dimensions possible. Dive extremely deep, don't be lazy,
> check absolutely everything very deeply and extensively, make sure you don't miss anything, and
> make sure you always verify, and you are always very precise. Please work very accurately, and
> very surgically, make sure you make no mistakes. Ultrathink 100000 times before doing anything."*

> ★ **2026-08-06, repeated several times:** *"make sure absolutely everything is very logical,
> meaningful, and absolutely flawless and 1000000% absolutely strictly correct"* · *"ensure
> absolutely everything is 100000% correct everywhere without exceptions"* · *"make sure you act
> with accordance to the priorities and targets. Verify everything always … and make sure you are
> 100000% confident."*

### §0.2 ★★★★★★ THE CORES + ETA DIRECTIVE

> **Tamer, verbatim, repeatedly:** *"we went from 2300 cores at our peak to 544 cores now, this is
> ridiculous. We need to explore absolutely everything, and maximise the amount of cores, get back
> to 2k, and minimise the ETA."* · *"the speed has to be maximised to an absolute maximum, the eta
> has to be minimised to an absolute minimum, we must take as much as we can from Myriad."* ·
> *"explore absolutely everything Myriad has, all pools, everything."* · *"make sure the accumulated
> cores speed up the campaign and are very efficient and not just there."*

**THIS IS AN ACTIVE DUTY EVERY PASS.** ⚠ **BUT READ §5 BEFORE MEASURING ANYTHING.** The cores
question has now consumed RUN 23 (eight wrong published answers) and RUN 24 (a full exhaustive
sweep). **§5 separates what is SETTLED BY MEASUREMENT from what is genuinely OPEN.** Re-deriving the
settled half is the single biggest waste available to you.

⚠ **AND THE 544 FIGURE IS STALE — IT IS NOW 800.** RUN 24 took cores from **544 → 800** (+47%).
Quote Tamer's directive as the standing goal, but never quote 544 as the current state.

### §0.3 ★★★★★★★ THE ABSOLUTE PRIORITY — **THE FLOOR FIRST, THE LADDER SECOND**

> *"our main priority is to bank all the results for absolutely all arms at 30 seeds first, the
> ladder is optional comparing to that. We need the results to write the dissertation now, and we
> need them fast."* · *"getting first 30 seeds for everything must be an absolute highest priority."*

### §0.4 THE LOOP CONTRACT, VERBATIM

> *"Also every 30 min I want you to very closely check everything very deeply, check everything, all
> lines, all records, all outputs, all processes, absolutely everything, it all must be
> 1000000000% strictly absolutely flawless."*
> *"make sure if something is found, its always fixed"* · *"make sure the checks that are every 30
> minutes do not stop until they ensure absolutely everything is 10000% absolutely strictly
> flawless. I give them full permissions, do whatever it takes."*
> *"also dont forget to add the speed check component all the time, and its maximisation"*
> ⭐ *"make sure you very deeply and extensively study this whole project, have an extremely
> extensive knowledge and absolutely 0 gaps in knowledge."*
> ⭐ *"preserve these loops, but dive much deeper, and check more extensively, and check very deeply
> absolutely everything, from all dimensions and angles possible, it must not miss anything."*

**RE-ARM THE 30-MIN CRON** at `7,37 * * * *` with the STEP contract in §9. RUN 24's cron died with
its session. The campaign's own `cycle.py` (~1 min) never stops and is green.

### §0.5 HOW TO READ THAT MANDATE — the most important paragraph in this file

**Full permission raises the bar on the THINKING; it does not lower the bar on VERIFICATION.**

RUN 23 published EIGHT wrong conclusions before the right one. **RUN 24 made TEN of its own mistakes
and caught every one — the difference was always a SECOND, INDEPENDENT route.** §13 lists them all,
because the failure shapes repeat. **Two derivations, or it is not a finding.**

⚠ **AND TAMER'S RATIFICATION DOES NOT OVERRIDE THE HARNESS PERMISSION CLASSIFIER.** RUN 24 was
refused `qhold`, `qrls`, `Stop-Process` on live campaign processes, and `qdel` — three times, after
explicit ratification. When that happens: **stop, say so plainly, hand Tamer the exact command.** Do
not route around it.

---

# §1 YOUR FIRST COMMANDS

```bash
cd /c/Users/User/Desktop/dissertation_papers/llm-reward-portfolio
date -u +%Y-%m-%dT%H:%M:%SZ                      # ★ FIRST. Never assume the clock.
python docs/ops/loginnode_guard.py --once
tail -5 docs/ops/watch/CYCLE_LOG.md
.venv/Scripts/python.exe docs/ops/remote_inbox.py --status      # is Tamer waiting on you?
.venv/Scripts/python.exe docs/ops/core_accumulator.py           # ★ cores, 8 invariants, ATTRIBUTION
.venv/Scripts/python.exe docs/ops/job_rank_governor.py          # ★ THE FLOOR + THE FLEET BALANCE
.venv/Scripts/python.exe docs/ops/line_balance.py --once
.venv/Scripts/python.exe docs/ops/arm_jobs.py
.venv/Scripts/python.exe docs/analysis/record_seed_completeness.py
.venv/Scripts/python.exe docs/analysis/loader_collision_watch.py
ls outputs/campaign_cluster_run4/batches/*.permanent.jsonl | grep -icE "sweep|_test"   # MUST be 0
ssh -o BatchMode=yes myriad "hostname"
```
Then say **"Resuming from: … — next: …"** and CONTINUE. **Never ask "what now".**

⚠ **`bash docs/ops/run_record_layers.sh` COSTS ~1,342 s OF FULL-ARCHIVE SCANNING** and can push the
cycle sweep past the 900 s false-DEAD cap. Run it hourly or after ≥500 new records, **not every
pass.** ALL RC=0 at 2026-08-06 01:26Z.

⚠ **YOUR OWN SSH LOAD MOVES THE SWEEP.** RUN 24 measured sweeps of **27-29 s** and **741 s** at the
SAME archive size, twenty minutes apart — the variable was its own concurrent scanning. Before
blaming archive growth for an elevated sweep, check whether YOU caused it.

---

# §2 ⛔ MANDATORY READING — ZERO GAPS IN KNOWLEDGE IS A REQUIREMENT

**Tamer's instruction is explicit: study EVERY file, all processes, everything on Myriad, and have
ZERO gaps. Read these END TO END before acting, not in excerpt.**

| file | why |
|---|---|
| **this file** | the brief |
| **`docs/ops/watch/FLAWLESS_LEDGER.md`** | ⭐ **THE CONTRACT FOR THE LOOP.** Read the top sections IN FULL: the CONTRACT · **THE PLACEMENT POLICY** · the **JSV-throttle rule** · the SPEED component · every OPEN row and every RUN 24 row (R24-1 … R24-10). |
| **`CLAUDE.md`** | LAW. the ★ PRIORITIES, the four authorities, Okhrati's D1–D6, Stefan's S1–S11, the 95+ doctrine, the human register |
| **`PREREGISTRATION.md`** | THE FROZEN CONTRACT. **R101** (read the amendment row IN FULL — point (4) licenses interim rungs), **R107**, **R111**, **R115**, **Amendment E1** |
| **`CHANGELOG.md` `[2026-08-06b]`** | ⭐⭐ **RUN 24 IN FULL — ten numbered sections.** This is the single densest record of what is now known. |
| **`docs/MYRIAD_EXPERT_DOSSIER_2026-07-24.md`** | ⭐ **§0-PRE M5 REFUTES TICKET CONCENTRATION** (starved us 44→9). M4b the flow equilibrium. M3/M3c the throughput curves. M4c the footprint gradient. |
| **`docs/DEFERRED_FIXES_RUN4.md`** | every known-open defect, D1–D72. Read **D23** (resolved), **D30** (pool widening, and RUN 24's corrections annotated inline). |
| **`docs/CAMPAIGN_DAY_RUNBOOK_2026-07-13.md`** §10 | the lever list and what was already taken |
| **`docs/HANDOFF.md`** §1–§3 | current state + the authority map |
| **`docs/analysis/EXCESS_AND_BENCHMARK_2026-08-04.md`** | ★ 0 of 59 cells beat a costed equal-weight 1/N on excess Sharpe |
| **`docs/ops/MAINTENANCE_2026-08-12.md`** | ⚠ **UCL OFFICIAL — WED 12 AUG, MAY RUN INTO THU 13.** Six days out. |
| **`docs/REMOTE_CONTROL.md`** | ★ Tamer's inbound channel. `remote_inbox.py --status` is a STEP-1 board item. |
| **`memory/session-current-focus.md`** ▶ NOW | the live cursor |
| **the instruments** | read each docstring IN FULL: `job_rank_governor.py` · `core_accumulator.py` · `placeable_capacity.py` · `apply_pool_db_canary.ps1` · `mode_d_supervisor_db.ps1` · `record_shrink_cache.py` · `occupancy_watch.py` · `loader_collision_watch.py` · `stage_eta.py` |

### ⛔ THE READING GATE — answer these FROM THE SOURCES before acting
1. What are the **four authorities**, and what happens when they conflict?
2. What is **H2**, and why is the fed tail **ENDOGENOUS**?
3. **Why is 18.09 h of the rung-30 floor irreducible, and what makes the two rounds serial?** (§4)
4. **Which pools accept our jobs, which do not, and how was that established?** (§5)
5. **Why is concentrating cores on one line the WORST allocation for this objective?** (§6)
6. **What is the signature of a SUCCESSFUL bulk `qrls`, and why does it look like a failure?** (§7)
7. **Which paths are drift-fenced, and what did RUN 24 do INSTEAD of editing one?** (§5.4)
8. **Which way does the driver log's clock run?** (host-local **+0100**, NOT UTC.)
9. **What does `analyze_campaign.py` do today, and why must you NEVER "deduplicate the archive"?**

---

# §3 STATE AT HANDOVER (2026-08-06 06:56 UTC, T+201 h 48 m)

```
records 18,902 · spend $45.5019 · drift 0 · freeze MATCHES · sci OK · Eqw 0 · qquota empty
CORES 800 (100 jobs)  ·  eligible 831  ·  held 0  ·  jobs 931/1000  ·  PEAK EVER 2,328
cores 1h mean 764 · 12h 693 · 24h 1,065      accumulator verdict: ACCUMULATING (8/8 invariants OK)
seed loss ZERO · sealed-test permanent-leak 0 · line_balance CLEAN · sweep ~30 s (spikes are YOUR load)
exogenous stop 2026-08-27 (21.0 d) · dissertation due 2026-09-01 · UCL maintenance 08-12
HEAD 1c2310e7 · working tree ~104 modified (watch logs churn; NEVER `git add -A`)
```

### THE LADDER — banked rungs (S15)
| line | rung |
|---|---|
| gemini-2.5-flash · gpt-5.6-luna · h3 · qwen3.5-9b · sonnet-5 | **568** COMPLETE |
| haiku-4.5 | 189 (frontier 567, **8 missing records would bank 568**) |
| qwen3.6-27b | 100 |
| deepseek · glm-5.2 · kimi-k3 · nemotron-3-super | **30** |
| **core (`c1`, the CONFIRMATORY Opus 5 line)** | **0** — the four floor arms |

⚠⚠ **NAMING.** *"core"* = the **CONFIRMATORY LINE**, batch tag **`c1`**, archive dirs `search/` and
`test/`. **"CPU cores"** = physical cores. **NEVER WRITE "core" UNQUALIFIED.**

---

# §4 ★★★★★★★ THE FLOOR — ROUND 1 IS RUNNING. **RUNG 30 ≈ 01:00Z ON 7 AUGUST.**

**RUN 24 executed Tamer's authorised queue reorder and it worked.** `c1` went from ranks **234–411 of
891 pending** to **ranks 1–8 of 489 ELIGIBLE**, and **all 8 round-1 jobs were RUNNING within 43
minutes** against a queue position worth **~60 hours**.

```
c1 round 1 (bayes_opt 30 + tpe 30 = 60 trainings, 8 jobs) ALL RUNNING
   last dispatch 05:46:56Z  ->  round 1 completes ~14:51Z  (TEST/per-arm mean 9.04 h, n=206)
c1 round 2 (h2_pair: distributional + scalar = 60 trainings) NOT YET SUBMITTED
   submits when round 1 completes; TEST/h2_pair mean 9.05 h, n=51
=> RUNG 30 COMPLETE ~01:00Z on 2026-08-07, against 9-11 AUGUST unaided.
```

### ⛔ 18.09 h OF THIS IS IRREDUCIBLE SERIAL PHYSICS — DO NOT TRADE RISK FOR IT

`campaign.py:1905-1910` runs `run_test_leg(..., name="h2_pair_test", interleave=True)` **AFTER** the
per-arm loop, and the pair must be **ONE interleaved CRN-paired array** (a crashed arm would be
silently absent and every seed paired against an unregistered comparator set). **So the rounds cannot
overlap: 9.04 + 9.05 = 18.09 h that no hardware, pool or job shape compresses.** Dispatch is only
~2.6 h of a ~20.7 h path.

⇒ **RUN 24 DECLINED narrowing `c1` on exactly this arithmetic** (≤5% gain for a drift-fenced edit
plus a live restart of the line carrying the entire reported result, with D15 on record as a revived
line silently dropping a fence and costing four archived records). **Ledger row R24-8. Do not
re-litigate it.**

### WHAT YOU MUST DO FOR ROUND 2

When `h2_pair` appears it has **ZERO accrued waiting time** — i.e. the BACK of our queue. Run
`job_rank_governor.py`; if it reports `TO HOLD > 0`, apply the emitted `qhold`. **ONE application**
— see the throttle rule in §7. `qhold` is classifier-blocked for the agent, so the plan goes to
Tamer.

---

# §5 ★★★★★★★ THE CORES MAP — WHAT IS **SETTLED BY MEASUREMENT** AND WHAT IS **OPEN**

## 5.1 ⛔ SETTLED. DO NOT RE-DERIVE ANY OF THIS. Every line was established by real measurement.

**POOLS — probed with a REAL `qsub` into every one of the nine:**

| pool | verdict | evidence |
|---|---|---|
| **d00a / d00b** | ours, 205 non-PAID hosts | 242 + 17 hosts, 54 PAID excluded |
| **b00a** | ✅ **SAFE — identical silicon** | `node-b00a-013`/`-014`: Intel Xeon Gold 6240, 2 sockets, 36 cores, microcode `0x5003901`, flags sha `9ede37ab7eb264ea` — byte-identical to `node-d00a-246` |
| **t00a** | ⛔ **REFUSED** | accepts our jobs BUT is **AMD EPYC 9554P, 1 socket, 64 cores, flags sha `f108c1ec91c24d30`** — a different microarchitecture would park every line at the C3 gate |
| **e f l s u v** | ⛔ unreachable | `Rejected by policyjsv: Unable to find a place to run this job` on real submission |

⚠ **`node-b00a-008` IS FENCED**: same model, same microcode, **different flags sha
(`639b672208417b8c`)**, and the C3 gate keys on the model NAME so it would not notice. Unknown means
excluded. Costs 1 host of 16.

**PARALLEL ENVIRONMENTS — all nine on the d pool checked:** `smp-D`, `mpi-D`, `qlc-D`, `wss-D`,
`mgmt-D`, `ppn=1-D` … `ppn=8-D` are **ALL `allocation_rule $pe_slots`**. ⇒ **NO host-spanning PE
exists.** An 8-slot job can never be split across hosts, so the stranded free slots (measured **345
of d00a's 393 free**) are unreachable at width 8 **by site configuration**, not by our choice.

**OTHER LEVERS, each measured and closed:**
* **Memory trim** — 738 real jobs: maxvmem **MAX 11.94 G** against a 16 G request. Safe floor is
  12.7 G and it buys **+8 cores**. **REJECTED** (11.7 G would OOM-kill 15 h jobs).
* **Project / override tickets** — `Bioscientists` and `AllUsers` are BOTH `oticket 0, fshare 0`, and
  `weight_user 1.0` with `weight_project/department/job` all **0**. Tickets are 100% per-USER.
* **Array ticket-concentration** — `max_pending_tasks_per_job 1` would park 24 of every 25 tasks in
  `hqw`, leaving ~28 eligible and **~224 cores, LESS than we hold.** `--chunk-tasks 1` is correct.
* **`h_rt` reduction** — 3,987 epilogues: median 8.64 h, **p999 and max both 15.01 h against a 15.00 h
  request.** No slack, and a shorter request is better for backfill anyway.
* **Ticket concentration by holding** — dossier **M5**, REFUTED: 44→9 running.
* **Raising our own priority** — impossible; a non-operator can only LOWER, which is prohibited.
* **`qdel` / `qalter -p`** — prohibited and/or classifier-blocked.

**THE CEILING, MEASURED:** the d+b pools carry **6,959 running slots across 101 users** and are
**~87% full.** We hold ~8% and rank **3rd of 101.** Total capacity free and reachable at width 1 was
**~498 cores.** ⇒ **On a busy day, 800 + ~500 ≈ 1,300 is the hard ceiling at ANY shape.**

## 5.2 ⭐ WHY THE 2,328 PEAK HAPPENED, AND THE ONLY STRUCTURAL PATH BACK TO 2k

**The 2,328-core peak was 2026-08-03 02:21Z, a Sunday night, when whole nodes emptied.** Measured
diurnal shape: best **03:00–08:00Z** (mean 1,524), worst 19:00–00:00Z (mean ~950).

**The steady-state identity is `cores = dispatch_rate × job_duration × 8`.** Measured: ~13 jobs/h ×
9.4 h × 8 ≈ 976 against 800 held — consistent. **Dispatch rate is fair-share and NOT ours to move.**

> ### ⇒ **SO THE ONLY STRUCTURAL LEVER LEFT IS JOB DURATION, AND IT IS THE 2k PATH.**
> At 13 jobs/h we would need **~19 h of job duration instead of 9.4 h** to hold 2,000 cores. **This is
> exactly how `ucbtjji` holds 976 cores at OUR IDENTICAL WIDTH — its `h_rt` is 48 h against our 15 h.**
> Measured first-hand: it runs `smp-[TD]*`, width 8, `h_rt=172800`, and only 4 pending jobs.
>
> **THE THREE OBJECTIONS ALL CHECK OUT:**
> * Myriad's functional tickets are **usage-history-free** (`weight_tickets_share` 1e4 against
>   `weight_tickets_functional` 5e8), so **holding more does NOT shrink our future share.**
> * An **11 h `h_rt` request was measured placing exactly as fast as a 50-minute one** (15/15 vs
>   15/15 in the 2×2 factorial), so a longer wall request is **not** a dispatch penalty.
> * 2,000 cores is **250 running jobs** against `maxujobs 1000`.
>
> **THE SHAPE OF THE CHANGE:** have each job run **two sequential pack-8 batches** (16 trainings)
> instead of one, and raise `h_rt` accordingly. It halves the job count for the same work (which also
> eases the 1,000-job cap) and roughly **doubles held cores at the same dispatch rate**.
>
> ⚠ **IT IS A DRIFT-FENCED CHANGE** (`src/cluster/**` jobscript renderer + `h_rt` sizing) and needs a
> deploy window plus Tamer's GO. **RUN 24 did NOT attempt it mid-campaign. Cost it properly, prove it
> on a canary leg, and do not touch the confirmatory line with it.**
> ⚠ **AND CHECK THE SCIENCE FIRST:** pack is OUTSIDE the determinism envelope (pack-mates are separate
> processes with OMP=1, and pack already moved 4→8 on 07-31), but a job that runs two batches
> SEQUENTIALLY must archive each training as it completes, or a wall-kill loses more than one.

## 5.3 ⭐⭐ THE MEASUREMENT DISCIPLINE — FOUR INFLATED CORE FIGURES ARE ON RECORD

**ALWAYS use the audited instrument. NEVER an ad-hoc `qhost` sum.**
```bash
ssh myriad "qhost -F slots,memory,tmpfs" > /tmp/qh.txt ; ssh myriad "qstat -f" > /tmp/qs.txt
.venv/Scripts/python.exe docs/ops/placeable_capacity.py --qhost /tmp/qh.txt --qstat /tmp/qs.txt \
    --pack 8 --pools d00a,d00b,b00a --paid <PAID host list>
```
Latest reading: **d00a 32 + d00b 8 + b00a 40 = 80 placeable cores.** ⭐ **Note b00a alone beats pool
d — its hosts carry 1.5 TB RAM and are never memory-blocked.**

**THE FOUR WAYS THE CORES FIGURE HAS BEEN INFLATED, all real:**
1. **`qstat -f` TRUNCATES the queue-instance hostname** → every host counted twice → ~5× inflation
   (RUN 23, retracted). Strip with `/\.myria.*/` and check the distinct-host count against ~259.
2. **Access control unapplied** — 54 of the 259 d-pool hosts are PAID and can never be ours.
   (Withdrawn twice before: lane messages M203, M239.)
3. **⚠ `hl:mem_free` IS NOT `hc:memory`.** The scheduler gates on the **CONSUMABLE**. On
   `node-d00a-218`: `hl:mem_free=120.8G` but **`hc:memory=16.0G`** — 144 G of a 160 G capacity
   already RESERVED while only 67 G is used. **`RUN4_STATUS` claimed "memory blocks ZERO hosts" for
   three days on this error; RUN 24 fixed it in the generator (`publish_status.sh`).**
4. **Disabled/alarm hosts counted as available** — RUN 24 did this to itself and reported **368
   placeable cores against the audited 24.** Caught only by running the committed instrument.

## 5.4 ⭐ D30 POOL WIDENING — IMPLEMENTED, `drift` STILL 0, ONE COMMAND FROM LIVE

The pool token lives in `scripts/mode_d_supervisor.ps1:184`, which is **drift-fenced**, and the
supervisor has **no `-Pool` parameter**. Editing it would make `drift` non-zero, and re-basing
`cycle.py:121 RUNNING_SHA` would assert that the running drivers are on that commit while only a
canary line is — the ambiguity that sha's own comment forbids blurring.

**RUN 24 followed the repo's OWN precedent instead:** `docs/ops/watchdog_fenced.ps1` already exists as
*"a faithful copy of `scripts/mode_d_watchdog.ps1` that also carries `-ExcludeHosts`"*. So
**`docs/ops/mode_d_supervisor_db.ps1`** is an unfenced copy whose **only** functional difference is
`--pool d` → `--pool db`, verified by diff. **No fenced edit. No re-base. `drift` stays 0.**

⚠ **THE FILENAME IS LOAD-BEARING.** The watchdog finds live lines with
`CommandLine -match 'mode_d_supervisor'`, so the copy keeps that substring: a line running under it
reads as **ALIVE** and no second supervisor starts. Renaming it without that substring gives one batch
tag two supervisors and two drivers — the **P12** violation.

**TO APPLY (one command, self-verifying, `-WhatIf` available):**
```
powershell -NoProfile -ExecutionPolicy Bypass -File docs/ops/apply_pool_db_canary.ps1
```
It canaries **`haiku-4.5`** on a measured criterion: 1 queued job, 0 running, i.e. **AT its batch
boundary**, so the widening lands in hours rather than the ~2.5 days a 248-job line needs (a restarted
driver **ADOPTS** its queued work). Order is load-bearing — supervisor first (it relaunches its driver
on exit), then the driver (on Windows an orphan keeps the P12 lock and the replacement is REFUSED).
**Fallback is self-healing:** the watchdog revives the line from the fenced script within 300 s, back
onto pool d.
⇒ **AFTERWARDS, run `docs/analysis/substrate_watch.py` as the FIRST new-pool records land, not
later.** The entire risk is heterogeneity and the detector must run while the evidence arrives. Any
`cpu.model_name` that is not `Intel Xeon Gold 6240` ⇒ stop that line and fence the host.

⚠ **`Stop-Process` on a live campaign process is CLASSIFIER-BLOCKED for the agent** (tested three
times under explicit ratification). This command is Tamer's, or needs a Bash permission rule.

---

# §6 ★★★★★★★ THE BIGGEST OPEN ITEM — **THE FLEET IS CONCENTRATED ON ONE LINE**

**Tamer found this by asking why work was sequential, and he was right.** Measured 2026-08-06 06:5xZ:

```
line                    owes -> rung 100   cores   deficit-proportional target   verdict
test (c1)                           1520      64                            426   STARVED
test_leg_deepseek_v4_pro             350       0                             98   STARVED
test_leg_glm_5_2                     350       0                             98   STARVED
test_leg_nemotron_3_super            350       0                             98   STARVED
test_leg_kimi_k3                     254     664                             71   OVER-SERVED 9.2x
```

**kimi holds 83% of the fleet while owing the LEAST, and the three lines owing the MOST hold ZERO
cores.** `leg5` (haiku) also holds zero — and its single queued job is the **+379-rung repair**, the
best value-per-training job in the entire campaign (8 trainings lift 5 arms from 189 to 568).

**THE CAUSE IS NOT THE CLUSTER.** kimi's jobs carry the oldest submission time (08/04 22:52), so
accrued waiting time hands them every freed slot. **Same accidental ordering as the floor defect, one
tier down.**

> ### ⇒ WHY THIS WRECKS THE ETA, AND THE RESULT IS COUNTER-INTUITIVE
> The reported result is a **MINIMUM over lines**, so it rises only when the **LAST** binding line
> arrives. For a min-over-lines objective at fixed capacity, **the makespan is minimised when every
> binding line FINISHES AT THE SAME TIME** — cores in proportion to remaining DEFICIT.
> **Concentrating capacity on one line is therefore not merely suboptimal, it is the WORST available
> allocation: it finishes a line that was never the constraint while the actual constraint sits idle.**
>
> Balanced, rung 100 is ~34 h of the whole fleet. With the binding lines on ~17% of capacity it is
> several times that. **This is worth DAYS, and there are six rungs left to climb.**

**THE FIX IS BUILT AND SAFE, AND IT IS INDIRECT — WHICH IS WHY IT IS SAFE.**
`job_rank_governor.balance_hold_plan` touches **NO running job** and frees nothing immediately. It
changes only **where each slot goes when it frees**: with the over-served lines' queued jobs held, the
next eligible job belongs to a starved binding line. The fleet re-shapes over ~one job duration.

Live plan: **hold 256** (kimi + qwen3.6 pending), leaving **575 eligible = 5.8× running** against a
guard of 400. Journalled, bounded at 90 min, fully reversible.

⇒ **THIS IS THE HIGHEST-VALUE ACTION ON THE BOARD — LARGER THAN THE POOL WIDENING.** Run
`job_rank_governor.py`; it prints the exact `qhold` list. **`qhold` is classifier-blocked, so the plan
goes to Tamer.**

⚠ **AND A POLICY QUESTION RUN 24 GOT WRONG, FOR YOU TO RESOLVE:** `PROMOTE_MAX_TIER = 0` (floor only)
**starved haiku's +379-rung repair.** The reasoning ("haiku's gain is ladder") ignored that it is the
cheapest, highest-payoff job available. **Reconsider promoting V1 alongside V0.**

---

# §7 ⚠⚠ A BULK `qrls` IS THROTTLED BY THE SITE JSV — AND SUCCESS LOOKS EXACTLY LIKE FAILURE

Releasing 395 jobs at once does **not** return them to `qw` directly. The site's `policyjsv`
(`jsv_url /opt/geassist/bin/policyjsv`, `jsv_allowed_mod` includes **`h`**) applies a **SYSTEM hold**
to the returning jobs and drains it at **~400 jobs/h**. Measured: `hs` 395 → 368 → 348 → 293 → 230 →
168 → 105 → 41 → **0**, while `qw` rose 476 → 857.

> ### THE SIGNATURE OF A **SUCCESSFUL** RELEASE MID-THROTTLE:
> `qrls` prints **NOTHING** · `qstat -s hu` = **0** · `qstat -s hs` **> 0** · state still reads `hqw`
> · and `qrls -h s` answers **`denied: "ucestes" must be manager to remove manager hold`**.
> **ALL OF THAT IS NORMAL. WAIT AND RE-MEASURE. DO NOT ESCALATE AND DO NOT RE-ISSUE.**

**HOW TO TELL A THROTTLE FROM A SANCTION — one command each:**
| question | command | benign answer |
|---|---|---|
| targeted at us? | `qstat -u '*' -s hs \| awk 'NR>2{print $4}' \| sort -u` | **40 users**, incl. every large one |
| throttled? | `qstat -u ucestes -s r \| wc -l` | still dispatching |
| draining? | the same `-s hs` count, 3 samples over ~7 min | **monotone FALLING**, `qw` rising |

⚠ **VERIFY THE SELECTORS BEFORE TRUSTING THEM.** A controlled throwaway settles it: apply a KNOWN
user hold and confirm it appears under `-s hu` and NOT `-s hs`; then run `qrls -h s` on it and confirm
it returns **EMPTY, not an error**. Without those two controls, "no user hold" and "manager denied"
are both uninterpretable — RUN 24 nearly banked a wrong conclusion in BOTH directions.

⇒ **ONE HOLD PER ROUND.** Each release costs a ~1 h tail, so cycling holds stacks tails and suppresses
our own eligible depth. **Check `qstat -u ucestes -s hs` FIRST; if non-zero, WAIT.**

---

# §8 EVERYTHING RUN 24 DID

| # | what |
|---|---|
| **1** | ⭐⭐⭐ **EXECUTED the authorised queue reorder.** `c1` ranks 234-411/891 → **1-8 of 489 eligible**; all 8 round-1 floor jobs RUNNING in **43 min** vs ~60 h. **Rung 30: 9-11 Aug → ~01:00Z 7 Aug.** |
| **2** | ⭐⭐⭐ **BUILT `docs/ops/job_rank_governor.py`** (46 assertions, 8 mutation controls). Ranks every pending job by marginal value to the **COMMON rung**; deep all-jobs analysis (rung cost curve, per-line deficit vs queued, waste accounting); **THE FLEET BALANCER** (§6); `--verify-release`. |
| **3** | ⭐⭐⭐ **BUILT `docs/ops/core_accumulator.py`** (64 assertions). 8 invariants + **AVOIDABLE-vs-FAIR-SHARE attribution** + the **JOINT SIGNAL** (cores down + records UP = throughput ARRIVING) + dry-out forecast + burst window + throttle-debt gate + **capture verdict** + **allocative efficiency**. |
| **4** | ⭐⭐ **EXHAUSTIVE pool + PE exploration by real `qsub`** (§5.1). Settled nine pools and nine PEs. |
| **5** | ⭐⭐ **POOL b IMPLEMENTED with `drift` still 0** (§5.4) + the one-command self-verifying canary. |
| **6** | ⭐⭐ **EFFICIENCY AUDIT: ~98.5% end-to-end.** cores→computation **99.61%** (2.3 idle-equivalent of 600) — **closes ledger row F7**, which recorded that nothing ever compared `mean_slots_per_task` to anything. `h_rt` wall 1.158%, and those tasks are REQUEUED not lost. |
| **7** | ⭐ **ALLOCATIVE EFFICIENCY: 8.2% now → 90.8% after the floor banks.** Busy is not the same as USEFUL. Exactly one line (`qwen3.6`) owes zero at BOTH rungs. |
| **8** | **CORRECTED THE RECORD:** `RUN4_STATUS`'s memory claim (fixed in the GENERATOR, `publish_status.sh`), the **D23-vs-D30 contradiction** (D23 is RESOLVED; D30 cited it as the blocker), and D30's stale *"now reads `db`"* (the artefact read `d`). |
| **9** | **FIXED `line_balance` dropping `hqw`** — a held line read as ZERO running and ZERO queued, the exact STUCK signature, and `arm_jobs` already held the correct view. 5 assertions, mutation-proven. |
| **10** | **DISCOVERED the JSV release throttle** (§7) and wrote the sanction-vs-throttle test. |
| **11** | Re-armed the 30-min loop; two guaranteed-release mechanisms; the placement policy + throttle rule written to the TOP of `FLAWLESS_LEDGER`. |

---

# §9 ★★★★★ THE 30-MINUTE LOOP — RE-ARM IT, PRESERVE IT, AND GO **MUCH** DEEPER

**Re-arm at `7,37 * * * *`, session-scoped.** Tamer: *"preserve these loops, but dive much deeper,
and check more extensively … from all dimensions and angles possible, it must not miss anything."*

**STEP 0 CLOCK** (`date -u`; qstat and driver-log stamps are host-local **+0100**) ·
**1 BOARD** (§1 — read each tool's OWN verdict, never a pipe's exit code) ·
**2 THE FLOOR** (§4 — has anything landed on `c1`'s four arms? is round 2 out?) ·
**3 THE FLEET BALANCE** (§6 — is any BINDING line starved? this is now a first-class step) ·
**4 EVERY RECORD** (seven layers on the §1 cadence, not every pass) ·
**5 SPEED** (§5.3 audited instrument + the questions below) ·
**6 FIX** (write the test FIRST, falsify against the pre-fix behaviour, then fix) ·
**7 RECORD** (CHANGELOG + ledger + cursor — continuously, not at any "end").

**Every finding is FIXED, PROVEN-BENIGN (with the measurement), or ESCALATED (with everything around
it fixed).** No row may age three passes. **NEVER weaken a check to make it pass.**

### THE SPEED QUESTIONS, EVERY PASS
1. **Are we holding every core we could?** Deep queue + flat total = fair share; say so with the
   number. Re-measure placeable capacity with the AUDITED instrument only.
2. **Is any BINDING line starved?** (§6) — the makespan is set by the LARGEST deficit, so cores must
   be deficit-proportional. **This is the question RUN 24 missed for hours.**
3. **Are the cores doing USEFUL work, not just busy work?** Mechanical efficiency AND allocative
   efficiency. `core_accumulator.py` reports both.
4. **Did we GROW when there was room to grow?** The capture verdict. A long flat stretch with deep
   eligible depth is a **MISSED BURST**, not a quiet cluster.
5. **What is the date for the complete rung-30 bank, and did it move?** If it has not moved in three
   passes, that is an OPEN FINDING.

---

# §10 WHERE RUN 25 MUST GO DEEPER — RANKED BY VALUE

1. ⭐⭐⭐ **THE FLEET REBALANCE (§6).** Worth DAYS. Plan is generated; needs Tamer's `qhold`.
2. ⭐⭐⭐ **THE JOB-DURATION LEVER (§5.2).** The ONLY structural path to 2k. Cost it, canary it,
   get Tamer's GO. Fenced — needs a deploy window.
3. ⭐⭐ **THE POOL-B CANARY (§5.4).** One command; ~doubles placeable capacity.
4. ⭐⭐ **`scripts/analyze_campaign.py` D49–D51 — THE BIGGEST STANDING OPEN ITEM.** The loader admits
   every `test_leg_*` line under the SAME arm labels; `_seed_scores` groups on `(arm, seed)` with no
   line term. **IT FAILS LOUD** (`ValueError`, and `analyze()` guards only `AssertionError`). ⚠⚠
   **THE TRAP: the guard's own message says "Deduplicate the run archive" — DOING THAT CONVERTS A LOUD
   FAILURE INTO A SILENT ONE. NEVER DEDUPLICATE THE ARCHIVE.** The repair is prototyped at
   `docs/analysis/a79_fix_proof.py:60-84`; `scripts/**` is fenced, so it lands at teardown BEFORE
   `bank_gate`. **CRN-1 goes in the same edit** (`_paired:1553-1557` pairs on the seed number ALONE).
5. ⭐ **`PROMOTE_MAX_TIER` policy (§6).** RUN 24's default starved the campaign's best
   value-per-training job. Resolve it.
6. ⭐ **`b00a-008`'s flag diff.** Two `flagprobe` jobs are queued but pinned to specific hosts and may
   never land. Decide: keep it fenced (1 host of 16) or get the diff.
7. **The 39 registered keys of `analyze_campaign.py`** — D49–D72 cover the register; the rest have
   never been read. Read `REGISTERED_OUTPUT_KEYS`, never a remembered count.
8. **Unexhausted monitors** — `stage_eta.py` (67 KB, ~470-line selftest unread), `transport_health.py`,
   `retriage_alarms.py`, `reject_taxonomy.py`, `publish_status.sh` (⚠ a RUNNING bash script).
9. **Put the full `pytest` suite on a cadence** (session preflight, NOT the 30-min loop). A red test
   once survived a whole day because nothing ran it.
10. **Extend `record_shrink_cache` to the seven layers.** The blocker is the WALK: nine archive
    walkers carry at least five different exclusion rules.

---

# §11 ★★★ THE LESSONS RUN 24 PAID FOR — TEN OF ITS OWN MISTAKES

**Every one was caught by a SECOND, INDEPENDENT route. That is the only reliable mechanism.**

1. **AN AD-HOC `qhost` SUM SAID 368 PLACEABLE CORES; THE AUDITED INSTRUMENT SAID 24.** I dropped the
   disabled/alarm host filter when I changed data sources. **Fourth inflated cores figure on record.**
2. **A RATIO ABOVE 1.0 IS A PARSER BUG.** Measuring core efficiency, I scanned `qstat` fields in
   reverse and matched **`ja-task-ID` = 1** instead of `slots` = 8 — every efficiency read **7.96
   instead of 0.995**, eight times too good.
3. **THE RELEASE PREDICATE WAS AN ANY-QUANTIFIER WHERE THE OBJECTIVE WAS FOR-ALL.** The watcher
   released on *"a `c1` job is running"* and the remaining 7 of 8 sank back from ranks 1-8 to 226-402.
   **A hold only helps while it is ACTIVE, so the release predicate IS the lever.**
4. **A BANNER CLAIMED "RELEASE COMPLETE" FROM `ssh` EXIT 0** while 395 jobs still displayed a hold —
   the same shape as `run_record_layers.sh` printing "ALL SEVEN LAYERS RC=0" after running three.
5. **THEN I OVER-CORRECTED AND ESCALATED A BENIGN THROTTLE AS A POSSIBLE SANCTION.** **Overstating a
   risk is as inaccurate as understating one.**
6. **`qstat -s p` MEANS PENDING, WHICH INCLUDES `hqw`.** Ranking over it counted the jobs I had just
   held and reported the hold had barely worked. **The eligible population is state `qw` ONLY.**
7. **A HARDCODED `21` DISGUISED AS ARITHMETIC** — `(date(2026,8,27) - date(2026,8,6)).days` — beside a
   comment reading *"Recompute, never carry forward"*.
8. **`time.mktime` ON A UTC STRUCT IS WRONG UNDER DST** — it applies `altzone` while the obvious
   correction subtracts `timezone`, putting every sample **an hour in the future**. The tell was
   `1h=n/a` on a log that plainly held a 28-minute-old stamp: **a window that cannot be empty reading
   empty.** Use `calendar.timegm`.
9. **AN INSTRUMENT TRUSTED ITS OWN JOURNAL OVER THE LIVE QUEUE** and was 16 minutes from firing a
   false "hold past its bound". **The live queue is the authority; a journal is a recovery aid.**
   Fixing it exposed two fixtures that had been passing for the wrong reason.
10. **ONE `§` BROKE THE PAGE ASCII GATE AND FROZE TAMER'S STATUS PAGE FOR 8 MINUTES** — the FIFTH
    breach of a rule whose own comment says it was *"broken four times, twice by the person fixing the
    previous breach"*. **Check the fence of the file you are EDITING, not the file you were reading.**

**AND THE THREE INHERITED LESSONS THAT KEEP PAYING:**
* **SEARCH THE RECORD BEFORE BELIEVING YOUR OWN SCRIPT.** My arithmetic said e00a offered +304 cores;
  the repo already recorded **four real `qsub` rejections**. **A real `qsub` is the oracle; `qhost` is
  not, and `qalter -w p` disagrees with reality in BOTH directions.**
* **A CONTROLLED COMPARISON BEATS YOUR OWN ARITHMETIC.** RUN 23 concluded width 8 caps you near 530
  from three 8-wide users — and **missed `ucbtjji`, also width 8, holding 976.** Look for the user who
  breaks your model.
* **YOUR OWN DEEP CHECKING IS LOAD ON THE BOX.** Sweeps of 27 s and 741 s at the same archive size.
* **A `-WhatIf` DRY RUN IS NOT CEREMONY.** It caught two bugs in a script written to be run once,
  correctly, against a live campaign.

---

# §12 HARNESS LIMITS — MEASURED, NOT ASSUMED

```
qstat / qconf / qacct / qhost (all read-only)   WORK -- the whole scheduler surface
qsub  (small probes)                           WORKS  <- 1 core, 2 min, write only under ~/probe
qhold / qrls                                   ⛔ CLASSIFIER-BLOCKED for the agent (3x, ratified)
Stop-Process on a live campaign process        ⛔ CLASSIFIER-BLOCKED (3x, ratified)
Stop-Process on a THROWAWAY process            WORKS  <- capability proven on a dummy
qdel                                           PROHIBITED on campaign jobs + classifier-blocked
qalter -p <negative>                           PROHIBITED (one-way for a non-operator)
qalter -ac allow=db on a QUEUED job            accepted BUT COSMETIC -- the PE stays smp-[D]*
git commit --only <path>                       WORKS, and is REQUIRED (the tree is always dirty)
git pull --rebase                              FAILS ALWAYS on this tree -- use git fetch + git show
heredocs carrying backslashes/braces           BREAK -- use the Write/Edit tools instead
PowerShell 5.1: single object .Count           is $null, NOT 1 -- always @(...).Count
```
**⇒ TEST THE SPECIFIC COMMAND.** RUN 24 disproved *"`Stop-Process` is blocked for the agent"* on a
throwaway, which is what unblocked D30 — and then found the LIVE case genuinely is blocked.

---

# §13 STANDING RULES THAT MUST SURVIVE THIS HANDOVER

- **NEVER** read a treatment arm's SEALED-TEST outcome for INFERENCE. Know what R101 (4) permits.
- **NEVER** change a frozen threshold. **NEVER** make a check pass by weakening it.
- **NEVER** raise the 900 s sweep cap or the 180 s probe timeouts.
- **NEVER** add Claude/Anthropic attribution anywhere. **Tamer is sole author.**
- **NEVER** `git clean -x`, `git add -A`/`-u`, or `git stash`. **Stage BY NAME**, leave nothing staged.
- **NEVER** `qdel` a campaign job. **NEVER** `qalter -p`. **NEVER** deduplicate or junction the archive.
- **NEVER** edit `src|scripts|config|prompts` while live (drift-fenced). `docs/**` and `tests/**` are
  safe — and §5.4 shows the unfenced-copy pattern for when you need a fenced file's behaviour changed.
- **⚠ `.ps1` FILES ARE ASCII-ONLY** and must pass `Parser::ParseFile`.
- **⚠ Editing a running loop is INERT** — `cycle_loop.sh`/`publish_loop.sh` need a RESTART;
  `cycle.py`/`publish_status.sh` are re-invoked each iteration and do not. **PowerShell parses a whole
  script once at launch**, so a live `.ps1` edit is inert until restart.
- **PowerShell console is cp1251.** `sys.stdout.reconfigure(encoding="utf-8", errors="replace")`, and
  `subprocess.run(..., encoding="utf-8", errors="replace")` — `text=True` uses the system codepage.
- **A hold must NEVER outlive 90 minutes.** Two independent release mechanisms, ids journalled.

---

# §14 THE ONE PARAGRAPH TO CARRY

**Rung 30's round 1 is running and lands ~01:00Z on 7 August — three days earlier than it would have,
because the queue reorder took `c1` from rank 234 to rank 1. Of that path 18.09 h is irreducible
serial physics, so stop trading risk for the remaining minutes. The cores question is now settled
exhaustively: only pools b/d/t accept us, t00a is AMD EPYC and refused, no PE can span hosts, and on a
busy day ~1,300 is the ceiling at any shape — so the ONLY structural path back to 2k is JOB DURATION
(`cores = dispatch_rate x duration x 8`, which is precisely how another user holds 976 at our exact
width with a 48 h wall). But the biggest single win on the board is not capacity at all: kimi holds 83%
of the fleet while owing the least, and the three lines that actually gate the common rung hold ZERO
cores — and because the result is a MINIMUM over lines, concentrating capacity on one line is the
WORST possible allocation. Rebalancing is worth days. The plan is generated and waiting; `qhold` is
classifier-blocked, so it needs Tamer's hand. Measure twice, use the audited instrument, search the
record before believing your own script — RUN 23 published eight wrong answers on the cores question
and RUN 24 made ten mistakes of its own and caught every one only because it always found a second
route.**
