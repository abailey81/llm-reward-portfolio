# 100-Loop Deep Adversarial Sweep — 2026-07-04

100 distinct deep probes across 10 domains, executed by 10 parallel read-only adversarial auditors (each
told to FIND the weakest point, report file:line, and mark clean only what is genuinely clean). The parent
(me) verified every load-bearing finding first-hand, applied only isolated safe fixes, and recorded the rest.
**Guardrails (held):** no frozen-experiment change, no forking-paths reaction to σ_D, no scope creep, no
fabrication. Companion to the earlier `SELF_IMPROVEMENT_SWEEP_2026-07-04.md` (20-loop); this one went deeper
and DID surface real issues.

## HEADLINE VERDICT
**The confirmatory CORE is rock-solid — verified first-hand, not inferred — and the real findings cluster in
the PROSE and the report-only diagnostics, plus two clean code-hardening gaps. Nothing found invalidates the
experiment or the banked null.**
- **Statistics (Domain A):** literature-exact. No CRITICAL/MAJOR code-correctness defect. BH family +
  drift-guard, three-way TOST (structurally cannot false-positive "equivalent"), Berger IUT, paired-seed
  rliable bootstrap (naive seed-averaging explicitly forbidden, ~21% FP documented), DSR/PBO on full
  C(16,8)=12,870 enumeration, DM/FZ0 with HLN + Newey-West, Bayes JZS (Lindley-guarded)/MCS/TSED honestly
  report-only. σ_D math exact (0.244·√(2·1.141)=0.369).
- **Data (Domain D):** every load-bearing number REPRODUCED against the on-disk univ5 parquet. 333/333 dead
  terminals recovered (0 fabricated), 0 changed cells vs univ3, univ4's 28 surcharge cells → 0 in univ5,
  purge 60/60 both boundaries, COVID crash provably in the purge (test opens 2020-03-30), no ticker/date
  reaches a reward, 5/5 checksums match, state features `.shift(1)` leakage-closed. **NO leakage.**
- **Identification (C), Theory core (B), Honesty/Ethics (I):** sound. The 06-30 theory proof-core fixes
  (M1/M2/M3/C2/C3/M4/M7) ALL hold; ethics/AI-disclosure/pivot/H1-snooping/superlative-hedging genuinely clean;
  tail-neutral base prompts verified first-hand (no tail/CVaR/drawdown vocabulary).
- **Citations (F):** unusually clean — zero dangling, zero misattribution, high-stakes years/authors all exact.

## CROSS-CUTTING THEMES (the real signal)
1. **Prose ran slightly ahead of code on REPORT-ONLY diagnostics** — three "manuscript claims an analysis/
   check that does not exist": mediation *sensitivity analysis* (C-F1), FZ0 *tail-index check* (A-F1), and the
   novelty *draft-artifact* ELfolio drift (G-F1). All fabrication-class, all confined to report-only/queued
   material (never the confirmatory core), all safe prose softenings.
2. **A hash-bound number drifted in the paper prose:** B\*=200k in every config + PREREGISTRATION.md, but the
   paper chapters still say 50,000 training steps (J-F1) — and the B.2.1 "undertraining" limitation is now
   *inverted* (the design deliberately chose a CONVERGED budget). Biggest single manuscript-consistency defect.
3. **Precision over-claims a probabilist/reviewer would catch:** "tail-blind" for a DSR selector that is not
   tail-blind (B-F1); a Null-branch table logic error (B-F2); "factorial dissection" for a report-only
   ablation (G-F2). All safe prose fixes.
4. **The deepest new objection (J-F3, Loop 99):** the confirmatory H2 replicates *training seeds* (n=30) but
   NOT the *authoring* step (n=1 winning program per arm) — so the H2 CI technically generalizes to those two
   programs, not to the feedback condition (pseudoreplication / unit-of-analysis). This does NOT invalidate the
   study, and it *reinforces the mechanism-as-headline reframe* (the across-candidate mechanism kernel is the
   channel-level evidence that samples authoring). Must be disclosed as a limitation.

## FINDINGS BY PRIORITY

### P0 — land before freeze (correctness / honesty / consistency; all safe)
| id | domain | sev | status | defect → fix | disposition |
|---|---|---|---|---|---|
| J-F1 | edge/craft | MAJOR | CONFIRMED (me) | Paper says B\*=50,000 training steps (CH4:73,97; CH6:33,133; CH7:140 B.2.1; MANIFEST:22) but frozen B\*=200,000; B.2.1 undertraining claim inverted. FIX: 50k→200k at budget sites (keep buffer=50k at CH4:77,81), rewrite B.2.1 to the convergence-pilot (≥2× critic knee, below overfit onset). Configs already correct — do NOT touch hash-bound files. | **OVERNIGHT (multi-site careful pass)** |
| B-F1 | theory | MAJOR | CONFIRMED | "tail-blind (λ=0)" overstated — DSR selector penalizes neg-skew/excess-kurtosis (§3.4 concedes "not perfectly tail-blind"); 6 sites (theory:146,233; CH4:167,246; CH7:131; NOMENCLATURE:14; 00_FRAMING:42). FIX: "tail-blind up to the DSR's 2nd-order skew/kurtosis correction (§3.4)" consistently. | **OVERNIGHT** |
| B-F2 | theory | MAJOR | CONFIRMED (me) | Table 3.1 Null row "λ=0 **or** non-responsive" → should be **and** (matches prose :268-273 + the row's responsiveness≤0 cell). | ✅ **FIXED** (theory:266 or→and) |
| I-F1 | honesty | MAJOR | CONFIRMED (me) | CH1:137 grade-modality leak "graded…without a viva…shapes how the result is reported." | ✅ **FIXED** (clause excised, substance kept) |
| C-F1 | ident | MAJOR | CONFIRMED (me) | CH7:60-62 claims a mediation sensitivity analysis that doesn't exist (mediation.py = a·b + bootstrap CI only; prereg:224-226 = descriptive only). FIX: soften to "descriptive decomposition; formal ρ/E-value sensitivity is future work." Also fixes C-F2 (responsiveness=path a ≠ indirect a·b; suppression needs opposite-sign) in the same paragraph. | **OVERNIGHT** |
| A-F1 | stats | MAJOR | CONFIRMED | CH7:215 claims "the tail-index of the FZ0 loss differential is checked" — no Hill/tail-index code exists; size calibration uses Gaussian innovations. FIX: soften to "should be checked / we report the differential's tail behaviour"; mark `heavytailsDM2026` %VERIFY. | **OVERNIGHT** |
| E-F1 | repro | MAJOR | CONFIRMED | freeze.py has NO assert_seeds_match / assert_matched_budget_match — executed campaign.yaml seeds(:8)/candidates(:4) not bound to frozen prereg; post-freeze silent seed drift passes GREEN. Timely for the 30→350 change. FIX: add both asserts mirroring assert_train_steps_match + hermetic test. Verify current campaign.yaml==prereg first so gate stays green. | **OVERNIGHT (code+test)** |
| J-F3 | edge | MAJOR | PLAUSIBLE | Pseudoreplication: confirmatory H2 = n=1 authored program/arm, replicated over training seeds only → CI generalizes to the programs, not the channel. Not pre-empted (andrews2024winners covers winner's-curse bias, not generalization target). FIX: add a CH7 limitation scoping the H2 verdict to the selected programs; foreground the across-candidate mechanism analysis as the channel-level evidence (reinforces the reframe). | **OVERNIGHT (limitation add)** |
| I-F1b/G-F1 | novelty | MAJOR | CONFIRMED (me) | Queued draft artifacts (RELATED_WORK_WATCH.md "#7", ADD-4 field-map) claim ELfolio "not reward" — contradicts CH2:100-104 + refs.bib:124 (ELfolio RL-path rewrites reward fns). FIX docs: correct both; pin claim to "first to ISOLATE reward-code authorship as the manipulated variable under pre-registration." | **OVERNIGHT (docs)** |

### P1 — high-value write-time (mostly Okhrati-graded mechanics; overnight backlog)
| id | domain | defect → fix |
|---|---|---|
| H-F1 | craft | Body = 16,001 words vs 10,000 hard limit (CH3 3553/CH4 3357 worst; CH1-4=11,922). P7 surgery: push formal apparatus into display-math (word-excluded), cut CH1/2/3. **CRITICAL grade risk.** [conservative overnight moves + record deeper restructure] |
| H-F3 | craft | Motivating tail EDA absent from body (§4.2). Add short EDA passage citing F3 + 2-3 tail facts (kurtosis 15.25, CVaR crossover, 19.7% co-crash) — fig+caption word-excluded, ~0 budget. Fixes F3 orphan + Okhrati priority-4. **HIGH value.** |
| H-F4 | craft | No wall-clock/compute in prose. Add device (RTX 4050), 85 min/candidate, 3 workers, prototype 17.9h to CH5 §5.2 + a CH4 compute line. Known numbers. |
| H-F2 | craft | 6/15 exhibits orphaned (F1-F4 never cited; T5/T6 unreferenced); two numbering schemes (F1-9/T1-6 vs chapter.n). Unify numbering + add a citing sentence for each in cited order. |
| H-F7 | craft | CH7 grades SQ1-3 but not H3/H4; add a compact RQ/hypothesis scorecard table (asked/answered/verdict), placeholders ok. |
| H-F5 | craft | Prototype null pre-disclosed near-verbatim CH1:144/CH3:269/CH7:26. Foreshadow-once CH1, detail CH5, reference CH3, deliver CH7 (also helps budget). |
| H-F6 | craft | Front-matter order: Abstract after Acknowledgements (should precede); signpost Theory/Prototype chapters; Cover Page = Moodle (USER). |
| I-F2 | honesty | σ_seed-dominance-as-a-finding (ADD-1): name σ_D as the MDE source §4.7; add B.5.1 RL-fragility sentence; add a σ_seed exhibit slot to CH6. **Results-time.** |
| B-F3 | theory | Kusuoka sentence (theory:168) imports loss-convention "supremum over CVaR mixtures" against the box's return convention → should be infimum (or tag ℓ=−Z); add ξ∈U_α to the box (:158). |
| B-F4 | theory | §3.4 caveat omits the endogeneity of the fed law (P_θ generated by the policy trained under the reward). Add one clause, matching CH1:103/00_FRAMING:39. |
| G-F2/F6 | novelty | CH1:130 "first factorial dissection" → "manipulates content (arms) and, in a report-only ablation, encoding" (verify vs prereg:130-149). CH1:75 "tail-distributional"→"multi-level CVaR/tail-quantile". CH1:56/CH2:52 "concurrent"→"recent/prior" (Gallego Mar2026 < freeze). |
| J-F5/B-F6/craft | theory/craft | Density: one intuition sentence opening §3.8 (theory:283) + de-densify f-div/DPI :125, elicitability :174, caveat-mega :136. Theory:129 "LR is σ(g)-measurable" → "has a σ(g)-measurable version". |

### P2 — minor / precision / cleanup (overnight, low-risk)
- **A-F2:** power_analysis.py:511 uses p_two/2 for the one-sided decision; use `pvalue_one_sided_greater` (headline rule) + fix docstring. Negligible numerically.
- **A-F6:** attach a CI wherever ρ=−0.141 is reported; frame CRN as "≤0, no reliable benefit," never "negative" (n=15, SE≈0.29). [= ADD-3]
- **B-F5:** Cor 3.3 constant — with ½‖·‖_TV and ‖L‖∞≤1 the sharp bound is 2δ, not δ (state "2δ" or normalize loss to [0,1]).
- **D-F1:** loaders.py manifest check matches only via basename fallback (fwd-slash vs backslash relpath, no break-on-match). Normalize separators + return on first exact match. Latent, not a live leak.
- **D-F2:** "COVID" shorthand (prereg:406, inference.yaml:10) can imply crash in-test; tighten to "post-COVID-crash recovery."
- **F-F2:** check_citations.py:28 regex blind to bare-year keys + uppercase (misses fisslerziegelgneiting2015, heavytailsDM2026). Align with build_paper.py `_KEY_RE`.
- **F-F3:** consolidate 3 duplicate bib-key pairs (bellemare2017c51≡…distributional; mcneilfrey2000≡mcneil2000estimation; sood2023drl≡…deep).
- **F-F1:** reconcile the RELATED_WORK_WATCH.md kvasiuk2026madevolve DO-NOT-CITE (143,170,190) vs the 07-02 upgrade (224-227); full re-fetch of all future-horizon 2026 preprints at the mandatory pre-submission sweep.
- **C-F3:** schema.py:21 "plausible-looking numeric table" → "format-matched, coherence-destroyed" (derangement breaks CVaR monotonicity).
- **E-F5:** pyproject.toml:17,29 stale "RTX 4090/rented" comments → laptop RTX 4050; optional lockfile for the campaign env.
- **E-F2:** test_test_leg_equivalence.py:80 asserts approx(1e-6) but pyproject calls it "byte-identical" — tighten to `==` or soften wording.
- **E-F3:** default `np.random.default_rng(0)` in bootstrap.py/es_backtest/overfitting/reporting (headline threads rng(0), but future direct callers non-reproducible).
- **J-F2:** PopArt `raw*raw` (popart.py:209) has no overflow/finite guard — a finite |raw|≥~1.34e154 → σ=inf forever → critic learns on zeroed reward silently (violates fail-loud). Guard the EMA (skip/clamp non-finite) + `isfinite` fallback in `_scale()` + test. Severity below CRITICAL (extreme trigger; poor-scoring candidate loses selection). [code+test]
- **J-F4:** a 100%-safe-default candidate is counted/warned but not dropped; consider a loud drop.
- **F-F5:** add Okhrati & Garrido 2018 (Risks, coherent risk) to the coherent-risk cluster (absent; examiner-pleasing).

### P3 — record / user-decision (no autonomous change)
- **A-F3 (IMPORTANT, tie to seed decision):** the chi-square UPPER-CI seed-sizing (~350) is NOT implemented — sigma_seed_pilot sizes off the POINT σ_seed over a (30..50) grid and floors ρ<0; no code produces 279/340/403. Not a threat (point estimate is the *generous* bound; upper-CI only strengthens INCONCLUSIVE). Before freeze: implement the chi-square upper-CL sizing OR state that point-estimate sizing is the anti-achievability bound. **Surface at the seed-ratification step.**
- **A-F5:** two co-primary IUTs, no across-pair disjunction correction (FWER≈0.0975). State H2-Tail = THE confirmatory primary in the prereg, or add Bonferroni-over-2. Pre-registration precision (USER).
- **A-F4:** all bootstrap CIs (incl. TOST) are percentile not BCa; note the coverage caveat for the skewed CVaR legs, or add a BCa robustness variant.
- **I-F4:** PREREGISTRATION.md:103,325,587 grade/viva phrasing — scrub ONLY if any prereg excerpt is surfaced to the examiner (it is internal, not in the ToC).
- **I-F5:** disclose the prototype gen-4 candidate saturation qualitatively in CH5 §5.4 (policy tension: "no prototype NUMBER" — qualitative dynamic OK?).
- **E-F4:** schema.build_block + loop._REFLECTION_PREAMBLE are outside the content hash (git-SHA pinned only) — disclosed design choice; on the record.
- **H-F8:** build the F1 system/architecture diagram (engine work) + cite at §4.1 head.

## PER-DOMAIN ROLL-UP
- A stats: 1 MAJOR (integrity, not code) + 5 minor; 10/10 core probes CLEAN. Core literature-exact.
- B theory: 3 MAJOR (all prose/consistency, proof-core intact) + 3 minor; M1/M2/M3/C2/C3/M4/M7 fixes hold.
- C ident: 1 MAJOR (prose over-claim) + 2 minor; 8 probes CLEAN incl. tail-neutral prompts, off-critic estimator, three-way decoupling.
- D data: 0 material; 3 informational; 10/10 CLEAN, reproduced vs on-disk univ5. No leakage.
- E repro: 1 MAJOR (freeze-guard gap) + 4 minor; seeding/sandbox/resume/mirror all CLEAN.
- F cite: 0 confirmed-bad; 1 procedural (verify 2026 preprints) + regex/dup cleanups; years/authors exact.
- G novelty: 1 CONFIRMED (draft-artifact drift) + 2 plausible + docs reconciles; live-paper fence disciplined.
- H craft: 1 CRITICAL (word budget) + 3 MAJOR (cross-refs, EDA, compute) + order/scorecard; intuition-discipline & one-sentence-story CLEAN.
- I honesty: 2 MAJOR (grade leak [fixed], σ_seed-finding) + minors; ethics/disclosure/H1/hedging CLEAN.
- J edge: 2 MAJOR (B\* stale, PopArt overflow) + the pseudoreplication objection (Loop 99) + minors; equivalence-path/roster/dynamic-consistency/scale-confound CLEAN.

## LOOP 100 — COMPLETENESS CRITIC (biggest remaining blind spots)
1. RESULTS/DISCUSSION (CH6, parts of CH7) are campaign-gated placeholders — unauditable until the run; the banked ADD-0..6 + this sweep's P1 list guide them.
2. Whether the report-only MECHANISM kernel (SQ1-3) reports **authoring-level** uncertainty (tied to J-F3): with one search run per arm, SQ1-3 are directional point statements — a genuine depth item to consider (does not change the frozen design).
3. The compiled PDF itself (cross-refs, numbering, figure rendering) can only be audited against a fresh build — part of the final pre-submission pass.

## DISPOSITION SUMMARY
- **FIXED this session (verified, isolated, safe):** B-F2 (Null table or→and), I-F1 (grade-leak excision).
- **OVERNIGHT sequential backlog (P0 remainder + P1 + P2):** the paper-prose corrections, the two code guards + tests, the docs reconciles, and the deepening additions — each verified (freeze --check stays green — 17/17 since the E-F1 seed-guard, 15/15 before — tests green, diff re-read), each logged, none touching hash-bound files.
- **RECORD / USER-decision (P3):** seed-sizing code-vs-plan gap (surface at ratification), IUT primary designation, BCa, conditional prereg scrub, saturation disclosure, F1 diagram.
- **No hash-bound file, no config numeric, no pre-registered hypothesis/analysis was or will be changed by the sweep.** Freeze gate is 17/17 (hash 3c2082 unchanged; the overnight loop's E-F1 seed/budget guards took it 15→17).
