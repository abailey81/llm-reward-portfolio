# Corpus deep-mining — PART 2 (batch-2/3 additions), 2026-07-04

Companion to `CORPUS_MINING_2026-07-04.md` (which holds the headline verdicts, the refs.bib table, examiner
alignment, null anchors, mechanism methodology, and per-slice items for L1–3/6/7). This part captures L4, L5,
L8, L9, L10, L11, L12, L13, L14, L17, L18, L19. **Landed 17/20** (pending L15 distributional-reward-for-LLMs,
L16 foundational-canon, L20 closest-neighbours+deepdive). **Novelty verdict across all 17: SAFE — cell unoccupied.**

---

## ★★ LOAD-BEARING citation-integrity corrections (fix before freeze/submission)

1. **DSAC misfiled → `ma2020dsac` NOT first-hand-verified (L5-C1).** On-disk `DSAC__2020.pdf` = **Duan et al. arXiv:2001.02811 (risk-NEUTRAL)**, NOT the cited `ma2020dsac` (Ma et al. 2004.14547, risk-*sensitive*, absent from corpus). Acquire 2004.14547 or mark not-first-hand; if citing the variance-clip methods point add a separate `duan2021dsac`. CH2:137 conflates them. (§3.7 remedy is TQC — theory safe.)
2. **Cartea byline INVERTED (L5-C2):** cite **Coache, Jaimungal & Cartea (2023)**, arXiv:2206.14666 (Cartea LAST).
3. **Kusuoka atomless precondition missing §3.5 (L5-C3)** — corroborates **P30**. Add "on an atomless probability space"; the empirical (atomic) estimator is coherent at finite N by Acerbi Thm 5.3.
4. **Fissler-Ziegel Cor 5.4 CONSTRUCT-VALIDITY CHECK (L9):** a *sufficient/jointly-elicitable* multi-level tail rep must pair each ES/CVaR level with its **supporting VaR quantile** (k=1+L elicitable; ES-only not identifiable). **VERIFY the fed vector includes the VaRs**; else soften the "sufficient tail statistic" claim. Cite `fisslerziegel2021correction` on necessity.
5. **Sign-convention slip class (L9):** FZ2016 (VaR,ES) score G₂ increasing & **convex** (losses negative) vs Nolde-Ziegel Prop 3 G₂ increasing & **concave** (losses positive). Match conventions (same class as M1/M3). Also **Chow-2015 α-convention (L8):** 1/α (tail-mass) vs R-U/Bäuerle 1/(1−α) (confidence) — put in the M1 Conventions box; our cvar_05 = tail-mass 0.05 = confidence 0.95, factor 20.
6. **"Zero pre-registrations" MISATTRIBUTION (L12):** the Agentic-Trading survey (2605.19337) never says it — reword `RELATED_WORK_WATCH.md` #8: cite the survey's ledger (2/19 time-consistent, 1/19 cost, 0/19 R3); "none pre-registers" is OUR reading.
7. **Negative-skew warning (L4):** do NOT cite Cont fact #3 for negative skew — our F3 skew is **POSITIVE +0.21**. Don't claim infinite variance (Cont 2<α<5); hedge kurtosis 15.25 with a bootstrap CI.
8. **ARM-FM "~300k plateau" is MiniGrid-DQN not SAC (L10)** — don't use as a 200k-SAC anchor (`CAMPAIGN_DESIGN...:85,93`). **DrEureka "fix reward, vary channel" is only the RAPP ablation**, not the headline.
9. **Map note fix (L17):** "examiner (Jaimungal)" → the examiner is **Dr Okhrati** (Jaimungal is a leading author in his area, not the examiner). **NatGas "monotone" → "tunable/adjustable"** (documented exceptions; not monotone for QR-DQN); author **Hêche** (circumflex).

## ★ P20 ξ-CLAIM — REFINED by L8 (updates pre-freeze checklist #4)
Troop (2021) deferral is **mechanically CORRECT and now first-hand vindicated** (UPOT requires heavy tail ξ>0 by construction — Def 2.5/Assump 2.1; the A(n/k) estimator eq.22 carries ξ̂_MLE in the denominator → ill-conditioned as ξ̂→0; validated only at n∈[5000,50000], α∈{0.998,0.999}, far from our n≈750/α∈{0.05,0.01}; Troop's own conclusion defers the light-tail case to future work). **BUT the `measurement.py:57,84,133` justification wording is data-contradicted** and must be reframed: "94% ξ≤0" is an **ESTIMATOR-behaviour fact** (at n≈750 with ~30–75 exceedances, MLE shape SE≈0.13–0.19, so ξ̂≤0 lands often even for a truly heavy tail ξ≈0.2), **NOT a population-tail fact** — and "portfolio losses are light-tailed" is theoretically fragile (max-sum equivalence preserves the tail INDEX under diversification; only the scale shrinks). **Fix:** rest the deferral on (a) sample-regime/estimator-instability + (b) Troop's explicit heavy-tail-only scope; DROP the "light-tailed / ξ∈(−0.5,0.3)" population framing; **re-measure the ξ̂ fraction on the actual Split-C fed windows** and correct the number. Also **cite `troop2021biascorrected` in prose** (CH4 §4.4 ~L131, CH7 §B.1.3 ~L153 — currently defined-but-uncited). This makes the pre-freeze checklist #4 fix precise: it's the *wording/measurement*, not the decision.

## ★ P6 / P9 CONTAMINATION REFRAME (L18 goldmine)
- **§4.5 reframe:** masking/anonymization ALONE does not remove parametric look-ahead (Gao-Jiang-Yan LAP Prop 1; Wu 2025). Lead §4.5 with the STRUCTURAL argument — LLM authors a **date-blind reward FUNCTION, not a forecast** (no μ̂ to correlate with future ε), train-split-only feedback, arm-differencing cancels common era-priors — and cite the LAP test as the SOTA detector our design defeats by construction. **ProfitMirage/FactFin ("LLM-as-code-generator reduces memorization") independently validates this philosophy.**
- **P9 silver-lining:** COVID-era is empirically the MOST contamination-prone regime (Temporal-Contamination-Detector) → excluding it *also removes the worst residual-era-prior window* → P9 = "disclosed cost **with a contamination-hygiene benefit**." (AlphaSharpe includes COVID as a stress-test — justify purge-vs-stress-test.)
- **P6 defence:** (a) we already model costs (most LLM-finance assumes zero — Explicit-Bias); (b) the look-ahead is **common-mode across all 7 arms → cancels in the H2-Tail between-arm CVaR difference**; (c) inflates the *absolute* left tail → makes the tail-improvement bar *harder* (conservative); (d) bounded by the cost sweep.
- **Cross-model (Opus+Qwen) = CMMD contamination robustness** (differing cutoffs as a natural experiment).

## ★ NEW refs.bib additions (batch 2) — verify, then cite-and-USE
- **Theory/risk:** `ahmadijavid2012evar` (EVaR — KL-ball dual unifies with CVaR's L∞ set as one φ-divergence-DRO family → §3.6 anchor + fixes "not a φ-divergence ball"); `coache2022conditionally` (2023 — k-elicitable spectral vector; closest comparator; examiner lineage); `cheridito2009time` (E[u]−λ·VaR **dynamically inconsistent = our exact reward form** → §C.14 Okhrati attack); use `acerbi2002spectral` (in refs, UNDERused — Thm 4.1 φ non-increasing, Thm 5.3 finite-N coherence); `rowland2019statistics` (quantiles/CVaR NOT Bellman-closed → tail-via-reward architectural defense); `duan2021dsac` (variance-clip, if cited); `abel2021expressivity` (via Motif/Skalse — acquire first-hand).
- **CVaR-RL canon (L8, distinguish — native optimizers we are NOT):** `chow2014cvar` (Chow-Ghavamzadeh NeurIPS'14 — Lagrangian mean-CVaR AC), `tamar2015sampling` (AAAI'15 — CVaR gradient + IS-for-small-α supports our EVT small-α argument), `bauerle2011markov` (cited; Ex 3.8 optimality fails for state-only CVaR = time-inconsistency pre-emption), `chow2015risk` (cited; eq.2 CVaR=robustness duality = Pillar C).
- **Tail backtesting (L8):** `bayerdimitriadis2022` (Bayer & Dimitriadis, JFEc 20(3):437–471 — Strict/Intercept ESR ES-backtest, well-sized at T=250, `esback` R pkg; one-sided Intercept targets under-estimation, pairs with Bauer); Bauer 2025 exact power table (upgrade the generic cite).
- **Stylized facts / stats (L4):** `mandelbrot1963`, `cont2001`, `toth2011`, `harvey2017scientific` (pre-reg checklist + SD-MBF Bayes-for-null), `schmidtlipson2009`.
- **LLM-reward lineage (L9/L10/L11):** `lehman2022elm`; ICPL (**priority** 2410.17233 — 30-reward budget twin + monotonicity + OpenLoop control); CoopMARL (2603.24324 — structurally closest); ARM-FM (2510.14176); Language-to-Rewards (`yu2023language`).
- **LLM-finance fences (L12/L13/L14/L17/L19):** FINCON (`yu2024fincon`), ONI (`zheng2024oni` — 2-family taxonomy locates us), Motif (`klissarov2023motif`), Agentic-Trading survey (2605.19337), QuantEvolve (2510.18569 clear %VERIFY), SHARP (2605.06822), Macro-Economist-Agent (2606.08283), Unnikrishnan (2411.11059), Adaptive-Alpha (2509.01393), Coache-Jaimungal **Robust Distortion** (2409.10096 — **closest non-LLM portfolio comparator, needs its own key**), NatGas/Hêche (2501.04421), Quantile-Targeted/Baruník (2510.19271), Tail-Safe (2510.04555), RAMAC (2510.02695), Srivastava composite-reward (2506.04358 — H1 template, **formulation not results**), Moghimi-Ku (2501.02087 — SRM=∫CVaR spectral grounding).
- **Contamination (L18):** DatedGPT (2603.11838), `kong2026explicitbias` (2602.14233 — five-sins + Structural-Validity checklist), `benhenda2026lookahead`, `merchant2025divergence`, `gao2025lookaheadtest` (LAP, measure-theoretic), `roy2026memguard` (CMMD; numbers cautiously), `zhang2026temporalcontam` (K(t) filtration + Shapley-DCLR); upgrade `li2025profitmirage`; + LopezLira-Tang-Zhu 2025 (2504.14765), FINSABER (2505.07078).
- **Framing (L8):** `gridach2025agentic` (ICLR'25 — its Challenges §7.1/§7.3 name overfitting/reproducibility/cost-eval as the field gap → our rigor stack is the worked answer; also AI-disclosure support); `shahriari2016taking` (BayesOpt review → backs the bayes_opt arm; bounds novelty). Snoek "EI-per-second" → budget-matched H4.

## ★ NEW theory / mechanism HOOKS (examiner-grade)
- **Ng-1999 ⊂ Skalse-unhackable ⊂ hackable LADDER (L9):** decompose (tail − scalar) reward; locate the pair — Ng-equivalent (identical optimal policy = strongest null) ⊂ Skalse-unhackable (no ranking reversal) ⊂ hackable (treatment possible). Placebo = potential-like/policy-invariant; genuine tail arm = optimum-changing. Novel mechanism framing.
- **OPRO "hallucinating numeric values" (L9)** — strongest SQ3 evidence (frontier LLM echoes/pattern-matches numbers without computing them). Pair with **Wallace** (extrapolation fails, sub-word brittle) + **ERFSL** (EUREKA-S misses a sign-flipped penalty, "only modifying reward weights") + **URDP/L2R** (field routes numeric optimization AWAY from the LLM to BO/templates) + **Batra** (numeracy bottleneck) = the numeracy-bottleneck cluster grounding ADR-039. Frontier-Opus ⇒ a null isn't a capability floor.
- **Mandelbrot §IV-A nonsense-moments + Rowland Bellman-closedness (L4):** L2/variance ill-posed under heavy tails; quantiles/CVaR not Bellman-closed → tail belongs in the reward/objective, not an L2 scalar or a value critic.
- **RU Gaussian collapse (L7) + Moghimi-Ku "blindness to success" (L17):** null plausible if returns near-Gaussian; feed MULTI-level (not single deep-tail) because pure left-CVaR "overlooks the right tail."
- **Bäuerle Ex 3.8 + Chow-2015 duality (L8):** state-augmentation license (inject tail via REWARD, keep SAC on the un-augmented state) + CVaR=distribution-shift-robustness (feeding the tail = feeding a robustness signal the sealed-OOS test measures). **Prashanth-Fu Lagrangian (L9):** the LLM penalty = Lagrangian relaxation of CVaR≤threshold.
- **Bauer 2025 power table (L8):** DM/MCS tail tests are underpowered (power 0.9–15% at p=0.01; type-III > power at short windows) → "non-rejection at the tail is a weak test"; **CVaR-25%/10% are the more reliable tail comparisons than 5%/1%**; prefer the joint (VaR,ES) score. Limitations headline.

## ★ NOVELTY — final cross-batch verdict (SAFE)
Every threat-bin paper breaks ≥2 conjuncts. Precision guards: **RD-Agent already feeds an 8-dim metric vector to an LLM loop → "vector-to-LLM-loop is NOT novel"**; our cell = LLM authors reward **CODE** × **multi-LEVEL left-tail** × **pre-registered** × mechanism. **Signal-vs-reward:** AlphaGPT/QuantAgent/ELfolio/RD-Agent/Adaptive-Alpha author the *signal/strategy/factor*; we author the *reward*. **BlindTrade/FactFin/ProfitMirage** ("LLM writes financial code on anonymized inputs") isn't itself novel → state the reward-channel-as-IV distinction wherever the design is introduced. **Distributional-CRITIC bin (L17) is uniformly on the critic/human-reward side** → reinforces the empty cell; the one MED comparator is **Coache-Jaimungal Robust Distortion (portfolio × risk-sensitive-RL, human-fixed objective)** — the G2 positioning cite (edge-or-honest-null vs a strong fixed risk objective). The field convention is **scalar fitness/feedback**; we depart to a multi-level tail vector.
