# Deep Sweep — 30 ultra-deep probes (FINAL), 2026-07-04

**What this is.** A user-requested final round of 30 parallel *ultra-deep* audit probes (P1–P30),
each aimed at territory the prior sweeps (100-loop + the 24-pass overnight loop + the 8-front / 13-agent
audits) did **not** work hard: training numerics, the env/simulator internals, the analysis-pipeline
math **at its boundaries**, measurement small-sample behaviour, config→code drift, a full cross-document
number reconciliation, and a fresh adversarial measure-theoretic read of the theory. Each probe verified
**first-hand** (traced the real path; ran boundary computations / Monte-Carlo / hand-checks), not from names.

**Headline.** The **confirmatory core is comprehensively clean** — ~20 independent deep probes (many with
first-hand hand-checks) found no defect in leakage, DSR, PBO, the headline IUT, TOST, rliable aggregation,
FDR multiplicity, the fed-vector construction, seeding/replay determinism, hypothesis→analysis wiring,
benchmark fairness, terminal/bootstrap handling, or the theory (M1–M13 fully applied, no reversed residual).
The sweep's value is a **pre-freeze checklist** (below) + a set of safe hardening fixes.

**Charter (unchanged).** Never modify the 8 hash-bound freeze files (`PREREGISTRATION.md`,
`config/{preregistration,inference,environment,data,arms}.yaml`, `prompts/{system,initial_generation}.txt`);
additive + reversible only; verify (tests + `freeze --check` must stay **17/17, hash `3c2082…`**); anything
judgment-heavy / in a hash-bound file / that changes pre-registered semantics or dynamics = **RECORD**, not apply.
**Correction learned this sweep (P26):** `CH6_results.md` and **all paper chapters are NOT hash-bound** — only the 8
files above are. So stale-parameter fixes in paper prose are in-scope.

---

## ★ PRE-FREEZE CHECKLIST — needs Tamer's decision (do BEFORE `freeze.py` runs)

These are the substantive findings. None flips the pre-registered **bounded-effect / equivalence** headline
(several are *conservative* for it), but each is examiner-catchable and most touch **hash-bound** files, so they
must be settled before the freeze locks them in.

| # | Probe | Issue | Why it matters | Suggested action |
|---|-------|-------|----------------|------------------|
| 1 | **P5** | **Reward-scale confound, instruments blind.** PopArt's `min_scale=1.0` is a one-sided shrink → identity for any reward with RMS≤1 (the operative regime); so `sigma_max==1.0` (the "no scale confound" signal, in 6 docstrings + a test) is vacuous there, and the `popart=False` falsifier is a *bitwise no-op* for sub-unit rewards. Scale is unarchived, so mediation has no covariate; it's plausibly entangled with the treatment (CVaR-in-bps-fed → ~1e-2 rewards; Sharpe-scalar-fed → O(1)). | Conservative for the equivalence headline (scale can only push arms apart), but a real threat to the **mechanism** kernel + any H2-Tail claim; exactly what an RL+risk examiner probes. | **APPLIED (instrumentation):** expose unclamped `raw_rms` in PopArt + archive per-candidate; correct the 6 docstrings + the test. **RECORD (design, yours):** (a) a true affine-invariance control (retrain a frozen winner's reward ×{1e-2,1e2}); (b) whether to lower `min_scale`; (c) add `raw_rms` as a mediation covariate; (d) extend the T2.4 disclosure. |
| 2 | **P6** | **Cost-ledger contemporaneous look-ahead.** Turnover drift uses `r_t` (post-decision) not `r_{t-1}`: `w_tilde=w_prev·(1+r_t)/…` (`portfolio_env.py:301-311`). Down-days are charged *more* cost → **fattens the left tail of realized `port_ret`** (the H2-Tail DV *and* the fed CVaR signal), and hits the flee-to-cash tail arm hardest. Disclosed but ~10× understated (`environment_spec_v1.md`) and framed as a benign "convention." | Bounded, conservative for equivalence, but a genuine look-ahead a measure-theoretic examiner flags; cheap pre-freeze fix. Changes env dynamics → re-baselines pilots. | **RECORD (yours):** carry drifted holdings `w_held` as env state (drift by prior return); update the 2 convention-locking tests (`test_env.py:98,156`); revert the spec note. + minor: 3 turnover defs differ (`baselines/rewards.py:373`, `reward_family.py:122`); defensive `port_ret<-1` assert. |
| 3 | **P9** | **COVID crash is absent from the scored test window.** The mandatory 60-session val→test purge puts the first scored test day at **2020-03-30 — after the trough**; the purge zone (2020-01-02…03-27) swallows the entire crash. Structurally unavoidable under Split-C (scoring COVID = a leak). But **hash-bound** `config/inference.yaml:10` + `PREREGISTRATION.md:406` still call COVID an in-test regime (CH4 discloses it correctly). | Freezing an inaccurate "COVID in test" claim; and the biggest tail event is excluded from a tail study (a power/interpretation caveat). | **RECORD (yours, pre-freeze):** tighten the 2 bound lines to CH4's wording ("post-COVID-crash recovery + elevated-vol regime"); add a 1-line Discussion caveat. |
| 4 | **P20 + P19** | **"ξ ≤ 0 for ~94% of realistic samples" is contradicted by the data.** Fitting the repo's own GPD estimator to the actual univ5 EW portfolio gives ξ ≈ **+0.22** (heavy-tailed); the 94% only holds near-Gaussian (falls to 60/33/20% for t8/t5/t4). The claim sits in **hash-bound** `PREREGISTRATION.md:260` (+ non-bound `measurement.py:82-86`). | A data-contradicted tail-shape claim about to be frozen, in a tail-risk paper read by a coherent-risk examiner. Does **not** change the decision — the UPOT-deferral stands on the sample-size/tail-level *regime mismatch* regardless of ξ sign, and a heavier tail only *strengthens* "fed CVaR is noisy → biased against H2." | **RECORD (yours, pre-freeze):** rest the UPOT-deferral on regime-mismatch + variance-dominance; drop/soften the "94% ξ≤0" figure or replay it against the archived realized sample; verify ξ sign on the real rollout once the campaign runs. |
| 5 | **P26** | **MDE self-contradiction.** CH4:232 + CH7:236 quote "≈0.256 Sharpe (≈0.169 DSR)" — the *Šidák-m=6 conservative sensitivity R25 demoted from the gate* — but the paper's decision rule is the one-sided IUT, whose **primary** MDE is 0.181 Sharpe / 0.120 DSR (0.141 @90%). "0.169 DSR" is in no authoritative source. | The inconclusive conclusion is unchanged, but the paper contradicts its own rule; feeds the seed-ratification. Non-hash-bound (fixable). | **APPLY (mine):** requote the primary MDE and *name* the conservative one; reconcile `docs/CAMPAIGN_power.md`'s half-stale prose. Flagged to you because it feeds seed-sizing. |
| 6 | **P23** | **SEARCH/selection split is unguarded.** The winner-selection window (`val_end=2019-12-31`) is read from the **non-hash-bound** `prototype.yaml`, with no freeze assert tying it to the frozen `data.yaml` splits — and this class of drift **already bit once** (`subexperiment.yaml` had a stale pre-Split-C `val_end`, caught only by manual audit). | The *executed search* split can silently drift from the frozen design; the freeze doctrine's own promise is incomplete. | **RECORD (yours):** add `assert_search_splits_match` to `freeze.py` (mirror `assert_seeds_match`) requiring `prototype.yaml`/`subexperiment.yaml` `val_end == data.yaml` splits — **changes the gate 17→18**, so your call; OR resolve the search boundaries from `data.yaml` directly. |
| 7 | **P18** | **Seed-sizing (~350) not in reproducible code + a false "freeze-ready".** The χ² upper-CI derivation (verified sound: 279/340/403 @80/90/95%) lives only in a changelog sentence + a throwaway script; the committed estimator caps at n=50 and returns `None`. Worse, `determine_design.py` reports **`freeze_ready: true` at the stale `n_seeds=30`**, ignoring the pending 30→350 amendment. | The sample-size justification for a pre-registered study must be reproducible; the false "READY" could greenlight a premature freeze. | **APPLIED (mine):** fix the freeze-ready gating to block on seed ratification. **RECORD (yours):** ratify ~350 + fold the χ² estimator into `estimate_seed_count` at ratification. |
| 8 | **P17** | **Stale prereg leg-decision line.** `PREREGISTRATION.md:543` (R25) still states `p_one = p_two/2`, but the code uses the direct upper-tail `pvalue_one_sided_greater` (the later R64 fix, for the skewed CVaR leg). The R25 paragraph lacks the "(superseded by R64)" pointer R13 correctly carries. | Freezing a §10 paragraph that contradicts the (more-correct) code. Hash-bound. | **RECORD (yours, pre-freeze):** append "(superseded by R64 — leg p is the direct upper-tail, not p_two/2)" at `:543`. |

Secondary pre-freeze wording (hash-bound, lower stakes): **P19-F2** ("≈2,961 sessions ⇒ strictly better-fed cvar_01" holds only under IID; under vol-clustering cvar_01 CV is flat ~0.33 — the window adds *episode coverage*, not variance reduction); **P21 NIT** ("Bowley skewness" is applied to the 5/95 quantiles, a Kelly-type generalization, not quartiles — consistent everywhere, an external-precision point).

---

## Applied fixes (safe, non-hash-bound; test-gated) — see the "APPLIED" markers per probe below and CHANGELOG [2026-07-04c]

(Filled as each lands; `freeze --check` re-confirmed 17/17 hash 3c2082 after all edits.)

---

## Full 30-probe disposition

**Legend:** CLEAN = verified sound, no defect · APPLY = safe fix I make · RECORD = for Tamer / write-time / hash-bound.

### Training numerics & RL dynamics
- **P1 — SAC alpha auto-tuning · CLEAN + 1 MED.** Divergence/collapse/NaN paths all provably safe; alpha identical across all 7 arms/legs. MED (→ P5): the `sigma_max` audit is blind to sub-unit reward magnitude. LOW: `target_entropy=-31` over-counts DOF by 1 (arm-invariant). *APPLY: raw_rms + docstrings (with P5); one limitation line.*
- **P2 — Replay buffer 50k vs 200k · CLEAN.** Hypothesized staleness **refuted** — the env is episodic replay of a fixed 2005–16 window (episode L≈2,961), so every GFC/2011/2015 day recurs ~17× in the buffer; no recency bias. 2 minor disclosure notes (hash-bound). *RECORD.*
- **P3 — Critic/PopArt dynamics · CLEAN + FIXED.** Uncentered 2nd-moment sidesteps negative-variance; bias-correction sound; last-layer-surgery N/A (reward-scaler form). **Fixed a real latent NaN-propagation defect** (a NaN reward poisoned the EMA and silently disabled PopArt for the rest of a candidate; `np.clip` doesn't sanitize NaN) — 2-line guard + 7 new tests (`tests/test_deep_p3_popart_dynamics.py`), 18+41 green. Unreachable in prod (`safe_call`) but now provably so. REPORT: post-spike signal-suppression (intended RMS-EMA behaviour, auditable); `warmup` off-by-one (dormant, default 0). *APPLIED by P3; docstring notes to add.*
- **P4 — Target-net / update ratio · CLEAN + provenance.** `tau/train_freq/gradient_steps/target_update_interval` all SB3-2.8.0 defaults, identical across arms/seeds (no confound). MINOR: they're used silently + outside `assert_fixed_agent_across_arms`, and `algos.yaml:25` says `tau: null` despite its header promising to record them. *APPLY: set explicitly (behavior-preserving) + record in algos.yaml + extend the guard. + fix stale `popart.py:63` "full-history buffer" comment.*
- **P5 — Reward-scale identification confound · MAJOR.** See checklist #1.

### Environment / simulator
- **P6 — Transaction costs · CLEAN core + MAJOR.** Conservation verified to 1e-15 (net=gross−cost, weights sum 1, cash≥0, cost from config). MAJOR: cost-ledger look-ahead — checklist #2.
- **P7 — Risk-free rate threading · CLEAN (R20 RESOLVED).** rf=0 headline by design (`preregistration.yaml:236`), DGS3MO-excess wired as a report-only sensitivity, env prices cash when set; unit check on real data confirms rf-invariance of the *arm contrast* (~0.016). **R20 memory is stale → update to resolved.** MINOR: `metrics.py` applies rf to Sharpe but not PSR/DSR (harmless; no non-test rf≠0 call reads them). *APPLY: one docstring note. RECORD: mean cash weight per arm (post-campaign).*
- **P8 — Observation leakage · CLEAN (empirical).** Corrupting the panel future leaves every decision-time obs **byte-identical** (max|Δ|=0.0) on synthetic + real univ5; VIX prelag truthful end-to-end; VecNormalize train-only frozen; half-open windows; no obs-mutating wrapper.
- **P9 — Split enforcement · CLEAN + MAJOR.** Leakage clean (25 tests + boundary dates on the real panel; seam over-conservative). MAJOR: COVID excluded + freeze-bound wording — checklist #3. MINOR: ~10 stale-window comments in `run_campaign.py`. *APPLY: reconcile the comments.*
- **P10 — Episode/delisting/terminal · CLEAN.** done-vs-truncated bootstrap correct end-to-end (`dones*(1-timeouts)=0` at edge, verified vs SB3 2.8.0 + scripted episode); delisting zero-fill preserves the delisting-day return, no ffill leak/weight desync. Minor: stale "PROVISIONAL/ADR-024" docstring (resolved); no delisting/timeout regression test; report-only `_pooled_cvar` phrasing; latent "liquidate_to_cash earns 0 not cash" (inert while cash rate=0). *APPLY: docstring + new regression test + phrasing. RECORD: bind delisting policy into the hash (pre-freeze).*

### Analysis pipeline (boundary-checked)
- **P11 — DSR internals · CLEAN.** Independent hand-check matched the module to 1e-10 on 9 anchors; skew/kurtosis terms, N-deflation, Euler–Mascheroni, annualization all correct; no off-by-3. Cosmetic: a display-only PSR uses bias-corrected vs plug-in moments (immaterial, freeze-adjacent). *RECORD.*
- **P12 — PBO enumeration · CLEAN.** Bit-identical to a `scipy.rankdata` clean-room over 40 matrices; enumeration symmetry, logit direction, ties, edge guards all Bailey-correct. 5 minor = disclosed design choices / write-time (report both mean- & Sharpe-ranked PBO; state the median-tie convention). *RECORD.*
- **P13 — Headline IUT · CLEAN.** Empirical decision table confirms genuine intersection-union (one failing leg kills the endpoint = max-p), α correctly un-split, CVaR sign consistent, ties/NaN conservative; CI tests pin the table. Report-only nuance: a Bonferroni-sensitivity p pools all 6 legs (understates H2-Tail; never gates). *RECORD.*
- **P14 — TOST equivalence · CLEAN (correct) + minors.** 90% CI for α=0.05 (no 95% confusion), strict containment ⇔ both one-sided reject, paired covariance carried, INCONCLUSIVE branch fires (verified at the boundary). *APPLY: thread `_frozen_equiv_margin` into 3 call sites; make the equivalence figure's boundary test strict (`figures.py:49`). RECORD (write-time): never claim H2-Tail equivalence vs the raw ±0.05 CVaR band — it's near-vacuous at daily scale; use the fractional band. Use IQM (not mean) in the DSR leg or note it.*
- **P15 — rliable aggregation · CLEAN.** IQM byte-equal to `scipy.trim_mean`, per-seed (not seed-averaged); null-calibration proves the safe path (per-seed rejects at 2.7% vs seed-averaged 48.7%). Minors: "stratified" collapses to i.i.d. paired bootstrap on one task; display CI percentile vs pivotal p; `n_boot=2000` could rise for the final headline (config). *RECORD.*
- **P16 — BCa · CLEAN (there is no BCa).** Every CI is honest percentile/parametric-t (docstrings say so; no integrity drift). **Discharges A-F4 with numbers:** at n=30 the trimmed-IQM headline is BCa≈percentile; BCa moves only the *raw-CVaR report-only* legs (~6%). *APPLY: a one-sentence coverage caveat on the CVaR-CI captions (placed to not cost body words).*
- **P17 — FDR multiplicity · CLEAN.** Confirmatory correction spans exactly the frozen m=6 (two IUTs), dependence-robust (Berger), byte-asserted + freeze-guarded; every other test is in its own declared+corrected family with disjoint keys. Minors: stale `p_two/2` prereg line (checklist #8); BH-PRDS assumption unstated on 2 non-gating sensitivities; never collapse the two co-primaries into one disjunctive "H2 supported". *RECORD.*
- **P18 — Seed-sizing power math · SOUND + 2 MAJOR.** See checklist #7.
- **P24 — Seeding/replay determinism · CLEAN.** Twice-seeded checks bit-identical (SAC stochastic predict max|Δ|=0.0); archive replays byte-for-byte; PBO full-enum deterministic; analysis pinned to `default_rng(0)`. MINOR (latent, not live): 8 OS-entropy `default_rng()` fallbacks in `inference/` (all reported-path calls pass an explicit rng; 2 helpers uncalled). *APPLY: seed the 8 fallbacks to match convention.*
- **P27 — Hypothesis↔analysis wiring · CLEAN.** Every H1–H4 (H2-RA/Tail) traced *and run* on crafted inputs (IUT needs all 3 legs — no single-leg leak; selection floor correct); 68 repo tests pass; config mirror matches; realized-family byte-assert fires. Minors: `method="bh"` is a misnomer (it's per-leg IUT); `H2_supported` alias == H2-RA only (contained). *RECORD.*
- **P29 — Benchmark fairness · CLEAN.** All 8 baselines: identical costs, causal 60-day estimation (no full-sample look-ahead), symmetric rf=0, same window/universe, per-seed median, N=30-vs-N=1 deflation favors baselines. Minors: daily re-opt inflates *non-binding* sophisticated-tier cost (disclosed); no true market tier (anonymized panel); weak momentum spec. *RECORD (write-time framing).*

### Measurement / fed vector
- **P19 — CVaR small-sample bias · CLEAN (construct validity HOLDS).** L≈2,961 (full train rollout, not per-episode); `cvar_01` is EVT-extrapolated, **fed-only, excluded from the m=6 family** (tested metric is empirical CVaR-0.05). Fed `cvar_01` is ±22–33% noisy under vol-clustering but bias-free, correctly ordered, flagged, biases *against* H2 → **not fed noise**. Minors: EVT gives no `cvar_01` gain over empirical (docstring over-scopes it); frozen "strictly better-fed" is IID-only (checklist secondary); unbounded-below EVT output at small L (defensive). *APPLY: soften docstring, add defensive floor. RECORD: F2/F3.*
- **P20 — EVT tail-fit · CLEAN (not in headline) + MAJOR.** EVT only in the fed vector (treatment), never selection/headline; paper *under*-claims its robustness. MAJOR: the ξ≤0 claim — checklist #4. Minors: `threshold_sensitivity` silently substitutes empirical at fallback thresholds (distorts the EVT-stability exhibit); fed GPD fit less defensive than the diagnostic one (no try/except, ξ-finiteness unguarded). *APPLY: skip/label fallback thresholds; add try/except + `isfinite(xi)` guard.*
- **P21 — Fed 6-vector consistency · CLEAN.** The *frozen* vector is internally consistent + empirically sound (0/4000 sign or monotonicity violations; the prompt carries no definitions so it can't drift from code). 2 real defects **outside** the frozen feed: `left_tail_mass` mis-rendered in the *legible* sub-experiment (degenerate decile + "bps" for a probability); `prompts/README.md` mis-states which templates load. NIT: "Bowley"→5/95 (checklist secondary). *APPLY: fix legible band+unit + test; fix README.*

### Config / repro / cross-doc / structure / theory
- **P22 — Endogeneity honesty · CLEAN code + 1 MAJOR (internal doc only).** Code is genuinely endogenous (fits the policy's own realized returns; verified); the paper's "critic-agnostic ≠ agent-independent" framing is intact everywhere in `paper/`. One banned phrase ("decoupled from the agent… works for any agent") survived in `docs/distributional_feedback_schema.md:12-13` (missed by round-6). *APPLY: reword to the endogeneity framing; + stale "log→simple returns" (R55).*
- **P23 — Config→code hardcoding · 1 MAJOR + minors.** MAJOR: search-split guard gap — checklist #6. Minors (mostly inert): fed CVaR levels hardcoded in `measurement.py` with 2 un-reconciled config mirrors (add a reconciliation test — guards the manipulated variable); stale `trainer.py:122` 50000 fallback; hardcoded SESOI/α/M in the throwaway sweep; roster sizes / notebook asserts. *APPLY: CVaR-levels reconciliation test. RECORD: the rest.*
- **P25 — Repro-audit script adequacy · MAJOR (mine to fix).** `audit_reproducibility.py` is a keyword smoke-test dressed as a verifier — never recomputes the hash/checksum/replay, and 3 "pillars" can't emit FAIL (a violated freeze still prints PASS); the test enshrines "fake hash ⇒ PASS". The *real* guards (`freeze --check`, `_verify_checksum`) are sound and run elsewhere, so it's confined to this one artifact falsely reassuring a marker. *APPLY: promote to a real verifier (reuse `freeze.canonical_hash` + checksum primitives, guarded imports, WARN-never-silent-PASS, let the pillars FAIL) + fix the test.*
- **P26 — Cross-document number reconciliation.** **Premise corrections:** CH6/all chapters NOT hash-bound (fixable); authoritative dims are univ5 **5406×963** (5283×953 is univ3). MAJOR #1: MDE self-contradiction (checklist #5). MAJOR #2: CH6 `B*=50,000` residual at **two** lines (`:33`, `:133`) — every other chapter says 200,000. MINOR: prototype candidate count 239 vs 240. **Everything else reconciles** (arms=7, seeds=30, n_cand=30, SESOI, buffer=50k, dims, univ5 EDA incl. positive skew, compute, no RTX-4090 leak, embargo). *APPLY: CH6 200k ×2, MDE reconcile, candidate count.*
- **P28 — UCL structural compliance.** Word-count denominator **VERIFIED CORRECT** (excludes math/code/tables/footnotes/appendices; 16,426 = tracked H-F1 overage). 4 fresh defects: References/Appendix order inverted (Appendix B embedded in CH7 prints before References); Harvard in-text cites leak QC notes ("verified verbatim", a raw DOI) inside `(Author, Year)`; **phantom "Appendix A"** in the ToC with no target; Ack-before-Abstract + Appendix B.6/B.7 range. "Dr Okhrati", Declaration, Ethics, AI-disclosure all clean. *APPLY: the 4 structural fixes. RECORD: H-F1 word surgery, H-F2 figure numbering (supervised).*
- **P30 — Theory residual rigor · CLEAN (no CRITICAL/MAJOR).** M1–M13 fully applied, no reversed residual; catastrophic-class checks (Thm 3.1 flips, Cor 3.3 orientation, DPI strict-convexity, CVaR convention, Kusuoka, elicitability) all clean; 21 attributions resolve. 5 MINOR precision items. *APPLY: add `E|Z|<∞` to the CVaR-dual min; fix Cor 3.3 constant (`2δ` not `δ` — delete the ½ to make TV the L¹ convention); atomless-space precondition for Kusuoka; `P-a.s.` on the sufficiency statement; disambiguate ξ in NOMENCLATURE.*

---

## Memory / tracking updates
- **R20 is RESOLVED** (P7) — update `project-gated-data-blockers` (was "PENDING: thread rf into headline Sharpe").
- The reward-scale confound (P5) and cost look-ahead (P6) are the two substantive pre-freeze *design* items; the ξ-claim (P20/P19), COVID wording (P9), MDE (P26), search-split guard (P23), seed χ² (P18), and stale `p_two/2` (P17) are the pre-freeze *wording/reproducibility* items.
