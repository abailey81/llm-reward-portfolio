# RUN 21 — SESSION PROMPT. **READ THIS IN FULL BEFORE YOUR FIRST SUBSTANTIVE ACTION.**

Written 2026-08-04 ~16:00 UTC at Tamer's instruction: *"I want to transition into the next claude
code session. Ultrathink very deeply and extensively, document absolutely everything from this
session, and write a prompt for the next claude code session, don't forget to also tell it to very
deeply and extensively study absolutely all files in this dissertation so it has the comprehensive
knowledge, and zero gaps in knowledge... Make sure you include my prompts on that everything should
be flawless as well. Make sure in addition it also preserves these loops, but dives much deeper, and
checks more extensively, and checks very deeply absolutely everything, from all dimensions and
angles possible, it must not miss anything, and make sure it ultrathinks, and minimises the ETA to
an absolute minimum as well. And make sure absolutely everything is very logical, meaningful, and
absolutely flawless and 1000000% absolutely strictly correct."*

> **You run the live campaign on an irreplaceable MSc dissertation.** RUN 4 has been running since
> 2026-07-28 21:08 UTC. Real money is spent, the test data is sealed, **there is no re-run.**
> This supersedes `docs/RUN20_SESSION_PROMPT.md`; where they disagree, **this wins.**
>
> ⚠ **A SEPARATE SESSION OWNS THE WRITE-UP.** `paper/**`, `docs/GRADE_95_MASTER_PLAN.md`,
> `docs/V2_WRITE_TIME_REGISTRY.md`, `docs/CITATION_WORK_MAP.md` are **NOT YOURS.** `CHANGELOG.md` is
> **SHARED** — re-read it immediately before every edit, and never reuse a date label.
>
> ⚠ **THE LANE CLASSIFICATIONS ARE ABANDONED.** Do not register a lane. Reading the board with
> `lanebus.py` STAMPS a heartbeat — it is not read-only. Avoid it.

---

# §0 ★★★★★ TAMER'S STANDING BRIEF — VERBATIM. THIS IS THE OPERATING CONTRACT.

### §0.1 THE FLAWLESSNESS MANDATE (carried forward every session)

> *"very deeply and strictly monitor everything constantly and ensure absolutely everything is
> strictly absolutely flawless 10000000%. Ultrathink very deeply and extensively. I give you full
> permissions, full freedom, and I ratify the actions. **I give you no permission to stop until
> absolutely everything is strictly absolutely 10000000% absolutely flawless.** Do whatever it
> takes. Very deeply investigate everything, and speed up to an absolute maximum. Please before act,
> make sure you very deeply study this dissertation. Take as much time as you need, as many tokens
> as you need. Make sure you also very deeply and extensively constantly check each record, make
> sure every record individually is very strictly flawless, logical, meaningful. Don't be lazy, I
> give full ratifications, full freedom, full permissions. Please make sure you study every file in
> this project very deeply, all processes, the whole thing going on on Myriad, absolutely
> everything, please don't miss anything. This campaign run is extremely important, and it must be
> absolutely flawless across absolutely all dimensions possible. Dive extremely deep, don't be lazy,
> check absolutely everything very deeply and extensively, make sure you don't miss anything, and
> make sure you always verify, and you are always very precise. Please work very accurately, and
> very surgically, make sure you make no mistakes. Ultrathink 100000 times before doing anything."*

### §0.2 THE LOOP CONTRACT, VERBATIM

> *"Also every 30 min I want you to very closely check everything very deeply, check everything, all
> lines, all records, all outputs, all processes, absolutely everything, it all must be
> 1000000000% strictly absolutely flawless."*

> *"make sure if something is found, its always fixed, and ensure that absolutely everything is very
> strictly absolutely flawless"*

> *"make sure the checks that are every 30 minutes do not stop until they ensure absolutely
> everything is 10000% absolutely strictly flawless. I give them full permissions, do whatever it
> takes to ensure absolute flawlessness."*

> *"also dont forget to add the speed check component all the time, and its maximisation"*

> ⭐ *"make sure you very deeply and extensively study this whole project, have an extremely
> extensive knowledge and absolutely 0 gaps in knowledge."* — **DO THIS BEFORE ACTING. §2 is the
> reading list; it is not optional and it is not a formality.**

### §0.3 HOW TO READ THAT MANDATE — the single most important paragraph in this file

**Full permission raises the bar on the THINKING; it does not lower the bar on VERIFICATION.**

RUN 20 fixed 23 defects across 18 passes. Two auditors returned **24 findings**, and **SIX of the
most serious were about RUN 20's OWN WORK THAT SAME DAY** — four structural defects in fixes made
hours earlier, one **false claim printed on the live board every cycle**, and a class fix that
missed three members of its own class. **Ten auditors across ten sessions have every time found more
than the author did.**

> **⇒ SEND AN AUDITOR AT YOUR OWN FIXES BEFORE YOU BANK THEM. Not at the campaign — at YOURSELF.
> That single act was the highest-value thing RUN 20 did.**

---

# §1 YOUR FIRST COMMANDS

```bash
cd /c/Users/User/Desktop/dissertation_papers/llm-reward-portfolio
date -u +%Y-%m-%dT%H:%M:%SZ                      # ★ FIRST. Never assume the clock.
python docs/ops/loginnode_guard.py --once        # UCL penalty check (exit 3 = UNKNOWN, P279)
tail -5 docs/ops/watch/CYCLE_LOG.md
python docs/ops/session_preflight.py --full      # ~200 s; 0 clear · 1 ATTENTION · 2 FAIL
.venv/Scripts/python.exe docs/ops/line_balance.py --once
.venv/Scripts/python.exe docs/ops/arm_jobs.py                    # ★ NEW (P276): the per-ARM join
.venv/Scripts/python.exe docs/analysis/record_seed_completeness.py   # THE COMMON BANKED RUNG
.venv/Scripts/python.exe docs/analysis/instrument_agreement.py       # ★ NEW (P288)
.venv/Scripts/python.exe docs/analysis/science_plausibility.py       # ★ NEW (P285)
bash docs/ops/run_record_layers.sh               # 7 layers + 3 measurements
ssh -o BatchMode=yes myriad "hostname"
```

Then say **"Resuming from: … — next: …"** and CONTINUE. **Never ask "what now".**

---

# §2 ⛔ MANDATORY READING — ZERO GAPS IN KNOWLEDGE IS A REQUIREMENT

| file | why |
|---|---|
| **this file** | the brief |
| **`docs/ops/watch/FLAWLESS_LEDGER.md`** | ⭐ **THE CONTRACT FOR THE 30-MIN LOOP.** Fixing is mandatory; the three terminal states; what is NOT a defect; the SPEED component; **the THROUGHPUT IDENTITY (§ "why are we not at 300 rec/h")**; **the ETA-AT-GLOBAL-MINIMUM proof**; every OPEN row. **READ IT IN FULL BEFORE EVERY PASS.** |
| **`CLAUDE.md`** | LAW. the ★ PRIORITIES, the four authorities, Okhrati's D1–D6, Stefan's S1–S11, the 95+ doctrine, the human register |
| **`PREREGISTRATION.md`** | THE FROZEN CONTRACT. **R101** (read the amendment row IN FULL — it defines the population as *the confirmatory Opus + all 10 legs*, sets the exogenous stop, AND at point (4) **licenses interim rungs as draft-filling, not inferential looks**), **R111**, **R115**, **Amendment E1** |
| **`docs/HANDOFF.md`** §1–§3 | current state + the authority map |
| **`CHANGELOG.md` `[2026-08-04d]` + `[2026-08-04e]` + `[2026-08-04f]`** | RUN 20 in full, across three blocks |
| **`docs/DEFERRED_FIXES_RUN4.md`** | every known-open defect, **now including D37–D48, the full fenced-module register** |
| **`docs/ops/MAINTENANCE_2026-08-12.md`** | ⚠ **UCL OFFICIAL — WED 12 AUG, MAY RUN INTO THU 13. READ EARLY.** |
| **`memory/session-current-focus.md`** ▶ NOW | the live cursor |
| **`docs/analysis/science_plausibility.py`** | ★ NEW. Read its docstring: it explains why the seven layers cannot answer "is this nonsense" and how the **blindness guarantee is enforced in code** |
| **`docs/ops/arm_jobs.py`** | ★ NEW. Read why `qstat` truncation made RUN 19's stated design unbuildable |
| **`docs/analysis/instrument_agreement.py`** | ★ NEW. The P259 test applied to a comparison: every row states an EXPECTED RELATIONSHIP |

### ⛔ THE READING GATE — answer these FROM THE SOURCES before acting
1. What are the **four authorities**, and what happens when they conflict?
2. What is **H2**, and why is the fed tail **ENDOGENOUS**?
3. What is the **COMMON RUNG**, and **exactly which units is it defined over?** (R101's own words)
4. **Why can an arm hold 567 perfect records and bank rung 189?**
5. Why must you NEVER read a treatment arm's sealed-test outcome — **and what does R101 point (4)
   permit that people assume it forbids?**
6. Which paths are **drift-fenced**, and what does editing one cost?
7. **What is the measured throughput CEILING, and why is 300 rec/h not available?** (§6)
8. **Why is the ETA at its GLOBAL MINIMUM, and what single measurement proves it per job?** (§6)
9. What is the difference between a record COUNT and a BANKED RUNG?
10. **What is D37, and why is its SILENT variant invisible to every instrument?**

---

# §3 STATE AT HANDOVER (2026-08-04 ~16:00 UTC, T+163h)

```
records ~13,400 · spend $45.5019 · drift 0 · sci OK · freeze 3ca6f01ab772 MATCHES
preflight VERDICT OK all 17 · 7 record layers RC=0 · line_balance CLEAN · Eqw/hqw 0
disk 39.0 GB · free RAM floor 6.3 GB (was 0.14) · slots ~1,870 · queue 18 · 10/12 up, 2 COMPLETE
cycle verdict OK on every line since 14:59Z (27 of the previous 30 read ATTN)
exogenous stop 2026-08-27 · backup branch: backup-2026-08-04-run20 (pushed, current)
```

### THE LADDER — banked rungs (S15/C6). **THE COMMON RUNG IS 0.**
| line | rung | what caps it |
|---|---:|---|
| gemini-2.5-flash · h3_singleshot | **568** | COMPLETE (h3 is EXCLUDED from the minimum, P287) |
| gpt-5.6-luna | **189** | holes at seeds **192/193** on five arms — **repair job 83464 RUNNING** |
| qwen3.5-9b | **100** | climbing |
| sonnet-5 · haiku · qwen3.6 | **30** | sonnet deep in a C4 sweep (371→383 records in one session) |
| kimi | **0** | `h2_pair` at 12 seeds, RUNNING |
| **core · deepseek · glm · nemotron** | **0** | the `h2_pair` every line tests LAST |

**⇒ CORE BINDS. `bayes_opt` owes 2 of 30, `tpe` owes 2, chain floor 0.37 d. Core reaches rung 30 in
~18 h from 15:45Z, i.e. around 2026-08-05 09:00–10:00 UTC.**

---

# §4 ⚠⚠ THE SIX THINGS THAT WILL MISLEAD YOU

**① THE COMMON RUNG IS 0 AND THAT IS THE HONEST STATE.** Four lines have not begun their headline
arms. Do not "fix" it.

**② A FLAT NUMBER IS NOT A STALLED NUMBER — AND THE CHECK THAT SETTLES IT IS `cpu` AGAINST `wall`,
NOT ELAPSED TIME.** `c1_bayes_opt_c27` read "owes 3" for five consecutive passes, which is exactly
the shape of a stall. `qstat -j` gave `cpu=98:43:36` on 8 slots = **ratio 8.01, every thread flat
out.** It completed at 12.37 h. **Elapsed time alone supports either conclusion; the ratio settles it
in one command.**

**③ ~94% OF THE FLEET'S OUTPUT CANNOT RAISE THE RESULT.** sonnet-5 is the one line deep in a C4
sweep and everything it produces lands ABOVE the common rung. High concentration is the design
working, not an imbalance.

**④ AN ALARM CAN BE REPORTING AN INCIDENT THAT ENDED HOURS AGO.** Tamer saw *"worst streak 21/240
(8.8% to fatal)"* and reasonably read it as live. It was the tail of a 31-minute outage that had
ended **2.3 hours earlier**. The row now carries the AGE (P292). **Before acting on any alarm, ask
what its timestamp is.**

**⑤ `qstat` TRUNCATES THE JOB NAME TO TEN CHARACTERS** (`leg8_leg_s`). Any census needing the arm
token or a complete state distribution must use **`qstat -u ucestes -xml`**. `line_balance` also
counts ONLY exact `r` and `qw`, so live `Rq` jobs are invisible to it.

**⑥ THE STATUS PAGE AND THE LEDGER CAN GO STALE.** Three of five WATCH rows were stale and one was
wrong in DIRECTION. **Re-measure every row you quote.**

---

# §5 ⛔⛔ MYRIAD MAINTENANCE — **WED 12 AUG, AND IT MAY RUN INTO THU 13 AUG**

`docs/ops/MAINTENANCE_2026-08-12.md` carries UCL's official notice. **Expect dispatch to stop ~15 h
before, from ~17:00 Tue 11 Aug, and `records=` to flatten BEFORE the 12th. That flattening is
CORRECT.** A two-day outage exceeds every death clock (TEST 12.0 h, SEARCH 3.0 h) — drivers WILL die
and supervisors WILL relaunch into a dead cluster. That is E1/E2, expected, no data lost.
**Position: RIDE IT.** Aug-11 pre-window checks are in the playbook §3.

⚠ **AND A NEW HAZARD RUN 20 DIAGNOSED — INC-1, THE VPN POOL.** On 2026-08-04 12:00:29Z every Myriad
login node began refusing SSH with `kex_exchange_identification: Connection reset by peer` **while
TCP port 22 was OPEN on all three** and UCL HTTPS was reachable — so not a route loss, not a network
outage, and not a usage penalty. **Our VPN address was `10.151.114.155`, and `acknowledged_alarms.txt:340`
records the identical event from the identical /24.** Tamer reconnected onto `10.151.110.107` and the
next `ssh` succeeded, 31 m 50 s after it began. **⇒ IF EVERY LOGIN NODE REFUSES WHILE TCP:22 IS OPEN,
CHECK THE VPN POOL FIRST AND ESCALATE A RECONNECT TO TAMER. It is the fastest fix and it is his to
make.** The mechanism is still unexplained: a lost route cannot produce a completed TCP handshake.

**COST OF THAT OUTAGE, MEASURED AFTER RECOVERY: NONE.** The backlog flushed (+100 records in 28 min),
the 12 h rate came back ABOVE its pre-outage value, the fleet never shrank, and **the slot count ROSE
during it** because SGE kept dispatching already-queued jobs. **A transport outage stops us WATCHING
the campaign, not the campaign.**

---

# §6 ★★★ THE ETA AND THE THROUGHPUT — **MEASURED END TO END BY RUN 20. DO NOT RE-LITIGATE; RE-READ.**

## §6.1 THE THROUGHPUT IDENTITY (Tamer asked *"why are we not even at 300 rec/h?"*)

> **records/hour = slots × utilisation × yield ÷ T_training**

Every term measured from `ledger/*.epilogue.jsonl` (3,235 tasks) and the cycle log's `cores=` history.

**T IS NOT ONE NUMBER, AND POOLING IT IS THE FIRST ERROR.** By phase, successful tasks only:

| phase | n | median | **mean** |
|---|---:|---:|---:|
| SEARCH (DFO), 8-thread | 1,580 | 4.24 h | **4.64** |
| **TEST / sweep (C4)** | 1,123 | 9.45 h | **9.52** |
| TEST / per-arm · baselines · h2_pair | 318 | ~9.0 h | **~8.9** |

```
S = 1,879 slots   y = 0.9972 (TEST yield)   T = 9.39 h
CEILING  = S*y/T = 199.6 rec/h        MEASURED 167-180 = 84-90% utilisation
SLOTS NEEDED FOR 300 rec/h = 2,824    =  1.50x what we hold
```

**300 rec/h was never physically available** on a cluster where we already hold ~20% of every
running slot across 98 users. **T IS FROZEN DESIGN:** 400,000 steps at ~13 steps/s/core is 8.55 h of
pure compute; the observed 9.39 h is that plus ~10% overhead. Reducing it means changing a registered
value or the thread count, and the second breaks CRN determinism.

**YIELD IS NOT THE PROBLEM AND THE APPARENT FAILURES ARE THE SCIENCE.** TEST-phase yield is
**99.72%**; the 194 `rc=1` tasks sit **entirely in SEARCH** and complete in **0.00 h** — they are the
sandbox rejects that authoring reliability MEASURES. The one expensive failure mode is `rc=126` at
exactly 15.00 h, the `h_rt` wall: 20 tasks, ~300 slot-hours.

## §6.2 THE ETA IS AT ITS GLOBAL MINIMUM — PROVEN PER JOB

```
CRITICAL   running  24 jobs /   192 slots      queued   0 jobs /     0 slots
NON-CRIT   running 218 jobs / 1,744 slots      queued  70 jobs /   560 slots
```

**Not one job on the critical path is waiting for a slot.** Adding capacity to a path with nothing
queued on it does nothing. The "our own jobs block our critical path" hypothesis is **REFUTED by
measurement.** And by pass 17 the queue had fallen to **18 against 231 running** — the scheduler
takes everything we submit essentially on arrival, so **there is no fair-share wait left to recover.**

**⇒ THE ETA IS BOUND BY A SERIAL DEPENDENCY CHAIN AND BY NOTHING ELSE.** The only lever that exists
is the human one — asking UCL RC for a larger allocation — and even that cannot help, because the
critical path is not slot-starved.

## §6.3 ⛔ WHAT IS CLOSED AND MUST NOT BE RE-LITIGATED

Cores/fair-share (fourteen measurements + the per-job proof) · 400k steps FROZEN · warm-start breaks
determinism · more threads corrupt CRN · re-packing costs more than it saves. **Never trade
correctness, CRN determinism or the frozen design for speed.**

**★ HOW TO MINIMISE THE ETA IN RUN 21, concretely:** the only things that move it are (a) core's
`bayes_opt`/`tpe` finishing their last 2 candidates each, and (b) the four capping lines' `h2_pair`
landing. **Watch them; do not accelerate them, because they cannot be accelerated.** What you CAN do
is make sure nothing STOPS: a driver death, a wall-killed candidate needing a repair round, or a
transport outage. **Speed work in RUN 21 = protecting the critical path, not enlarging the fleet.**

---

# §7 ★★★★★ THE LESSONS RUN 20 EARNED — read these before you write any code

> ### ① SEND AN AUDITOR AT YOUR OWN FIXES. **SIX of RUN 20's most serious findings were about RUN 20's own work that same day.** Four structural defects in fixes made hours earlier, a false claim printed on the live board, and a class fix that missed three members of its own class. The author cannot see these.

> ### ② A SURPRISING NEGATIVE IS A CLAIM ABOUT YOUR OWN SCRIPT FIRST. Earned FOUR times in one session: the pack-tail hypothesis (refuted — 3,209 of 3,222 ledgers hold ONE task), the pooled-T hypothesis (refuted), the self-inflicted-queue hypothesis (refuted), and **16,698 "violations" from a new auditor of which every single one was mine** (cash residual, a RETURN field read as an exposure, baseline NaN by design).

> ### ③ A CLASS FIX IS ONLY AS COMPLETE AS THE POPULATION YOU DREW IT OVER. I fixed "zero is not clean" across the seven gated layers and missed three other tools that walk the archive — **because I enumerated the LIST, not the PROPERTY.**

> ### ④ VERIFY THE ARTEFACT, NEVER THE PATCH SCRIPT'S EXIT CODE. Three times in one session: a patch that reported `OK` while REMOVING a call it was meant to relocate; a docstring repair that mangled three files; and a falsification harness fooled by **argparse's usage-error exit 2, the same code a vacuity guard uses.**

> ### ⑤ A FALSE REASON IS WORSE THAN NO REASON. I asserted a marker "CANNOT clear" and printed it on the board every cycle. An auditor read all 2,072 lines of `campaign.py` and refuted it; I verified first-hand and the claim was simply wrong. **The FIX was right and the REASON was false — and the next session would have reasoned from the reason.**

> ### ⑥ A STATISTIC WITH NO TIME ATTACHED CANNOT BE ACTED ON. A "worst streak" with no age read as live for 2.3 h after it ended. A re-triage trigger stated on a monotone counter can never NOT be breached. A CUSUM quoted as a trend was process-local and reset 15 times.

> ### ⑦ READ THE REGISTERED TEXT BEFORE ESCALATING A "DESIGN QUESTION". A6 was escalated to Tamer and Dr Okhrati as a pre-registration question. **R101 answers it in its own words.** It was an instrument bug, and the correct action was to make the instrument match the freeze.

> ### ⑧ RE-READ THE CLOCK AFTER ANY LONG WAIT. Elapsed time is not observable from inside a turn.

> ### ⑨ WRITE TO A FILE. ALWAYS. Heredocs and inline quoting have now failed **eight times across five sessions.**

---

# §8 STANDING RULES THAT MUST SURVIVE THIS HANDOVER

- **NEVER** read a treatment arm's SEALED-TEST outcome for INFERENCE. ⚠ **But know what R101 point
  (4) actually permits** — see §11.4.
- **NEVER** change a frozen threshold. **NEVER** make a check pass by weakening it.
- **NEVER** add Claude/Anthropic attribution. **Tamer is sole author.**
- **NEVER** `git clean -x`, `git add -A`/`-u`, or `git stash`. **Stage BY NAME.**
- **NEVER** leave files staged — an auto-committer runs every ~2 min.
- **NEVER** lower SGE priority; never `qdel -u`. Explicit ids only.
- **NEVER** edit `src|scripts|config|prompts` while live (drift-fenced). `docs/**` is safe.
- **NEVER** put backticks, `$(…)` or heredocs in a `bash -c` string or a `-m` commit message.
- **⛔ NEVER JUNCTION THE ARCHIVE.**
- **PowerShell console is cp1251** — non-ASCII in `print()` CRASHES.
- **⚠ Editing a running loop is INERT** — `cycle_loop.sh`/`publish_loop.sh` need a RESTART;
  `cycle.py`/`publish_status.sh` are re-invoked each iteration and do not.
- **⚠ NEVER EDIT A RUNNING BASH SCRIPT** (bash reads by byte offset).
- **For anything invoked every few minutes: WRITE A CANDIDATE, PARSE IT, LINT IT, DIFF ITS SYMBOL
  SET, THEN REPLACE ATOMICALLY.** And check that every name it uses is BOUND on every path — `ruff`
  and `ast.parse` do NOT catch an unbound local.
- **END-OF-WORK, all four:** `scripts/update_handoff.py --suite-status "…"` · a SHORT cursor ▶ NOW
  entry · a DETAILED CHANGELOG block even with no commits · push the backup branch.

---

# §9 EVERY DEFECT RUN 20 FIXED (P275–P297) — **allocate P298 next**

**Instrument defects (17):** P276 **A-d14 closed** — the per-arm job join, unbuildable as RUN 19
described it because `qstat` truncates job names to 10 chars · P277 P4 certified `wall_clock` over a
tier it never examined (**11,154 of 12,693 records**) · P278 a re-triage trigger on a monotone
counter · **P279 the login-node guard returned "comfortable" with ZERO BYTES through a live outage** ·
P280 `science_watch` 5,776 → 667 MB · **P282 S10 and S15 disagreed about THE REPORTED RESULT (12 vs
0), the gated one reading high** · P283 R8 passed silently on **every NaN** · **P285 nothing anywhere
asked whether the numbers were physically sensible** · P286 six gated layers certified an EMPTY
archive · **P287 A6 closed against R101's own text** · P288 the instrument-agreement checker ·
**P289/P290/P291 the OOM trajectory closed: 20.7 GB → 2.05 GB** · P292 a "worst streak" with no age ·
P293 a D14 ATTN that had fired for 42 h on a self-healed crash · P296 the vacuity class completed.

**MY OWN ERRORS (6, and they are the important ones):** **P275** a process identified by its START
TIME, not its command line · **P284** 16,698 false findings in one run · **P294** four structural
defects in my own same-day fixes, one a LIVE false-alarm generator · **P295** a false reason printed
on the board every cycle · **P296** a class fix over the wrong population · **P297** a cache key that
omitted the half of the input that changes, after I told Tamer it was not a weakening.

---

# §10 ⚠ OPEN ITEMS

**ESCALATED TO TAMER (2):**
* **E-spend** — `spend=$45.5019` is **$8.7603 realized + $36.7418 estimated**, and the CONFIRMATORY
  line is **100% estimate**. **The dissertation must state the split, never the pooled figure.**
  A write-up decision, not an ops patch.
* **E-sent / W1-D36** — `sentinel.py`'s blind spots (a glob matching 0 of 42 ledgers; a rate family
  covering 1 line of 12; six checks reading inputs that do not exist; 58 frozen records collapsed to
  11) and the structurally-permanent `gate_failure_drift` CUSUM. **`scripts/` is drift-fenced.**
  **Campaign exposure assessed: every blind spot is covered by an unfenced instrument.**

**THE DEPLOY-WINDOW REGISTER — `docs/DEFERRED_FIXES_RUN4.md` D37–D48**, from a read of **100% of
`driver.py` and `campaign.py`**. Headed by:
* **D37 (CRITICAL)** — an arm that fails SELECTION vanishes from C2 **and** C4 with no log, no marker
  and no summary field. **Its LOUD variant is covered** (the C3 gate reads `present=0/30` and the
  supervisor deliberately does not relaunch); **its SILENT variant — an arm with complete core
  records whose selection later fails — is covered by NOTHING**, and it is a silent cap on the
  COMMON RUNG.
* **D39** — the shared throttled puller **returns SUCCESS without pulling**, which resets the
  consecutive-failure counter AND the 12 h outage clock on a non-pull, and can let a resume
  **re-author PAID candidates** off a mirror the process never refreshed.
* D38 · D40 · D41 (each MAJOR) · D42–D48.

**Inherited disclosures (unchanged):** D34 · D35 · **R115 PROVISIONAL for 3 of 10 core groups —
RE-RUN BEFORE SUBMISSION** · `campaign_summary.json` at teardown UNRECOVERABLE ·
`metrics.train_curve.return` 100% NaN · **`wall_clock` is 0 on the ENTIRE sealed-test tier — the
compute is recoverable from `ledger/*.epilogue.jsonl` (`secs`), and the measured per-training wall is
9.39 h mean / 9.45 h median** · **S4 determinism is VACUOUS in this archive — use the 30/30 farm.**

---

# §11 ★★★★★ THE 30-MINUTE LOOP — **PRESERVE IT, AND GO DEEPER**

**Re-arm it immediately.** RUN 20 used the harness's own cron at `7,37 * * * *`, session-scoped. The
prompt is the STEP 0–6 contract below, enforced by `docs/ops/watch/FLAWLESS_LEDGER.md`.

### §11.1 THE CONTRACT, UNCHANGED
**Every finding is FIXED.** A pass does not end while a fixable row remains open. Exactly three
terminal states: **FIXED** (falsified against pre-fix behaviour), **PROVEN-BENIGN** (with the
measurement), **ESCALATED** (outside this session's authority, with everything around it fixed).
**No row may age three passes.** **Never make a check pass by weakening it.**

### §11.2 THE STEPS
**0 CLOCK** · **1 BOARD** (every tool in §1; read each tool's OWN verdict, never a pipe's exit code)
· **2 DEEP DIVE** (§11.3) · **3 EVERY RECORD** · **4 SPEED** (append the row; compare; a material
drop is an OPEN FINDING worked to a cause) · **5 FIX** (falsify; verify a copy, move it in
atomically) · **6 RECORD** (ledger row, SPEED LOG, and anything a future session must not rediscover).

### §11.3 ★ WHERE RUN 21 MUST GO DEEPER — the seven instruments RUN 20 audited are DONE; these are not

1. **⭐⭐ `scripts/analyze_campaign.py` — THE HIGHEST-VALUE UNAUDITED TARGET IN THE REPOSITORY.** It
   produces THE RESULT, it runs ONCE at teardown, and **it has a KNOWN OPEN DEFECT** (`LOADER-POOLING`:
   its loader pools LEG records into CORE arms and would present a confirmatory H2 verdict computed
   on other models' data that *looks* complete and balanced). It is drift-fenced, so the deliverable
   is a REGISTER plus, where possible, an unfenced detector under `docs/`. **Audit its 35 `out[...]`
   keys the way RUN 20 audited the layers: what population does each exclude, can each ever change
   state, does it agree with the archive.**
2. **`src/inference/**` — the statistical machinery** (DSR, PBO, TOST, the bootstrap, FDR). Never
   audited. Read-only. Every confirmatory number passes through it.
3. **The remaining `docs/ops/` monitors**: `crash_watchdog`, `sandbox_gap_watch`, `budget_watch`,
   `myriad_watch`, `ssh_gate`, `disk_runway`, `status_stage`, `arm_coverage`, `publish_status.sh`
   (⚠ a running bash script — never edit it live).
4. **THE BLIND QUALITY REPORT Tamer was promised** — per-leg completeness, hole positions, seed
   coverage, wall-clock from the epilogue ledgers, authoring reliability, reward-code taxonomy, R115
   exclusions, determinism evidence. **Everything except the outcome values.** `science_plausibility.py`
   is the pattern to follow, including `_assert_blind()`.
5. **The MEMORY profile of everything else that walks the archive.** RUN 20 measured the layers
   (<55 MB) and the three big tools (now ~680 MB each). **Nothing else has been measured**, and
   P270's projection was built on the wrong population twice.
6. **`science_plausibility.py`'s own body and `instrument_agreement.py`'s** — the first auditor
   explicitly did NOT read them in full, and **`_assert_blind()` is load-bearing for effect-blindness.**
7. **Cross-instrument agreement, extended.** `instrument_agreement.py` covers four quantities. Add
   the job census, the spend total, the arm rosters at every consumer, and the seed frontier.

### §11.4 ⚠⚠ THE BLINDNESS POSITION — READ THIS BEFORE ANYONE ASKS TO SEE RESULTS

Tamer asked to see the finished legs. **RUN 20's answer, and it is the settled one:**

* **The confirmatory result does not exist yet.** The headline H2 contrast is on the CORE line, and
  core's `distributional`/`scalar` hold **zero** sealed-test records.
* **R101 point (4) EXPLICITLY LICENSES INTERIM LOOKS**: *"the first checkpoint (30 seeds, all 11)
  fills Results as PROVISIONAL … interim rungs are draft-filling, NOT inferential looks (the
  calendar, not results, sets the rung)."* **Looking at rung 30 is REGISTERED BEHAVIOUR, not a
  deviation, and needs no disclosure.**
* **What is forbidden** is treating an interim look as inferential, or letting it change data
  collection. The stop is the calendar, the ladder is registered, and after P287 the population is
  registered too — **so there is no discretionary lever left for an outcome to bias.**
* **The recommendation given to Tamer:** wait for the common rung to reach 30 (all eleven, balanced),
  then look. Looking earlier gives a *worse* picture — two of twelve units, one of them a control.
* **If he asks again, this is his study.** Read what he names, report it exactly, and record the date
  and scope. Do NOT lecture him. **Do NOT read it on your own initiative.**

### §11.5 ★ THE SPEED COMPONENT — every pass
Record the SPEED LOG row (rec/h 12 h and 24 h · slots · running/queued · single-line concentration ·
chain candidates owed · ETA to rungs 30/403/568 · days to the Aug-27 stop). **Compare to the previous
row. A drop is an OPEN FINDING worked to a cause.** But read §6 first: **the ETA is not core-bound,
it is serial-chain bound, and the queue is already empty.** The genuine work is **protecting** the
critical path, not enlarging the fleet.

---

# §12 HARNESS LIMITS — MEASURED, NOT ASSUMED

```
qdel <explicit ids>                  WORKS      Stop-Process -Id <pid> -Force   WORKS
qstat -u ucestes -xml                WORKS  <- REQUIRED for arm tokens and full states
New-Item -ItemType Junction          WORKS      Set-ScheduledTask               WORKS
qalter -p <negative>                 PERMITTED, DELAYED, ONE-WAY (prohibited by standing rule)
qalter -p <positive>                 OPERATOR ONLY        qalter -l    REFUSED SITE-WIDE
qalter -js                           REFUSED (js absent from jsv_allowed_mod)
taskkill /PID                        BLOCKED    HKLM registry write             BLOCKED
bash `kill <windows-pid>`            NO-OP (Git Bash has its own pid namespace)
git commit --only <path>             WORKS, and is REQUIRED in publish_status.sh
```
**⇒ STANDING RULE: TEST THE SPECIFIC COMMAND. Five "BLOCKED" claims have been disproved.**

---

# §13 THE ONE SENTENCE TO CARRY

**Ten auditors across ten sessions have all found more than the author did, and in RUN 20 SIX of the
most serious findings were about RUN 20's own fixes made hours earlier — including a false reason
printed on the live board every cycle. Send an auditor at YOUR OWN WORK before you bank it, falsify
every fix against the behaviour it replaces, verify the ARTEFACT rather than the patch script's exit
code, and remember that a class fix is only as complete as the population you drew it over.**
