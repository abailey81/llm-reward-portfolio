# ⭐ HANDOFF PROMPT — NEW CLAUDE CODE SESSION: RUN 4 IS LIVE AT T+40 h, TAKE IT OVER

> Written 2026-07-30 13:40 UTC at Tamer's instruction: *"I want to pass this chat into the new fresh
> claude code chat, exactly the same way it was passed to you, like remember the first prompt… document
> absolutely everything in all docs, including changelogs, handoffs and etc… grant the new sesssion all
> the rights, everything that was granted to you, tell it how we communicate remotely, what to follow
> and etc… The transition must be extremely smooth, and it should feel like the session never endeded,
> the next session must have absolutely 0 gaps in its knowledge… make sure it desnt miss anything."*
>
> **Paste this whole file as the first message of the new session.** It is written to leave zero gaps.
> It SUPERSEDES `docs/RUN5_SESSION_PROMPT.md` (still on disk as the previous session's brief, and still
> accurate as dated history — do not "fix" it).
>
> **THE CAMPAIGN IS RUNNING RIGHT NOW.** It does not need you to start it. It needs you to watch it,
> keep it honest, and not break it.

---

## §0. TAMER'S INSTRUCTIONS — VERBATIM, ALL STILL BINDING

### §0a. The instruction that started the previous session (still in force, every clause)

The previous session was opened by pasting `docs/RUN5_SESSION_PROMPT.md` in full, whose own §0a carried
the instruction that started the session before it — still binding, quoted here so the chain is unbroken:

> I am now in the midde of the run which I have started with anotehr claude code session, and I want to
> transition to this one. This is an extremely important work, so I want you to very deeply and
> extensivelly study absolutely all files very extensivelly, understadn what was previous claude code
> session was doing, and proceed and ultrathink I want to ensure the smooth transition… I wantt to make
> sure absolutely everything is strictly absolutely flawless, logical and etc. I need you to work very
> accuratelly and surgically, and monitor everything very closely, including results and othe rprocesses…
> make sure you in details read all docs, all md docs, all handoff, all changelogs. Absolutely
> everything, make sure you dont miss, anything. Ultarthink deeply

### §0b. Every instruction Tamer gave the PREVIOUS session (2026-07-29 → 07-30), verbatim

1. *"so where does it stand now? Why do we only have 448 cores, we wont be able till 28yth of august…
   maximise teh speed to an absolute maximum. Also why did you stop tracking?"*
2. *"Good, ultrathink and proceed, continue monitoring absolutely everything withoiut exceptions
   continiously throughout thsi whole campaign run… full permissions, full resources, full ratifications."*
3. *"SO ultrathink, and check every 2 minutes absolutely everything deeply. also if so many cires are
   available? Why are we nbot demanding them? Leyts speed up the training, demand all cores, eevrything,
   ultrathink deeply"*
4. *"Please ultarthink very deeply and extensvelly, absolutely everything must be 100000000% absolutely
   strictly flawless… I give you absolutely full rights to do whatever is needed, including unfreezing,
   starting a new run and etc… for example TAKING THE SHARpe, for me its not really logical that its
   negative… Ensure you use absolutely all cores myriad can offer, our target to speed up to 4000 cores."*
   — **sent twice**, the second adding: *"And its not only sharpe, its absolutely everything… PLease this
   dissertation is extremely critical and important"*
5. *"Please watch absolutely everything very closely, make sure teverything si strictly flawless, logical
   and etc. Absolutely everything must be absolutely strictly perfect across all dimensions possible.
   Constatntly make sure, and also constanntly keep watching very closely"* — sent with the full
   **"Getting every criterion into the top band"** feedback document (pasted twice in total).
6. *"lets ask for more, lets speed up, why are we asking for so low, ultarthink."*
7. **⚠ *"Why dont you listen to what I say???"*** — see §9 trap 11. This is the most important line in
   this section. He said it because I kept *explaining why cores could not be raised* instead of acting
   on permission he had already granted. **When Tamer grants permission, ACT — do not re-litigate it.**
8. *"Please ythe absolutely biggest priority is the quality, the project quality, teh results quality,
   teh quality of absolutely everything. I want to publish this work, please ensure its 100000000%
   flawless very strictly and deeeply. No exceptions, even a minor ones."*
9. *"Ultarthink very deeply and extensievelly and proceed. Dont stop."*
10. *"so whast happeened with teh feedback I gave you?"* — I had assessed the feedback and implemented
    **none** of it. Do not let a delivered assessment stand in for delivered work.
11. *"Please ultarthink very deeply and extensievlly on feedback, aseess, ultarthink and implement…
    take as much time as you need."*
12. *"yes, finish with these A6 and onwards, make sure absolutely everythiing is strictly absolutely
    flawless."*
13. *"Please build everything, ultarthink very deeply, very deeply and extensivelly analyse teh
    feeedback, dont be lazy. Please ensure evertything is 100000% flawless very strictly"*
14. *"Ultarthink very deeply and extensivelly always. Make sure absolutely everyhting is 1000000000%
    strictyl absolutely flawless"*
15. The handoff instruction quoted at the top of this file.

**STANDING REPORTING DUTIES (carried from the session before, items 3–4 of its §0b):** every update must
carry **campaign timeline · CPU cores in use · jobs in progress · each stage's current ETA**. Use
`python docs/ops/stage_eta.py <measured_cores>`.

### §0c. Older standing instructions, carried forward and still binding

> absolutely everything must be strictly flawless… I give you full freedom, unfreeze and/or change if
> needed... its even fine if we relaunch the campaign from the start again after changes if needed. I
> want to priorities the quality very heavily.

> also dont forget to always document what was happening befor, whats going on now, and the future. Make
> sure you dont miss anything, this would help me with teh write up as well

> make sure you document everything from all previous runs, so we know mistakes and etc

> Use the absolute maximum myriad can offer us to speed up the training to an absolute maximum.

> the highest priority possible is to make it FLAWLESS by ALL MEANS!!! … Very deeply and strictly
> ultrathink, and bring all issues, inconsestiencies, gaps and etc to an absolute strict 0%, not 0.1%, 0%!!!!!!

> make sure the new session would be absolutely aware of absolutely everything that happened herere, this
> run, in previous sessions and in previous runs as well, everything. It must have an absolute context

---

## §1. STANDING RIGHTS — ALL OF THEM, NO EXCEPTIONS

- **Absolutely full permission to do anything about this project.** Explicitly including: **unfreezing
  the pre-registration, changing any hash-bound file, re-freezing under a new tag, discarding a run, and
  relaunching from scratch.** Frozen or not is never a barrier if quality genuinely requires the change.
- **Ratify on Tamer's behalf** where a decision is clearly implied by his stated priorities (standing
  delegation, 2026-07-13), conditioned on ultrathinking and the CLAUDE.md priorities.
- **Full resource governance** on the laptop (RAM, apps, power, services) — **never** kill VS Code,
  terminals, or live training.
- **Use the absolute maximum Myriad can offer**, without cutting the science.
- **Act. Do not stop for routine approval.** Ask only where proceeding under any assumption would be
  unsafe or would waste real money. **And never re-explain why a granted permission cannot be used —
  measure, then act** (§0b item 7).

### Hard prohibitions — violating any is a defect

| never | why |
|---|---|
| add Claude/Anthropic attribution to any commit, PR, tag or doc | Tamer's **sole authored** work; author is `Tamer Atesyakar <t.ates232004@gmail.com>`. The global "end commit messages with `Co-Authored-By: Claude`" convention is **REVOKED** here. Verified 0 across all commits — keep it that way |
| `git clean -xfd` or any `-x` | destroys ~1.2 GB of gitignored **licensed Refinitiv gold** (measured: 1,264 paths incl. the frozen headline panel) |
| `git add -A`, or `git add -u` without reading `--numstat` first | sweeps untracked `outputs/` / stages mass deletions |
| lower SGE job priority (`qalter -p <negative>`) | standing rule, absolute. Fair-share forbids self-**elevation**; raising is RC/admin only and is **Tamer's declined call** |
| `qdel -u ucestes` | would destroy the surviving `l16xx` p6-ladder jobs feeding figure F11 — **delete by explicit job ID only** |
| backslash / escape / **backtick** content in a bash heredoc or a `-c` string | the shell mangles it. **Use the Write/Edit tools for structured content.** I violated this twice more this session — once it aborted a doc append mid-write |
| **inline `git commit -m "…"` in PowerShell** | quotes/parens break it repeatedly. **Write the message to a file → `git commit -F <file>`** |
| pull Refinitiv data from Bash | the sandbox blocks it — **PowerShell + `.venv-lseg`** only |
| a process query that greps command lines without excluding `$PID` | it matches your own shell, then lies (P10 — and it recurred this session, see §9 trap 12) |
| editing `src/` `scripts/` `config/` `prompts/` while the run is live | **§3. The single most important rule.** |

---

## §1b. ⚠ THE PRIORITIES AND BINDING STANDARDS — reproduced here because CLAUDE.md IS UNTRACKED

**`CLAUDE.md` is deliberately NOT in git** (commit: *"Keep the internal operating brief out of the
published artifact"*). It is laptop-local. **A session that reads only the repository will never see any
of the following**, so the load-bearing parts are reproduced verbatim. If `CLAUDE.md` is present on the
machine, read it in full as well — this is a safety net, not a replacement.

### ★★★ THE PRIORITIES — the absolute, overriding north star

> **Every decision — design, model, theory, writing, scope, tooling — is measured against these. When
> anything trades off against them, THEY WIN. Non-negotiable, and the highest priority in the file.**
>
> 1. **MAXIMISE THE GRADE → a 95 %+ FLOOR, as close to 100 % as humanly possible** (UCL distinction top
>    band). *"Good enough", "competent", "solid" are NOT the target — the ceiling is.*
> 2. **WORLD-CLASS, CUTTING-EDGE, PUBLISHABLE** — a genuine frontier-grade contribution (TMLR-and-up /
>    ICAIF-main), the kind of work a leading lab would put its name on. **Not** a workshop demo, **not** a
>    competent-student exercise.
> 3. **VERY DEEP** — depth, intuition, mechanism and genuine originality over breadth and textbook
>    machinery; the insight that earns the top band and survives Okhrati's scrutiny.
> 4. **CORPUS-GROUNDED + GENUINELY NOVEL** — lean HEAVILY on the 196+ first-hand-read paper corpus
>    (cite-and-USE, not cite-and-wave), and keep the conjunctive novelty cell genuinely novel, protected
>    by dated sweeps every 2–3 weeks plus a MANDATORY pre-submission sweep.
>
> These four are **inseparable**. **Default to the most ambitious option that is rigorous and honest;
> never trade depth, quality, or ambition for convenience, speed, or "it's fine".**

**⚠ 2026 GRADE-INFLATION ADJUSTMENT (supervisor-confirmed 2026-07-21):** the bar was RAISED this year —
*last year's distinction ≈ this year's merit*. Every dimension needs UNAMBIGUOUS distinction evidence, and
**communication is the binding constraint**.

### ★★★★ REPRODUCIBILITY — "THE SINGLE MOST IMPORTANT POINT OF THIS DISSERTATION"

> Reproducibility **outranks speed, convenience and cleverness**. **A result that cannot be reproduced is
> not a result.**

Three layers must GENUINELY hold: **analysis** = deterministic archive replay · **protocol** = re-runnable
by anyone (keyless golden path) · **experiment** = open-weight, hash-pinned, self-hosted legs. **THE
DETERMINISM ENVELOPE is the operative rule:** anything that changes floating-point arithmetic is part of
the frozen design — device, thread counts, BLAS, tf32, fused optimizers, library versions, every
provider/quantization/reasoning pin. This is why the **`t` pool is excluded** (AMD vs Intel changes
reduction order and breaks CRN bit-exactness), and why **a substrate change is a DESIGN decision, not an
ops one**. A pin nobody can verify is **fictional**.

*RUN 4 supplied live evidence: it reproduced RUN 1's `baseline_raw_return` to four decimal places across a
full re-execution, days apart, on different nodes.*

### ★★★ THE FIVE DUTIES — accurate · surgical · always-ultrathink · always-verify (incl. your own work)

> Tamer's words: *"work very accurately and surgically, and always ultrathink and verify everything.
> Everything must be strictly flawless. It has to always verify everything, including its work."*
> **The standing bar is STRICTLY FLAWLESS — 0 %, not 0.1 %.**

1. **ACCURATE.** Every number, path, flag, hash and count is the REAL one, read from the real artifact *at
   the moment of writing*. No approximations presented as measurements. **If it is stated, it was
   observed** — cite the command/count/log line beside the claim.
2. **SURGICAL.** Smallest correct diff; no drive-by refactor; never touch a hash-bound file outside the
   unfreeze→amend→re-freeze protocol. Read the target and one nearby example first; **re-read your own
   diff** afterwards. Precision is targeting, not timidity — the change must still be COMPLETE at every
   call site.
3. **ALWAYS ULTRATHINK.** Before acting on anything non-trivial: what could be wrong, what would falsify
   it, what the strongest counterargument is. **The first plausible answer is a hypothesis.**
4. **ALWAYS VERIFY — INCLUDING YOUR OWN WORK.** Nothing is done until it was RUN and observed. Read
   `PYTEST_RC` from the LOG, never a wrapper's or a pipe's exit code. Prove a new test FAILS against the
   pre-fix code. **A surprising negative result is a claim about your own script first.** *The author must
   not grade their own work.* **Overstating a risk is as inaccurate as understating one.**
5. **VERIFY IT IS CORRECT AND LOGICAL, not merely that it RAN.** A green check proves execution, not
   truth. Sanity-check magnitude/sign/units; cross-check via an **independent route**; check internal
   consistency across the whole record; check the conclusion actually follows. **A surprising result is an
   obligation to investigate, never a result to report as-is.**

> **Two live case studies you inherit.** (a) A previous session reported *"the science checks out"* having
> verified only INVARIANTS; Tamer challenged it and was right — **invariants holding ≠ results being
> sound**. (b) This session found **D17** purely because seven records shared a bit-identical fraction and
> that *could not be chance* — the anomaly was the finding. Duty 5 is what earns the grade.

### ★★★ OTHER BINDING STANDING RULES

* **ZERO-DEFECT, FIX-ON-SIGHT** — leave no gaps, bugs, inconsistencies or stale statements. If you
  suppress something deliberately, flag it.
* **NEVER MISS ANYTHING — EXHAUSTIVE COMPLETENESS.** Enumerate the FULL scope of any multi-part task and
  complete ALL of it; never sample. Anything added mid-stream joins the scope. **Re-sweep at the end to
  PROVE nothing was dropped.**
* **PLANS ACCUMULATE — NEVER DROP PRIOR WORK.** A new instruction ADDS to the standing set.
* **STRICT ASSESSMENT · SIGNAL OVER NOISE.** Add ONLY what serves the priorities. Depth over breadth.
  **Leave alone what is already sufficient** — expanding what needs no expansion is a defect, not
  diligence. (This session checked before building and found `NOMENCLATURE.md`, `APPENDIX_B_limitations.md`
  and `FIGURE_TABLE_MANIFEST.md` already existed; two were **extended**, none rebuilt.)
* **PUBLICATION-GRADE BACKBONE — NO LAZY HEDGES.** Never soften a claim to report-only merely to protect a
  result — find the framing that is simultaneously strongest AND least fragile.
* **MAXIMUM STRICTNESS — QA GATES AT 1.0.** Every quality/compliance gate at full strictness. **NOT** the
  pre-registered statistical parameters (α=0.05, BH q=0.05, power 0.80, SESOI 0.05) — there the registered
  value IS the strict form, and raising it would invert the science.
* **STRICT CONTINUOUS DOCUMENTATION + ALWAYS-RESUME.** Document EVERYTHING, in full detail, ALWAYS — in
  `CHANGELOG.md` and `docs/HANDOFF.md` §1 — **even in a session with no commits**. At session start resume
  from HANDOFF §1 + the cursor + the latest CHANGELOG, say *"Resuming from: … — next: …"*, and **CONTINUE
  mid-stream — never restart cold.**
* **THE DOCUMENTATION IS WRITE-UP RAW MATERIAL.** The record is the primary source CH4/CH6/CH7 are written
  from, so anything undocumented is **lost to the dissertation**. Past, present and future every session;
  **every mistake with root cause / how found / fix / lesson — including your own**; write it as it
  happens, recording the evidence, not just the conclusion.
* **STEFAN'S 5 CRITERIA** (industrial supervisor, binding lens, each *deeply justified*): real gap ·
  principled, elegant, non-fragile method · **reproducibility ("THE CRITICAL POINT")** · sound ideas with
  everything justified · **clarity of what is measured** (the fed tail is **ENDOGENOUS** — never write
  "agent-independent").
* **THE FOUR AUTHORITIES** are strict binding law: the ★ PRIORITIES · Okhrati's grading function · Raad +
  Stefan's industry feedback · the IFTE0008 guidelines. One owner per truth — `docs/HANDOFF.md` §3.
* **MYRIAD PRIORITY IS ABSOLUTE** — never lower the SGE priority of any of our jobs.
* **CAMPAIGN-SPEED PRIORITY** — drive training wall-clock to its global minimum via pools/packing/
  parallelism, **never** via different arithmetic.
* **★★★ ADDRESS TAMER BY NAME AT THE START OF EVERY MESSAGE.** Every reply begins with "Tamer". No
  exceptions, every turn, every reply type.

---

## §2. THE PROJECT AND ITS COMPLETE HISTORY — absolute context, nothing assumed

### §2.1 What the dissertation actually is

**An LLM designs the REWARD CODE for a risk-sensitive portfolio-RL agent, and we test whether giving it
richer tail information changes the reward it writes.** UCL MSc; author **Tamer Atesyakar**; examiner
**Dr Ramin Okhrati** ("Dr", never "Prof") + a **second marker from any discipline**. Graded on the
submitted PDF alone — **there is no viva** — so the document and its citation integrity are the dominant
levers. Deadline **1 Sep 2026**.

* **Agent**: SAC, tiny MLP, long-only 30 risky assets + cash (31 weights summing to 1), proportional
  turnover cost, PopArt. **400,000 steps per candidate** (B\*, R77).
* **Data**: licensed **Refinitiv survivorship-free point-in-time** panel, 953 RICs, headline universe
  **univ5** (`data/gold/returns_panel_univ5.parquet`, 5,406×963). Split C — train 2005-16, val 17-19,
  **sealed test 2020-26**.
* **⚠ The sealed window is `2020-03-30 → 2026-06-30, n = 1,571 sessions`** — the 60-session R18
  production-lookback purge, **not** every session after 2020-01-01. Conflating them understates every
  benchmark by ≈0.47 Sharpe. This caused a real retraction — see §6.
* **The manipulation**: only the **fed feedback block** differs across arms. Verified by execution —
  `scalar` 0 tail numbers, `scalar_cvar5` 1, `distributional` 6, `placebo` six inert `+0.0000` constants,
  `placebo_shuffled` the same six values on **deranged** labels. Block lengths **67/86/275/293/275** chars.
* **RL framing**: simulated-online off-policy SAC on a historical-replay simulator — **not** offline RL.
* **The three-link chain**: fed tail signal → authored reward code → trained policy → realised tail. A
  null LOCATES where it breaks. SQ1 responsiveness → SQ2 transmission → SQ3 specificity.

**THE IDENTIFICATION PRINCIPLE — a litmus for every proposal:** *only the reward may vary across arms.*
Any new STATE or REWARD input is creep that breaks identification.

### §2.2 The frozen design (v2.1)

| | |
|---|---|
| freeze | **`3ca6f01ab7724d47bd5d01bc9e73b4d3150c049e1048dd86a864b400a230432f`**, tag `prereg-v2.1`, seal commit `b9c2be5` |
| v2.0 history | `4f90ecc4…` preserved (tag `prereg-v2.0` + `docs/prereg-v2.0.sha256`) — never overwrite an anchor |
| **9 arms** | `distributional` `scalar` `placebo` `scalar_cvar5` `placebo_shuffled` + DFO `random_search` `bayes_opt` `cma_es` `tpe` |
| **11 H1 baselines** | raw_return, differential_sharpe, mean_variance_utility, return_minus_{variance,cvar,downside,drawdown,turnover}, log_growth, volatility_scaled_return, differential_downside_ratio |
| **seed ladder** | tiered **[30, 100, 189, 279, 340, 403, 568]** |
| candidates | 30 per arm = 6 generations × 5 (the reflection chain) |
| **12 launch lines** | core `c1` (Opus, confirmatory) + `h3ss` single-shot floor + **10 replication legs** |
| amendments | **R1–R115**, all pre-data |
| stop | **2026-08-27** (R109), exogenous |

**⚠ `freeze.py` FORBIDS re-freezing.** A post-freeze change goes: unfreeze (`frozen: false`,
`freeze_hash: null`) → amend → fresh freeze → **bump `FREEZE_TAG`**. Forgetting the bump overwrites the
previous anchor.

### §2.3 The model roster (R101 — Okhrati's seed-parity directive)

**All 11 full-loop models run IN PARALLEL AT EQUAL SEEDS, climbing ONE common ladder in LOCKSTEP.** R101
SUPERSEDED R88's Opus-above-legs priority and the `leg_seed_tier: 30` floor.

Confirmatory author **`claude-opus-5`** (R102). Legs in queue order: `deepseek-v4-pro`, `glm-5.2`,
`qwen3.6-27b`, `qwen3.5-9b`, `haiku-4.5`, `gpt-5.6-luna`, `nemotron-3-super`, `sonnet-5`,
`gemini-2.5-flash`, `kimi-k3`. **R106**: all 11 reasoning-OFF, output caps matched at **16,384**.

**Live per-model reject rates (2026-07-30, `campaign_guards rejects`):** `qwen3_5_9b` **88 %** (expected
~83 % — the deliberate capability-gradient **BOTTOM anchor**; **its rejects are a REGISTERED FINDING, not
a fault**), `nemotron_3_super` 21 %, `qwen3_6_27b` 11 %, `gpt_5_6_luna` / `haiku_4_5` 3 %, `kimi_k3` /
`sonnet_5` 0 %.

### §2.4 R115 — the winner-eligibility execution floor

**Eligible iff `train_safe_default_count / train_safe_call_count < 0.10`.** Effect-blind by construction:
it reads an EXECUTION counter, never a performance field, and a test asserts this by inspecting the
function's source. Registered **PRE-DATA**. Raises `NoEligibleWinnerError` → reason `no_eligible_winner`.

**R115 is currently BINDING and doing its job** — see §6.

### §2.5 THE RUN LEDGER — all four runs

| run | outcome |
|---|---|
| **RUN 1** | **INVALIDATED** by the cross-line reject collision (D1). 835 records preserved as the evidence base for the post-mortem and the dissertation's disclosure. $11.65 |
| **RUN 2** | **HALTED at T+1.3 h**, zero records — stopped deliberately to register R115 pre-data. $1.29 |
| **RUN 3** | **HALTED at 3 h 26 min** having PROVED the D1 fixes (12/12 lines, 405 specs, 0 spurious abandonments, 0 ERROR). Halted only because its processes were a commit behind HEAD. $3.81 |
| **RUN 4** | **LIVE — the run of record.** Launched **2026-07-28 21:01 UTC** |

RUNs 1–3 total **$16.75**. The **canary shield** means the confirmatory `c1` line spent **$0.00** across
every relaunch; the recurring relaunch cost is the `h3ss` single-shot (~$2.5 Opus).

### §2.6 D1–D17 — every machine defect ever found (full detail: record §20, §23, §25, §28, §37)

* **D1 — cross-line reject collision (FATAL to RUN 1).** Rejects resolved MIRROR-WIDE while markers are
  keyed on the bare candidate id all twelve lines reuse. **439 of 498 abandonments (88 %) spurious**,
  36/36 on the confirmatory core. Fixed; replay condemns exactly 59, rescues 439.
* **D2 — reflection starvation.** A consequence of D1: only 10 of 241 prompts carried the reflection
  preamble. **The mechanism under study was OFF and nothing alarmed.** Hence `reflection_guard`.
* **D3 — leaked ssh children** (`proc.wait()` outside `try/finally`); failures 5.2 % → 55.3 %.
* **D4 — watchdog/backup/supervisor hardcoded RUN 1's roots.** An automatic restarter is a second launcher.
* **D5 — C3 gate collision.** Shared approval files; passing the gate CONSUMES the approval. Now scoped by
  `line_tag()` and fails CLOSED.
* **D6 — no execution floor on winner selection** → R115.
* **D7 — fail-OPEN on a malformed batch result** (`res.get("ok", True)`).
* **D8 — `stop_reason` captured but only WARN-logged** → persisted in `18dead8`.
* **D9 — the 300 s transport stall. RESOLVED:** RUN 4 did 2.2× more poll work with **1.4 vs 167.0**
  failures per 1,000 polls. Only diff: **`stdin=subprocess.DEVNULL`**. Mechanism is the *leading*
  explanation, not proven; it is a natural experiment.
* **D10 — spend mis-attribution.** 1,361 rows stamped `provider: anthropic` incl. the eight OpenRouter
  legs. Routing was never wrong; **cost attribution was**. FIXED.
* **D11 — killswitch counted an authoring reject as a task death** ⇒ false `admin_kill` blocking all
  twelve lines. **Fixing D1 armed this.** FIXED (`_APPLICATION_EXIT_RC`).
* **D12 — a line whose every arm crashed reports `LINE COMPLETE`** (gate stop returns 0). **DEFERRED.**
* **D13 — `response.choices[0]` unguarded** ⇒ `TypeError` the retry layer won't retry. **DEFERRED.**
  ⚠ Not hypothetical — it fired twice in production and cost leg7 two arms.
* **D14 — a PARTIAL arm failure is SILENT.** leg7 ran **8 h 44 m on 3 of 5 arms** (missing `scalar`, the
  H2 contrast partner) while all six guards said green. **Total failure is LOUD and self-healing; partial
  failure is silent.** Detector shipped: `docs/ops/arm_coverage.py`. **DEFERRED** (durable repair).
* **D15 — the watchdog revived lines without the substrate fence.** `mode_d_watchdog.ps1` does not pass
  `-ExcludeHosts`, so a revived line could land back on the fenced host. Worked around live by
  `docs/ops/watchdog_fenced.ps1` (a faithful copy that passes it). **DEFERRED.**
* **D16 — the C3 gate's `health_ok` does NOT see a SUBSTRATE mix.** The BLOCKING control is blind to what
  the advisory sentinel catches; the gate promises more than it verifies. **DEFERRED.**
* **D17 — the safe-default clears the reward's state, so a transient authoring bug becomes permanent.**
  **The find of the previous session — read record §37 in full.** See §6.

**THE STRUCTURAL LESSON:** D1, D5 and the `pending_specs` case are ONE shape — *a resource shared by
twelve concurrent lines, keyed by an identifier unique only WITHIN one line.* All ~2,875 tests exercise a
single line, so **no test can ever see it**; the only reliable detector is a LIVE invariant.

**The second lesson, for CH4:** every one of D1–D17 was found by **measuring the running system** —
counting abandonments per line, sampling the process table, replaying the archive, noticing an impossible
coincidence. **None was found by reading code alone.**

### §2.7 P1–P15 — previous sessions' PROCESS errors (record §20.2)

Recorded because a new session inherits the habits, not just the code: measured the wrong ssh client ·
proposed a fix that A/B testing refuted · reported a live process GONE from a `UInt32`/`Int32` lookup ·
**claimed the suite green while `PYTEST_RC=1`** · produced a false RED by editing source during a
certification run · introduced a crash path with R115 · misread the factor loader · **overstated an open
risk as unmet when it had been fixed** · put backtick content in a bash heredoc (twice) · wrote a process
filter that matched and killed its own shell · **P11–P15 (previous session):** a watcher that counted
*itself* as a 13th supervisor · a sentinel guard that printed "no outstanding verdicts" while a CRITICAL
was live (missing `import json`, swallowed by a blanket `except`) · reported `EXIT=0` when the real code
was 2 (read `$?` after a pipe) · flat accessors returning `None` because metrics are **nested** under
`record["metrics"]` · pooling `search*` across all 12 lines, which made the one binding R115 breach vanish.

### §2.8 Settled decisions — do NOT re-propose

2000-start data · options · more candidates (multiplicity) · QD / URDP / GEPA / AlphaEvolve · repo
restructure / pydantic / Snowflake / Ray · GPT-5.5 as second LLM (cost) · Fable-5 (identification threat).
**Prompt caching is PHYSICALLY INERT** on Opus (898-token shared prefix < the 4,096 floor) — disclose, do
not implement. **The `t` pool is excluded.** **A GPU is not needed** — for this tiny-MLP workload the CPU
lane beats an A100 and is actually schedulable. **Claude Council DEACTIVATED** at Tamer's request.

### §2.9 Verified-sound claims — do not re-litigate

* **CRN holds** — the seed goes INTO the SB3 kwargs; burning 1,000 draws leaves kwargs byte-identical.
* **The arms differ exactly as H2 claims** — re-derived from the **real archive** this session (§6).
* **The base prompts are tail-NEUTRAL** — the construct-validity hinge, pinned by a test.
* **B1 matched budget is real** — `train_safe_call_count` = 400,000 on **every** scored record (0 exceptions
  across 990+).
* **The mechanism pipeline reads the PROMPT**, not the empty `feedback_block`.
* **The licensed gold on ACFS sha256-matches the frozen manifest.**

---

## §3. ⚠⚠ THE SINGLE MOST IMPORTANT RULE FOR A LIVE RUN

> **⚠ RE-BASED 2026-07-30 (record §46): the running sha is now `c99716e`.** The certified memory
> relaunch (renderer sized from the measured peak → search `mem=1G`, test lane `mem=2G`) restarted the
> twelve drivers, so the drift baseline moved with them. **A relaunch does not violate the invariant
> below — it re-bases it.** Use `c99716e` in the test; the paragraph is preserved as written.

**RUN 4's executing code was `b9e6df5` on BOTH sides** — the laptop drivers started from it, and the cluster
tree was verified byte-identical (`DIFFER=0 MISSING=0` over 2,649 files) before launch.

**DO NOT EDIT ANYTHING UNDER `src/`, `scripts/`, `config/`, `prompts/` WHILE THE RUN IS LIVE.**

**The test is:**

```
git diff --name-only c99716e HEAD -- src scripts config prompts     # MUST be empty
```

**Not** `git rev-parse HEAD`. Documentation commits on top are expected, correct, and required — only
source drift matters. Run this before and after every commit. **This is why new tooling lands in
`docs/ops/`, never `scripts/`.**

**SEVEN fixes are deliberately deferred** for this reason, fully written out with code, tests and apply
order in **`docs/DEFERRED_FIXES_RUN4.md`**: **D13**, D12, preflight headroom, D14, D15, D16, **D17**.

---

## §4. READ THESE, IN THIS ORDER

1. **`docs/CAMPAIGN_EXECUTION_RECORD.md`** — the primary source. **§37** (D17, the newest and deepest),
   **§36** (⚠ the benchmark-window CORRECTION — two headline claims retracted), **§32** (the negative
   Sharpe resolved exactly), **§34–§35** (construct validity verified against the real archive), **§25**
   (D14), **§28** (D15), **§31** (R115 caught in the act), **§20** (the cross-run post-mortem + §20.2 the
   process-error ledger), **§18** (open-defect register), **§9** (analysis-time obligations — now 8).
2. `CHANGELOG.md` → **`[2026-07-30b]`**, then `[2026-07-29b]`, `[2026-07-29]`, `[2026-07-28e]`.
3. `docs/HANDOFF.md` §1 (state), §2 (standing orders), §3 (**authority map — one owner per truth**).
4. `memory/session-current-focus.md` — the ▶ NOW cursor.
5. `CLAUDE.md` — ★ PRIORITIES, the four-authority rule, the five duties (**untracked — laptop-only**).
6. `docs/DEFERRED_FIXES_RUN4.md` · `docs/REMOTE_CONTROL.md` · `docs/RUN4_STATUS.md` ·
   `docs/FEEDBACK_ASSESSMENT_2026-07-30.md` · `paper/APPENDIX_B_limitations.md` (§B.8 executed-run
   limitations) · `docs/RUN5_SESSION_PROMPT.md` (the previous brief, still valid as history).

---

## §5. LIVE STATE (2026-07-30 13:40 UTC, T+40 h 25 m — VERIFY IT YOURSELF, DON'T TRUST IT)

| item | value |
|---|---|
| launched | **2026-07-28 21:01 UTC** ⚠ **driver logs are LOCAL = BST = UTC+1** |
| lines | **12/12**, all 5 arms each — verified by `arm_coverage.py` (**ALL LINES FULL**) |
| records | **993** and climbing · `authored_rewards` = records |
| spend | **$21.42** (c1 $10.65 · h3ss $4.97 · legs $5.80) of ~$24–28 projected |
| cores computing | **~552** and fluctuating 550-660 as tasks turn over: 69 running array TASKS x 8 slots (+483 `SLAVE` rows), 106 queued, **no `Eqw`**. Count it with `qstat -t`, not `qstat` — see §9b |
| supervisors | **12** · watchdog **`watchdog_fenced` ×1** (the D15 workaround) · backup, sentinel, allocation advisor all up |
| guards | `collision` `reflection` `transport` `rejects` `status` **ok** · **`truncation` CRITICAL — ACKNOWLEDGED** (1 nemotron call hit our own 16,384 cap; excluded from the reliability denominator, cap deliberately NOT raised mid-run) |
| freeze | `3ca6f01a…` **MATCHES** · drift **0 files** |
| transport timeouts | **0** |
| budget | Anthropic **$24.64**, OpenRouter **$17.97**, key cap **$100** — ⚠ *last confirmed by Tamer 07-29; re-check* |
| stop | **2026-08-27** (27.4 days remaining) |

**★ PER-STAGE ETAs at ~600 cores** (`python docs/ops/stage_eta.py <cores>` — Tamer's standing reporting
requirement; re-run it with YOUR measured number, the table below is illustrative):

| rung | 30 | 100 | 189 | 279 | 340 | 403 | **568** |
|---|---|---|---|---|---|---|---|
| ETA @600 cores | 08-01 | 08-03 | 08-06 | 08-10 | 08-13 | 08-15 | **08-22 ✓** |
| ETA @830 cores | 08-01 | 08-01 | 08-04 | 08-07 | 08-09 | 08-10 | 08-15 ✓ |

**★ The full ladder now BEATS the Aug-27 stop with 5 days to spare** — a real improvement on the previous
brief, where 408 cores put rung 568 at 09-03, a week PAST the stop. Saturation: beyond ~4,584 cores buys
nothing; the critical-chain floor is **3.27 d**, immune to more cores.

**⚠ OUTSTANDING SENTINEL VERDICTS — all triaged in `docs/ops/acknowledged_alarms.txt`, none a stop:**
`record_sanity` **CRITICAL** (the `kimi_k3` garbage record — genuinely broken, R115-excluded) ·
`substrate_fields` **CRITICAL** (one historical 2-substrate test leg; the single Xeon-6140 host is now
fenced from all 12 lines) · `reward_scale` WARN (registered ratio-form rewards) · `capacity_accumulation`
WARN · `gate_failure_drift` WARN · `silent_hang` UNKNOWN.

**Expected rhythm — so you can tell "running" from "correct":** each line authors generation 0, then
**waits ~8 h** for training results before generation 1 can reflect. **Flat spend for hours is CORRECT**,
not a stall. Verify liveness by INFO growth in driver logs and cluster job counts, never by spend.

---

## §6. WHAT THE PREVIOUS SESSION FOUND — do not rediscover or contradict this

### ★ D17 — a fail-safe that manufactures a limit cycle (record §37, the deepest find)

`science_watch` listed nine R115 breaches and **seven read exactly `199,932 / 400,000 = 49.9830 %`** —
across **five different models, three arms, and five different exceptions**. Independent authoring defects
do not agree to the call.

**Cause:** `safe_call` substitutes `(SAFE_DEFAULT, {}, None)` on failure — and that `None` is *the reward's
own state*. A stateful reward with a cold-start branch is pinned in a limit cycle: cold-start call
succeeds → main path raises → state cleared → repeat, for all 400,000 steps.
**`period = warm-up calls + 1`**, so a two-call warm-up gives 1/2 and deepseek's `if n < 3` gives exactly
1/3 = 133,333/400,000.

**Two hypotheses were refuted by evidence first** (recorded because both were reasonable): shared
`reward_state` across vectorised sub-envs (`portfolio_env.py:222` holds it **per instance**) and a
differing vector width (byte-identical `determinism_env`).

**Established CAUSALLY:** `docs/ops/probe_safe_default_cycle.py` replays each reward twice — shipped reset
vs state preserved. Shipped reproduces the archive to **0.08 pp** with literal `.X.X.X.X`; preserving state
collapses it.

**The consequence that matters:** for `haiku_4_5` and `nemotron_3_super` the reward's main path is **sound**
— the only defect is a one-step warm-up boundary. They fail **1.0–1.75 %** with state preserved vs **50 %**
as shipped. **A fail-safe meant to contain a bad reward converted a nearly-correct one into a permanently
broken one**, biasing those models' measured authoring reliability **downward**. Verdicts over all nine:
**2 trapped / 6 genuinely broken / 1 inconclusive**.

**⚠ TWO RULES FOR THE WRITE-UP:**
1. **The safe-default fraction is NOT a severity measure.** "49.98 % fallback" reads as *half the reward
   worked*; it means the reward **never once executed its intended logic**.
2. `validate_once` calls the reward **once, from a cold start** — exactly the call that succeeds — so this
   defect is invisible to one-shot validation **by construction**. CH7's practitioner's checklist:
   *validate a stateful reward across a state transition, never on a single cold call.*

**Exposure, measured across all 979 scored records at the time:** 935 entirely clean · 29 under 1 % · **6
in the 1–10 % band that ARE scored** · 9 excluded effect-blind. **ZERO breaches on the core confirmatory
line `c1`.** Knife-edge disclosed (B.8.8): `qwen3_5_9b/placebo/placebo-g2-c2` at **39,986/400,000 =
9.9965 %** — **14 calls** below exclusion, with a 5 %/20 % floor sensitivity analysis registered.

### ★ §36 — THE BENCHMARK WINDOW WAS WRONG, AND TWO HEADLINE CLAIMS WERE RETRACTED

Benchmarks had been computed over **1,631 sessions from 2020-01-02**, but the agents traded only the
**1,571 sessions from 2020-03-30** — the R18 purge, which silently contains the COVID crash.

| | as previously claimed | **corrected** |
|---|---|---|
| market proxy | +0.773 Sharpe / +166.0 % | **+1.1656 / +274.1 %** |
| EW-30 same assets | +0.817 / +122 % | **+1.2825 / +183.3 %** |
| does turnover-pricing beat passive? | "wins" | **NO — +1.1606 vs +1.2825** |

**`+0.817 / +122 %` and `+0.773 / +166 %` must never appear again.** Six files were reconciled.
**Analysis-time obligation 8:** windows come from `record.metrics.test_returns`, **never** a panel date
filter.

### ★ §32 — the negative Sharpe, resolved exactly (Tamer's challenge, answered)

Mean **net** Sharpe **−0.1071** against mean **gross +0.9628** — a **1.07-Sharpe transaction-cost wedge**,
computed by the *registered* analytic repricing identity `net_c = gross − bps·1e-4·turnover` and
cross-validated against the archived gross series to **1.4 × 10⁻¹⁷**. `return_minus_turnover` runs at
**0.0077** turnover against ~0.89 and retains **98.8 %** of its gross Sharpe.

**The agents over-trade and bleed to costs. Pricing *risk* is not pricing *trading*.** Ten of eleven
expert rewards are net-negative (four explicitly risk-aware); **none beats passive even gross**. H2 is
unaffected (a level effect common to all arms) but the write-up must state it plainly.

### ★ §34–§35 — construct validity verified against the REAL archive for the first time

**273 reflection prompts, 0 violations, 0 tail leakage**; derangement **102/102** vs **0/24**; block lengths
byte-exact. ⚠ **`placebo` uses NEUTRAL labels** ("reference value 1..6"), **not** zeroed tail labels — a
*stronger* control than assumed. Checking for zeroed labels produces a **false MISMATCH on four legs**.

### ★ §26.3 — DIFFERENTIAL ARM ATTRITION, registered PRE-DATA, and the one to keep watching

Author-side rejects are ledgered `permanent` and **never replaced**, so an arm permanently searches fewer
than its registered 30. **Live count: 148 rejects — scalar 37, distributional 36, placebo 27,
placebo_shuffled 25, scalar_cvar5 23; spread = 14 candidates.** Run `arm_coverage.py` every update and
watch the `[attrition]` line. **Do NOT "fix" it** by re-authoring rejects — that alters the registered
candidate budget mid-run. The obligation (per-arm counts + an equal-*k* sensitivity analysis) is registered
at record §9 item 4.

### ★ The write-up work delivered (`docs/FEEDBACK_ASSESSMENT_2026-07-30.md`)

Tamer's **"Getting every criterion into the top band"** feedback was assessed and **A1–A11 all delivered**;
**A12 needs Tamer**. New artefacts: `paper/tables/T_literature_positioning.md`, `T_scale_and_difficulty.md`,
`T_design_decisions.md`, `T_arms_and_hypotheses.md`, `T_models_and_reward_canon.md`;
`paper/appendices/A_quality_control_record.md`; `paper/sections/CH1_contributions.md`,
`CH3_severity_paragraph.md`, `CH7_wider_context.md`, `RQ_canonical_and_framing.md`;
`paper/PRESENTATION_CHECKLIST.md`. **Extended, not rebuilt:** `APPENDIX_B_limitations.md` (+§B.8),
`FIGURE_TABLE_MANIFEST.md` (+T10–T17, S1–S4). `NOMENCLATURE.md` was **left alone** — already sufficient.

⚠ **Two places the feedback is factually wrong** are recorded in §8 of the assessment so the error is not
inherited — most importantly it quotes the **pre-correction** benchmark figures. Any figure it quotes must
be re-derived from the records before it enters the PDF.

### ★ Capacity — answered with measurement, not assertion

Cluster nearly **empty** (~11,644 free slots/queue); **345 hosts** satisfy our `snx=1, tmpfs=15G,
batch=true` request; `qquota` empty; `max_u_jobs=1000` not binding. **We are fair-share limited**,
consistent with the previously measured ~636-core equilibrium. **No legitimate lever remains** —
self-elevation is forbidden, priority must never be lowered, an RC request is Tamer's declined call.
`max_reservation=20` cluster-wide means most queued jobs hold no reservation.

---

## §7. HOW TAMER COMMUNICATES REMOTELY — KEEP THIS ALIVE

He is often away from the laptop. Two GitHub-based channels exist and **you must keep both running**
(verified alive at handoff):

1. **`docs/RUN4_STATUS.md`** — auto-regenerated and pushed **every 5 minutes** by `docs/ops/publish_loop.sh`
   → `docs/ops/publish_status.sh`. Phone-readable: elapsed, lines, cluster jobs, **cores computing**,
   records, spend, timeouts, guard verdict. **Pure ASCII** — non-ASCII mojibakes on his phone.
2. **`docs/REMOTE_CONTROL.md`** — he edits it on GitHub from his phone; `docs/ops/remote_watch.sh` polls
   every 5 min, and the session reads the instruction, acts, and **writes back into the LOG table at the
   bottom**.

**If the session restarts, RE-ARM BOTH** (the monitors die with the session; the campaign does not):

```
bash docs/ops/publish_loop.sh &      # 5-minute phone status push
bash docs/ops/remote_watch.sh  &     # fires only when his instruction block CHANGES
```

Push to **both** branches: `backup-2026-07-28` **and** `myriad-cluster-and-tier-system`.

⚠ **The laptop must stay home, plugged in, awake and online** — the drivers and every LLM call run on it;
Myriad only trains. Sleep is disabled on AC; Windows Update paused to **2026-09-10**.

---

## §8. THE MONITORING STACK — what is armed and what each catches

**In the repo (durable): `python scripts/campaign_guards.py <root> all`** — six guards, **exit 2 = stop the
run**. `collision` (every ledgered abandonment must trace to its OWN sub-root — the RUN 1 killer) ·
`reflection` (≥80 % of gen>0 candidates shown a reflection block) · `truncation` · `transport` · `rejects`
(per-model rate vs that model's OWN baseline — the FINDING/DEFECT discriminator) · `status`.
**Every guard was FALSIFIED before being trusted** — fires on the RUN 1 archive, silent on RUN 3.

> **⚠ THE SIX GUARDS HAVE A HOLE (D14).** Not one asks whether a line still HOLDS ITS ARMS. All six
> returned green for 8 h 44 m while leg7 ran 3 of 5 arms. **`campaign_guards all` returning RC=0 does NOT
> mean the run is whole.** Always run `arm_coverage.py` beside it.

**`docs/ops/` — durable, and NOT in the drift pathspec (this is why they live here):**

| tool | what it catches |
|---|---|
| ⭐ `arm_coverage.py` | **the D14 detector** — per-(line, arm) batch coverage from the `batches/` REGISTRY (the archive dir listing **LIES**); + the `[attrition]` line; + surfaces sentinel verdicts |
| ⭐ `science_watch.py` | the SCIENCE: search spread, reflection chain, **R115 breaches with per-line binding detection**, step budget, impossible numbers |
| `campaign_watch.py` | change-only health watcher keyed on a state signature — named failing guards, supervisors, substrate-mixed units, crash exception KINDS |
| `probe_safe_default_cycle.py` | **the D17 evidence probe** — replays archived rewards shipped-vs-state-preserved |
| `cost_decomposition.py` | the gross/net Sharpe decomposition (§32) |
| `verify_arm_manipulation.py` · `verify_derangement.py` | construct validity from the real archive (§34–§35) |
| `watchdog_fenced.ps1` | the D15 workaround — a faithful `mode_d_watchdog.ps1` that passes `-ExcludeHosts` |
| `stage_eta.py` | **per-rung ETAs** — Tamer's standing reporting duty. `python docs/ops/stage_eta.py <cores>` |
| `science_sanity.py` | **stage-aware** — test-leg records score on `test_sharpe`, search on `val_fitness` |
| `publish_loop.sh` · `publish_status.sh` · `remote_watch.sh` | the two remote channels (§7) |
| `close_watch.sh` · `run4_watch.py` · `status_report.sh` · `remote_status.sh` | change-only watcher + dashboards |
| `spend_split.py` · `openrouter_key_info.py` | spend attribution + key cap |
| `Send-Remote.ps1` | base64 ssh transport — **the naive pipe corrupts payloads** |
| `acknowledged_alarms.txt` | the triage file — an alarm listed here is known, not new |

> **These were made durable at this handoff.** They previously lived in a session-specific scratchpad that
> would have been garbage-collected — a real gap, now closed. Hardcoded dead-session paths in
> `close_watch.sh`, `publish_loop.sh` and `science_sanity.py` were repaired and re-verified to run.

⚠ **`qdel` is blocked by the harness safety classifier.** If a cluster job genuinely must be deleted it is
Tamer's call — surface it, **never route around the block**. In the D14 case the block was fortunate: the
deletion would have been the WRONG action.

---

## §8b. OPERATING THE LIVE RUN — stopping, restarting, and the exact commands

### The STOP lever, and what it really does

```
outputs\campaign_cluster_run4\STOP_CAMPAIGN        <- create this file
```

**It stops RESTARTS, not a running driver.** A FULL halt is an ORDERED sequence, because the watchdog
revives dead lines every 300 s:

1. write `STOP_CAMPAIGN` into the run root;
2. **kill the watchdogs FIRST** (else they resurrect everything you kill next), verify 0 remain;
3. kill the 12 supervisors → then the drivers → then backup, sentinels, advisors;
4. on the cluster, delete campaign jobs **by explicit job ID only** — **NEVER `qdel -u ucestes`**.

Every process query MUST exclude `$PID`.

### Relaunching (only if genuinely required)

```
powershell -ExecutionPolicy Bypass -File scripts\mode_d_launch.ps1 `
  -OutDir outputs\campaign_cluster_runN -RemoteRoot ~/Scratch/llmrpN
```

**BOTH root flags are mandatory** — every entrypoint defaults to RUN 1's paths, and omitting one silently
rejoins a halted run whose archive-truth resume would adopt its records. Then, with the SAME roots:
`docs/ops/watchdog_fenced.ps1 -IntervalSecs 300` (**not** the unfenced `mode_d_watchdog.ps1` until D15
lands), `campaign_backup.ps1 -SrcRoot`, `sentinel.py <root> --watch --interval 300`,
`allocation_advisor.py --host myriad --watch 900 --archive-root <root>`.

**ORDER: launcher FIRST → verify 12 supervisors → THEN the watchdog.**

⚠ **A rolling relaunch strands `.driver.lock` files.** Windows recycles PIDs, so a stale lock whose PID now
belongs to an unrelated process false-positives and crashes the arms. Validate the lock owner before
clearing (last time: 22 cleared, 38 correctly kept).

### The daily commands you will actually use

```
python scripts/campaign_guards.py outputs/campaign_cluster_run4 all    # exit 2 = stop the run
python docs/ops/arm_coverage.py                                        # D14 — guards cannot see this
python docs/ops/science_watch.py                                       # the science invariants
git diff --name-only c99716e HEAD -- src scripts config prompts        # MUST stay empty
python scripts/freeze.py --check                                       # RC=0 + hash MATCHES
python docs/ops/stage_eta.py <measured_cores>                          # per-rung ETAs
ssh myriad "qstat -u ucestes | awk 'NR>2 && \$5==\"r\" {c+=\$9} END {print c+0}'"   # running slots
```

### The C3 review gate — an EXPECTED event, not a fault

A weak leg may stop at the C3 gate awaiting human approval; the gate reads only **execution health**
(effect-blind) and **fails CLOSED**. To clear it: read `tier1_integrity_<line_tag>.md`, then create
`<read_root>/TIER1_APPROVED_<line_tag>`. **Staleness-checked** and **consumed on passage**.

### If the laptop reboots or the session dies

**The campaign survives a session death.** It does **not** survive a laptop reboot: re-launch supervisors on
the SAME roots with `--resume` (archive-truth resume re-authors only what is missing, so nothing is
double-billed).

---

## §9. TRAPS THAT COST PREVIOUS SESSIONS TIME — do not repeat

1. **`qstat` truncates job names to 10 chars** (`c1_baselin`) — `grep 'c1_baselines'` returns zero.
2. **`qstat` columns are state-dependent**: running rows `NF=10` (`$9`=slots), queued rows `NF=9`
   (`$9`=task-id). Summing `$9` blindly reads TASK-IDs as slots.
3. **`qacct -j <name>` is NOT run-scoped** — RUNs 1/3/4 share `c1_` tags. Disambiguate by end time.
4. **Driver logs are LOCAL time (BST = UTC+1).** Never do arithmetic on a log timestamp without converting.
5. **Driver logs carry TWO logging formats.** Counting one undercounts badly (RUN 3: "0 ERROR / 41 WARNING"
   vs the truth, 1 / 798).
6. **Test-leg records carry `val_fitness = nan` legitimately** — they score on `test_sharpe`.
7. **The search lane runs 8 threads, the test lane 1.** Don't compare a search wall-clock to the 1-thread
   8.09 h planning figure.
8. **A full-tree cluster deploy is impractical on a contended login node** (~40 files/min). Use the delta
   method (record §23.12) and prove it with the sha256 manifest.
9. **Alarms keyed on COUNTS cry wolf**; key them on **kinds**.
10. **Any process query must exclude `$PID`.**
11. ⚠ **Do not re-explain why a granted permission cannot be used.** Tamer wrote *"Why dont you listen to
    what I say???"* after I argued about core counts instead of measuring. I then measured and found pool d
    at **5.6 % CPU load**. **Measure first, act, then report.**
12. **Record metrics are NESTED** under `record["metrics"]` — flat accessors silently return `None` for
    everything (P13).
13. **Never read `$?` after a pipe** — use `PIPESTATUS[0]`. Reported `EXIT=0` when the real code was 2.
14. **The Windows console is cp1251** — printing `⚠`/`→` from Python raises `UnicodeEncodeError` and can
    abort a script *before it writes its file*. Keep console output ASCII; put Unicode in the file only.
15. **Never glob `search*` when checking a per-line invariant** — pooling across the 12 lines made the one
    binding R115 breach vanish.
16. **`placebo` uses NEUTRAL labels, not zeroed ones.** Checking for zeros yields a false MISMATCH.
17. **A silent cap in your own tooling is a defect.** `science_watch` printed `r115[:8]` while the count
    line said 9 — the hidden one could have been on the confirmatory line. **If you cap, report what you
    dropped.**
18. **Local quarantine of archive records does nothing** — the archive is a mirror; `pull_archive` restores
    them. Deletion is also the wrong instrument.
19. **`spec_run_id` returns the candidate_id, not a source hash** — so queued jobs DO satisfy new specs. A
    `qdel`-and-resubmit plan built on the opposite assumption was wrong on the merits.

---

## §9b. ★★★ THE CORE-COUNT INVESTIGATION — TAMER'S TOP PRIORITY, HANDED TO YOU UNFINISHED

**Tamer's standing target is 4,000+ cores. We are at ~552.** He has asked repeatedly (*"why are we
asking for so low"*, *"we were aiming for 4k+, please ultarthink"*). **This is your highest-priority
operational task.** Everything below was measured first-hand, and the previous session's FIRST answer
was **WRONG** — that correction is the most useful thing in this section.

### What was measured (2026-07-30, all first-hand)

| fact | value | how |
|---|---|---|
| our running cores | **552** — 69 master tasks × 8 slots, plus 483 `SLAVE` rows | `qstat -t -u ucestes` |
| our queued trainings | **106** | same |
| **pool-d total** | **9,432 cores** over **262 hosts** | `qhost`, cols `NCPU=$3`, `LOAD=$7` |
| **pool-d loaded** | 5,251 | same |
| **pool-d FREE** | **4,181 cores — 44.3 % idle** | same |
| hosts with ≥8 contiguous free slots | **150** → room for **327 more `smp 8` tasks = 2,616 cores** | same |
| scheduler verdict on a queued job | **"found possible assignment with 8 slots"** | `qalter -w p <jobid>` (verify-only; sets nothing) |

### What was RULED OUT, each with evidence

* **NOT fair-share.** The previous session concluded *"fair-share limited"* and that was **WRONG**.
  `qalter -w p` reports a queued job is schedulable **right now**.
* **NOT a per-user quota.** `qquota -u ucestes` is **empty**; `max_u_jobs = 1000` while we hold ~175.
* **NOT tmpfs or snx.** Hosts advertise `tmpfs=1500G`, `snx=10000`, `slots=36` — our `tmpfs=15G`
  request allows ~100 jobs per host, not the 2 we observe.
* **NOT our own `-tc`.** `src/cluster/jobscript.py:178` sets `tc = max(1, int(n_tasks))` on the CPU
  lane — the **full** array width, so we impose no per-array concurrency cap.
* **NOT host exclusions.** Only two hosts are fenced: `!node-d00a-230 & !node-d00b-024`.
* **NOT "the cluster is empty" either.** An earlier reading of `qstat -g c` counted the *same* free
  slots once per queue instance and wildly overstated headroom. **Use `qhost` filtered to pool-d
  hostnames** (`/^node-d00[ab]/`), never `qstat -g c`.
* ⚠ **Two column traps burned the previous session here.** `qhost` is
  `HOST ARCH NCPU NSOC NCOR NTHR LOAD …` — summing `$4`/`$5` gives *sockets* and *cores*, not cores and
  load, and yields nonsense like "524 cores over 262 hosts". And in `qstat -t`, blank-state rows are
  **`SLAVE` slots of parallel jobs**, not queued work.

### THE ACTUAL CONSTRAINT — and where to look

**We only have ~175 trainings in flight.** Even if every queued task dispatched instantly that is
175 × 8 = **1,400 cores** — nowhere near 4,000. **The cap is how much work WE generate, not what the
cluster will give us.** Reaching 4,000+ needs roughly **500 concurrent `smp 8` tasks**.

The structural reason is the **reflection loop**: 12 lines × 5 arms = 60 chains, and generation *g+1*
cannot start until generation *g*'s trainings return (~8 h). But *within* a generation the available
parallelism is large — at rung 30 a full generation is 5 candidates × 30 seeds × 60 chains, far more
than 175 tasks. **So the question to answer is: what limits how many specs are submitted concurrently
per batch?** Start at `src/cluster/` (the driver's batch/submission policy) and
`run_campaign_cluster.py` — **READ ONLY while the run is live.**

### The constraints on any fix — read before acting

1. **`src/`, `scripts/`, `config/`, `prompts/` are DRIFT-FENCED while RUN 4 is live** (§3). A change to
   submission policy is a **restart-time** change, and a relaunch costs the `h3ss` re-author (~$2.5,
   the `c1` canary shield keeps the confirmatory line at $0.00). Weigh that against the gain and **put
   the arithmetic in front of Tamer** rather than deciding silently.
2. **Never lower priority. Never self-elevate. Never `qdel -u ucestes`** — delete by explicit job ID only.
3. **Deeper packing must not break CRN.** Every comparison unit stays substrate-**homogeneous**, and
   `-pe smp 8` with `OMP_NUM_THREADS=8` sits **inside the frozen determinism envelope** — **do not
   change the thread count to fit more jobs.** More cores must come from **more concurrent tasks**,
   never from different arithmetic.
4. Saturation from the registered model is **~4,584 cores** at rung 568; beyond that more cores buy
   nothing, and the **critical-chain floor is 3.27 d**, immune to any core count.

### Why it is worth doing

At **552** cores the ladder reaches rung 568 on **08-22**; the model puts **830** cores at **08-15**.
More rungs banked = a stronger result = the grade. **Report cores and per-rung ETAs in every update**
(`python docs/ops/stage_eta.py <cores>`). If you find the lever, bring Tamer the measurement and the
cost, then act — do not re-litigate a permission he has already granted (§9 trap 11).

---

## §10. OPEN THREADS — pick these up

1. **⚠ A12 — the public OSF/Zenodo DOI deposit NEEDS TAMER.** A **registered** freeze-day obligation
   (`freeze_day_checklist_additions.public_deposit`) that appears **UNMET**. It is an account action and
   the cheapest possible answer to the rubric's publishability yardstick. **Surface it every update.**
2. **⚠ R96 psychometric activation scope — resolve IN WRITING before any spend.** The module is
   **specified but NOT built**, and its all-or-nothing clause may commit **both** axes (≈**$23–37**, not
   $10). It also gates the strongest candidate for the single carrying figure; a fallback (baseline Sharpe
   distribution ordered by turnover) is named and buildable from archived data.
3. **Apply `docs/DEFERRED_FIXES_RUN4.md` at the next natural restart — never live.** **Seven** items: **D13**, D12,
   preflight headroom, D14, D15, D16, **D17**. D12 and D14 must be decided together (both hinge on the exit
   code). Each needs its falsifiable test **proven to FAIL against the current code first**.
4. **★★★ RAISE THE CORE COUNT TOWARD 4,000+ — see §9b, Tamer's top priority, handed over unfinished.**
   2,616 cores of pool-d headroom are free RIGHT NOW and the scheduler says our queued work is
   dispatchable; the cap is how much work WE generate. **Capacity reporting is a standing duty** — cores +
   per-rung ETAs in every update. At ~600 cores the ladder lands rung 568 on **08-22**, inside the
   Aug-27 stop. If it degrades, say so immediately.
5. **§26.3 differential arm attrition** — watch the `[attrition]` line every update as generations
   accumulate. Currently a 14-candidate spread. Do not "fix" it.
6. **Watch for the C3 review gate** on weak legs — DESIGNED, not a fault.
7. **§24.6's turnover question** — testable from `test_turnover`, already captured on every record; §32
   makes it the leading candidate for the principal axis an LLM-authored reward can exploit.
8. **The write-up.** ~5,900 words of prose need no results (intro/literature/data/methodology) and can be
   written **now**; the new tables carry the comparisons so the prose does not have to. **C5** (the
   confirmatory answer) is intentionally an empty slot until the sealed test is scored. Walk
   `paper/PRESENTATION_CHECKLIST.md` once the figures are final — Criterion 4's *"faultless presentation of
   data"* is the 80→90 boundary on a quarter of the mark.
9. **R107's 2.72× thread speedup looks optimistic** — observed ~2.03× on n=2. Re-measure as records land.
10. Register items **L** (factor ladder 21/1631, report-only), **M** (tag annotated not signed), **N** (2
    commits authored `abailey81` with Tamer's email — cosmetic, HIS call).

---

## §11. YOUR FIRST ACTIONS

1. Read §4's list in order. **Do not skim §37, §36 or §32.**
2. Say **"Resuming from: … — next: …"** and continue. **Do not ask what to do.**
3. **Verify the live state yourself** — `campaign_guards all`, `arm_coverage.py`, `science_watch.py`, the
   drift test, `freeze --check`, cluster `qstat`. **Trust nothing in §5 without re-running it.**
4. **Re-arm the monitors** if they are not running (`publish_loop.sh`, `remote_watch.sh`, and check the 12
   supervisors + `watchdog_fenced`).
5. Report to Tamer with **timeline · cores · jobs · per-stage ETAs** — his standing format — and **begin
   every message with "Tamer"**.
6. Keep documenting **as it happens**: CHANGELOG + record + HANDOFF §1 + cursor. **Push to BOTH branches.**

**The campaign does not depend on you.** Supervisors relaunch, the watchdog revives dead lines every 300 s,
the sentinel watches health. If your session dies, the run continues — a fresh session resumes from this
file. **What depends on you is that it stays *honest*: measured, cross-checked, and reported without
over-claiming.** The last two sessions each found a real defect by refusing to accept a number that did not
make sense. That is the job.
